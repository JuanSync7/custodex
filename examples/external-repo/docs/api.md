---
cdm:
  audience: eng-guide
  fingerprint: 636b0c04f100e220
  schema_version: 1.0.0
---
# Widget API

> The public surface of the `widget` library — its functions and constants.

This is an **engineering guide** for an adopter repo. The managed `symbols`
region below is generated by `cdmon` from `src/widget.py`; a change to a public
signature drifts this table until `cdmon monitor --apply` regenerates it.

<!-- CDM:BEGIN symbols -->
| symbol | kind | signature |
|--------|------|-----------|
| DEFAULT_WIDTH | variable | DEFAULT_WIDTH = 80 |
| make_widget | function | def make_widget(label: str, *, width: int = DEFAULT_WIDTH) -> str |
| widget_area | function | def widget_area(width: int, height: int) -> int |
<!-- CDM:END symbols -->
