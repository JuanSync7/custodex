---
cdm:
  audience: user-guide
  fingerprint: c280e8f1cab354d2
  schema_version: 1.0.0
---

# Companion tools

> The switches accepted by the shell (`batch.sh`) and tcl (`gui.tcl`) helpers.

This single table is harvested from **two different languages** at once — shell
`getopts` and tcl `regexp` argument matching — proving one doc can track a
multi-language surface.

<!-- CDM:BEGIN switches -->
| Switch |
|---|
| -G |
| -Q |
| -T |
| -f |
| -g |
| -h |
| -n |
| -q |
| -t |
| -v |
<!-- CDM:END switches -->

Back to the [command-line options](cli.md).
