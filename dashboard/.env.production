# Production build = the single-origin deploy: FastAPI serves this SPA AND the API
# on the same port (:33333). So the client talks to the API at the origin root
# ("/repos", "/health") — the trailing slash is stripped to "" by the client.
# Dev (`npm run dev`) ignores this file and falls back to "/api" (Vite proxy).
VITE_API_BASE=/
