# Slice E-04 — SQLAlchemy DB layer (Postgres-first) + Alembic

EPIC E. Swaps the E-03 `InMemoryStore` for a real **SQLAlchemy** store behind the
SAME `Store` Protocol — Postgres-first in prod, but the default offline suite runs
it on **in-memory SQLite** (K4/K9), with a `pg` pytest marker (mirroring
`live_llm`) for the real-Postgres CI run. Persists repos, records, resolutions,
coverage snapshots.

## Goal (validable)
A `SqlStore` over a fresh SQLite engine satisfies the `Store` Protocol contract
(the E-03 server tests pass against it, swapped via `create_app(store=...)`); a
register→ingest→read round-trip persists across store instances sharing one engine
(real persistence, not a dict); the Alembic migration creates all tables and
`downgrade` drops them (up/down round-trip); the SAME suite is marked `pg` to run
against `$CDMON_DATABASE_URL` Postgres in CI (skipped by default). Proven by:
SqlStore contract tests on SQLite, a migration up/down test, and the E-03 server
tests re-run against `SqlStore`.

## Packaging
- Add `sqlalchemy>=2.0`, `alembic` to the `[server]` extra (and `[dev]` so the
  gate runs them). Postgres driver `psycopg[binary]` belongs in `[server]` too but
  is only needed for real PG — document that SQLite (stdlib) needs no driver.
- Extend pytest `addopts` to also exclude `pg` by default: `-m "not live_llm and
  not pg"`. Register the `pg` marker in config.

## In scope — `code_doc_monitor/server/`
- `db.py` (or `models.py`): SQLAlchemy 2.0 declarative models — `repos`,
  `records`, `resolutions`, `coverage_snapshots`. **Postgres-first portable types:**
  use a JSON column (`JSONB` on PG via `postgresql.JSONB().with_variant(JSON, "sqlite")`
  or SQLAlchemy's `JSON` type) to store the full shared pydantic model
  (`ReviewRecord`/`ResolutionRecord`) for K6 additivity, PLUS indexed scalar
  columns for querying (`repo_id`, `doc_id`, `verdict`, `drift_kind`, `audience`,
  `detected_at`, `source_sha`). Document this "indexed columns + full JSON" hybrid.
- `SqlStore(engine)` implementing `Store` (E-03 Protocol) + methods for resolutions
  + coverage snapshots (even if endpoints land in E-05). Deterministic ordering
  (e.g. by `detected_at`/insertion id). Sessions managed per-call or via a factory.
- `engine_from_url(url)` / a small factory; `create_all(engine)` for dev/tests.
- `alembic/` migration env + an initial migration mirroring the models; the env
  reads the URL from `$CDMON_DATABASE_URL` (default a local sqlite file for dev).

## Test plan (`tests/test_db.py`)
- SqlStore contract on `sqlite:///:memory:` (or a temp file engine): add_repo →
  get_repo/list_repos; add_record → records_for; unknown repo handling matches the
  Store contract; resolutions + coverage CRUD.
- persistence: two `SqlStore` instances on the same file engine see the same data
  (proves it's not in-memory dict).
- the FULL `ReviewRecord` round-trips through the JSON column byte-for-byte
  (including additive fields like `source_sha`).
- migration up/down: run Alembic `upgrade head` then `downgrade base` on a temp
  SQLite DB; assert tables created then dropped.
- E-03 server tests re-run against `create_app(SqlStore(sqlite_engine))` (a
  parametrization or a second fixture) → identical behavior.
- `pg` marker: a copy (or parametrization) of the contract test marked `@pytest.mark.pg`,
  skipped unless `$CDMON_DATABASE_URL` is a Postgres URL — it must SKIP cleanly by
  default (assert it's deselected by `addopts`).
- Additive: all prior tests green. server+db ≥90%; whole-pkg ≥90%. The real-PG
  driver call is the only acceptable uncovered leaf (K4 — like live_llm).

## CI (`.gitlab-ci.yml`)
- Add a `tests:pg` job (manual/scheduled, like `tests:live-llm`) that spins a
  `postgres` service, sets `$CDMON_DATABASE_URL`, runs `pytest -m pg`. The default
  offline job stays SQLite-only.

## Dogfood
`server/` not tracked → confirm `cdmon check` exits 0. Pin `db.py`/`SqlStore` in
ARCHITECTURE.md first. Update SPEC "Central server" section (DB + migrations).

## Out of scope
Query filters/status/coverage ENDPOINTS (E-05 — this slice gives the store
methods + tables). Auth (E-06). The dashboard (F). Connection pooling tuning.

## Constraints
K0 (sqlalchemy/alembic under `[server]` extra; core unchanged), K4/K9 (offline
SQLite default suite; real PG behind the `pg` marker), K6 (store the SHARED schema;
JSON column keeps it additive — old rows parse after a field is added), K10
(deterministic ordering), K8, K9.
