# Slice D-03/D-04 — similarity retrieval + agent few-shot exemplars

EPIC D. Mines the resolution substrate (D-01/02): retrieve the most-similar past
**resolved** drifts and feed them to the agent backend as few-shot exemplars, so
remediation improves from history. Builds on D-01/02.

## Goal (validable)
(D-03) `rank_similar(target, records, resolutions, top_n=3)` returns the N most
similar PAST RESOLVED records to a target drift/record, deterministically, with a
planted near-duplicate ranked first and unresolved records excluded. (D-04) when
exemplars are present, the agent backend's composed prompt INCLUDES them (a new
prompt artifact), proven offline with a fake driver; with none present the prompt
is byte-identical to today (additive). Proven by ranking tests + an agent
prompt-composition test.

## In scope
### D-03 — `custodex/similar.py` (new, pure, deterministic)
```python
class Exemplar(BaseModel):              # frozen, extra="forbid"
    record: ReviewRecord
    resolution: ResolutionRecord
    score: float

def rank_similar(
    target: ReviewRecord,               # or a Drift — pick one, document it
    records: list[ReviewRecord],
    resolutions: list[ResolutionRecord],
    *, top_n: int = 3,
) -> list[Exemplar]: ...
```
- Population = records whose `record_id` is in `resolved_index(resolutions)`
  (RESOLVED only). Never the target itself.
- Score = deterministic weighted sum of feature matches: same `doc_id`, same
  `drift_kind`, same `audience`, same `region_id`, exact `surface_hash` match.
  Document the weights. Tie-break: higher score, then more-recent `resolved_at`,
  then `record_id` (total order → deterministic, K10). Pure, no I/O.

### D-04 — agent few-shot exemplars (additive)
- `backends.FixRequest` gains `exemplars: tuple[Exemplar, ...] = ()` (additive;
  default empty ⇒ every existing backend test unchanged).
- `monitor.run`: OPT-IN retrieval (a `Monitor(use_exemplars=False)` flag +
  resolutions-log path, default OFF so existing behavior is byte-identical). When
  ON, before `backend.propose`, read the review log + resolutions, `rank_similar`,
  attach to the `FixRequest`. Keep it cheap; default OFF.
- `agent/` — a new prompt artifact `EXEMPLARS.md` (template/instruction) +
  `select_artifacts` includes it only when `req.exemplars` is non-empty +
  `render_context` renders the exemplars (drift → resolution, esp.
  `resolved_text` for OVERRIDDEN). MockBackend IGNORES exemplars (stays
  deterministic). Offline fake-driver test asserts the exemplar text appears in
  the composed prompt when present, and is absent otherwise.

## Test plan
- `rank_similar`: planted near-duplicate (same doc_id+drift_kind+region+audience)
  resolved → ranked #1; an unrelated resolved record lower; UNRESOLVED records
  excluded; target excluded from its own results; `top_n` cap; deterministic
  tie-break (two equal-score → stable order); empty population → [].
- agent (offline, fake driver): `FixRequest` with exemplars → composed prompt
  contains the exemplar's resolution text + the `EXEMPLARS.md` framing; without
  exemplars → prompt has no exemplar section (and equals the pre-D-04 prompt).
- monitor: `use_exemplars=False` (default) → identical to today (existing tests
  green, no log read); `use_exemplars=True` with a seeded log+resolutions →
  FixRequest carries ranked exemplars.
- Additive: ALL prior tests green. ≥90% new code; gate ≥90%.

## Dogfood
`backends.py`/`monitor.py` are tracked (remediation). Adding `exemplars`/the flag
WILL drift → reheal after green; `cdx check` + `cdx lint` exit 0; re-run full
suite post-reheal. The `agent/` subpackage is the opt-in `[agent]` extra (K0) —
keep the lazy-import boundary; EXEMPLARS.md ships with it. Pin contracts in
ARCHITECTURE.md first.

## Out of scope
Promotion to deterministic rules (D-05/06). The central server consuming
exemplars (EPIC E). Embedding/vector similarity — the feature-match score is
sufficient and offline/deterministic (note vector retrieval as a future option).

## Constraints
K0 (no new dep — feature-match, not embeddings; agent stays the `[agent]` extra),
K4 (offline, fake driver in tests), K6 (FixRequest additive), K10 (deterministic
ranking), K9. Default-OFF retrieval keeps the whole change additive.
