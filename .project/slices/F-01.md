# Slice F-01 — dashboard scaffold (Vite+React+TS) + repos-status page

First EPIC-F slice. Stands up the `dashboard/` SPA, a typed API client for the
EPIC-E endpoints, and the first page (repos with status). Its OWN test gate
(Vitest) — the Python `pytest` gate is untouched. Node v24 + npm 11 are available.

## Goal (validable)
`cd dashboard && npm ci && npm run lint && npm run test && npm run build` all pass:
the app renders a list of repos with each repo's status (total records, by-verdict,
unresolved, coverage %) fetched from the central API; component tests mock the API
and assert the rendered repos + a loading and an error state. Proven by Vitest
tests + a clean production build.

## In scope — new `dashboard/` directory
- Scaffold: Vite + React + TypeScript (`react-ts` template). `package.json` scripts:
  `dev`, `build`, `lint` (ESLint), `test` (Vitest), `test:run` (CI mode).
- Vitest + @testing-library/react + jsdom; a mock-fetch layer (MSW or a typed fake
  `fetch`/client injected) — NO real network in tests.
- `src/api/client.ts`: a typed client for the EPIC-E endpoints used here:
  `GET /repos` → `RegisteredRepo[]`, `GET /repos/{id}/status` → `RepoStatus`.
  Base URL from an env var (`VITE_API_BASE`, default `/api`) with a Vite dev proxy
  to the FastAPI server. Reads are OPEN (no auth header needed — per E-06).
- `src/types.ts`: TS types mirroring the server models — `RegisteredRepo`,
  `RepoStatus` (`{repo_id, total_records, by_verdict, escalations, unresolved,
  last_detected_at, coverage_ratio}`). Hand-write now; note that `cdx schema`
  can generate record types later (F-02+).
- `src/pages/Repos.tsx`: fetches repos + each status, renders a table/cards with
  loading + error states. Keep styling minimal (plain CSS / a tiny system).
- `src/App.tsx` + router stub (a single route now; F-02/03 add routes).
- `README` note + `.gitignore` for `node_modules`/`dist`.

## Test plan (`dashboard/src/**/*.test.tsx`, Vitest)
- `Repos` renders the repos returned by a mocked client (assert repo ids + a
  status number visible).
- loading state shown before the promise resolves; error state shown when the
  client rejects.
- the API client builds the right URL/path (unit test with a fake fetch capturing
  the request).
- `npm run build` produces `dist/` with no TS errors; `npm run lint` clean.

## Wiring to the backend (no Python change required)
- Document running both: `uvicorn custodex.server.app:app` + `npm run dev`
  with the proxy. Optionally note that the server can serve `dist/` later (G/F-05);
  NOT required this slice.

## Out of scope
Drift timeline (F-02), coverage view (F-03), resolve action (F-04), health
(F-05). Real e2e/Playwright (optional later). Do NOT modify `custodex/`
or the Python gate — this slice is frontend-only. Do NOT add the dashboard to
`cdmon.yaml` (it's TS, not tracked).

## Process / constraints
- The PYTHON gate must stay green and UNCHANGED: after finishing, run
  `.venv/bin/pytest -q` to confirm 648 still pass and `cdx check` exits 0 (you
  changed no Python, so this is a guard, not a reheal).
- Keep deps lean + pinned (`package-lock.json` committed to the tree). Prefer the
  Vite/Vitest defaults; avoid heavyweight UI frameworks for F-01.
- Pin the dashboard architecture (structure, API client contract, test approach)
  in `.project/spec/ARCHITECTURE.md` (a new "Dashboard (EPIC F)" section) BEFORE
  building.
- Record: STATUS.md row, LESSON_LEARNT.md entry (Vite/Vitest setup gotchas), tick
  ROADMAP F-01. If npm install needs network and fails, STOP and write
  `.project/problems/F-01-HANDOFF.md` (don't leave a half-scaffold).

## Validable gate (this slice's "green")
```
cd dashboard
npm ci            # or npm install on first scaffold (then commit lock)
npm run lint
npm run test:run  # Vitest, all green, no network
npm run build     # tsc + vite build, dist/ produced, zero TS errors
```
plus `.venv/bin/pytest -q` still 648 passed (Python untouched).
