# custodex — status board

Validation command (the repo-wide gate):

```bash
.venv/bin/ruff format --check .
.venv/bin/ruff check .
.venv/bin/mypy custodex
.venv/bin/pytest -q --cov=custodex --cov-branch
```

| slice | title | status | evidence | notes |
|-------|-------|--------|----------|-------|
| TDOC-∑ | EPIC TDOC — test→test-doc mirror (config + dogfood + demo 1:1 + frontend) | DONE | **The same machinery that syncs source→docs now syncs tests→test-docs — with NO engine change (a test file is just a `.py` file, so `extract`/`drift`/`heal`/`coverage` already handle it; K0).** New `config/cdmon/tests.yaml` (dogfood, `dir-covered: [tests/smoke]`) → 2 test-docs under `test-docs/smoke/`; `demo/config/cdmon/tests.yaml` (`dir-covered: [tests]`) → 4 test-docs under `demo/test-docs/`, 1:1 with the demo test files. New `FEAT-CONFIGV2-017` + `DEMO-058` + tagged `tests/system/test_testdoc_mirror.py` → **trace 199/199**. Frontend `grouping.ts` gains `isTestDocPath`/`partitionDocs` → a **Test docs** section on Documents/Drift/Mapping (mirrors the README section). Demo coverage 80%→**88.9%** (`tests` unit 100%; the deliberate `scheduler.py` source gap preserved so the walkthrough's link→100% still holds). **FULL GATE GREEN:** ruff fmt+check clean, mypy clean (53 files), full offline pytest exit 0 (branch coverage **97.69%** ≥90), dogfood+demo `cdx check`/`lint`/`coverage`/`index` clean, `cdx wiki --check` fresh, `astro check` 0 errors. | Count-pins were the #1 ripple (as ever): both `_copy_dogfood_tree` helpers had to also copy `test-docs/`+`tests/smoke/`; `demo_as_git.py`'s `DEMO_HISTORY` needed `test-docs` folded into the adopt-cdx commit (else a 9th catch-all commit); `_DEMO_DOC_IDS`/`_DEMO_CODE_REFS`/`document_count`=8/`code_ref_count`=11 + two `== {…}` doc-id sets + `percent_files`=88.9/`summary.percent`=88.89 + `test_editable_tree` units=3; renaming a test re-staled `TEST_WIKI.md`. Decision: the eng-only `api-index` LISTS the eng-guide test-docs (consistent — it lists every eng-guide doc; the README is excluded only because it is `user-guide`); the user-facing *separation* is the console **Test docs** section. |
| GIT-∑ | EPIC GIT consolidation — dogfood docs/catalog/wiki + full gate | DONE | **The 3 new modules documented + cataloged + the demo-as-git e2e (the user's ask).** `config/cdmon/core.yaml`: `gitfetch`/`secrets`/`gitauth` added to the `central-client` doc's code_refs → their public symbols are now in `docs/api/central-client.md` (rehealed) so self-coverage counts them. New `feature-doc/catalog/gitsync.yaml` (4 `FEAT-GITSYNC-*` features) fixes the R-07 module-orphan check; tagged into `test_gitfetch`/`test_secrets`/`test_gitauth`/`test_pr` + a new DEMOS.md journey **M** (DEMO-052…055) → trace stays complete. Wikis regenerated. **FULL DOGFOOD GATE GREEN (host load eased — the scans ran):** `cdx check` clean, `cdx lint` clean, `cdx coverage --fail-under 95` exit 0, `cdx trace --fail-on-gap` **196/196**, `cdx wiki --check` fresh. Broad pytest sweep: **288 passed** across every GIT-touched file + neighbors (full-suite run on CI). ruff+mypy clean on all touched files. | The EPIC-R meta-gates (`coverage`/`trace`/`wiki`) DID run on this host this time (the load that starves them is intermittent). Documenting the new modules in `central-client` (not waiving them) keeps the catalog honest; the 4 `FEAT-GITSYNC-*` features are tested AND demoed (the demo journey describes the real, e2e-tested clone→sync→docs-PR scenario over `demo/`). |
| GIT-05 | `gitauth.py` short-lived App/OAuth token minting + route resolution — PHASE 2 of EPIC GIT | DONE (focused-green; full gate CI) | **`gitauth.py` 100%** (58 stmts, 10 branches): `github_app_jwt` (real RS256, verified against a GENERATED test RSA key + payload `iss`/`iat`/`exp`; loud on bad PEM AND on a non-RSA key), `mint_github_installation_token` (JWT→`POST /app/installations/{id}/access_tokens`→token, loud on no token, custom api_url), `mint_gitlab_oauth_token` (refresh-token grant), `mint_provider_token` dispatcher (github-app / gitlab-oauth / unknown-kind / bad-JSON / missing-field all loud). **14 unit tests** `tests/unit/test_gitauth.py` (incl. the K0 subprocess boundary: core never imports `gitauth`, import stays lazy until a mint). **Route wiring:** `RepoIdentity.provider_kind` (additive, last); `app._resolve_provider_token` — PHASE 1 (`None`/`token`) replays the opened secret, PHASE 2 (`github-app`/`gitlab-oauth`) mints a fresh short-lived token via an injected `token_exchange_http` seam; both `/sync` + `/docs-pr` use it. **Phase-2 route test** (over both stores): register a `github-app` repo + sealed credential JSON → `/sync` opens it, signs a real App JWT, the fake exchange returns the installation token, and the **MINTED** token (not the credential) reaches the cloner. ruff+mypy clean. **Dogfood RE-HEALED:** `server` doc (the new private route helpers + `create_app` param) → 0 drift. **PHASE 2 COMPLETE** — the hot token is never stored. | **`from_credential` on the transports proved UNNECESSARY**: a minted token is just a string, so the route mints then reuses `from_repo(remote_url, token)` + the clone seam VERBATIM — only the credential SOURCE changes (a mint step). RS256 needs an RSA key, so `github_app_jwt` narrows `load_pem_private_key(...)` to `RSAPrivateKey` (satisfies mypy's key-union AND is a correct K8 runtime check). The JWT `iat`/`exp` need epoch ints, derived deterministically from the injected ISO clock (`_iso_to_epoch`) so tests stay K10. `provider_kind=None` ⇒ PHASE-1 replay, so every pre-GIT-05 repo is byte-identical. | 
| GIT-04 | server wiring: remote `POST /sync` (clone-on-demand) + `POST /docs-pr` — PHASE 1 of EPIC GIT (close-out) | DONE (focused-green; full gate CI) | **31 tests**: `tests/integration/test_server_gitsync.py` (14 × InMemoryStore AND SqlStore) — remote `/sync` clones a NO-`local_path` repo from a REAL `file://` origin (no network) → surfaces docs+coverage; SSRF allowlist (non-allowlisted https host / non-https scheme → 400, env self-hosted host passes); sealed-credential seal-at-register → open-at-sync → PLAINTEXT flows to the cloner; missing-KEK → 500 (register AND sync); `/docs-pr` clones→heals→opens via an injected transport factory, `dry_run` never calls it, a clean repo opens nothing, no-provider → 400, the DEFAULT (real `from_repo`) factory path via `dry_run`; the 401/403 auth matrix on both new writes. PLUS `tests/system/test_demo_gitsync_e2e.py` (3) — **the REAL `demo/` tree** git-init'd as a `file://` origin: clone-on-demand `/sync` surfaces core-api/getting-started/io-api + coverage; **add a file upstream → re-sync SEES it** (new undocumented file in the refreshed coverage snapshot); upstream drift → `/docs-pr` opens a PR. ruff+mypy clean; existing `test_server_sync`/`test_server` green (the seal block only fires when a `provider_secret` is present → pre-GIT-04 registers unaffected). **Dogfood RE-HEALED:** `server` doc (the new routes/helpers) → 0 drift. | **`create_app` gained two injectable seams** (`cloner`, `pr_transport_factory`, both default-real) so the routes test fully offline (K4): `/sync` uses a REAL `file://` clone (no transport needed); `/docs-pr` can't build a transport from a `file://` URL (`_parse_remote` rejects empty host), so the factory is injected. **`file://` is allowed by the SSRF check** (local, no network egress) — that's what lets the real-git e2e run offline; https is allowlisted to github.com/gitlab.com + `$CDMON_ALLOWED_GIT_HOSTS`. **Sealing is at the route**: register seals `provider_secret` → `set_provider_secret`; sync/docs-pr open it → pass plaintext to clone/transport; a missing/bad KEK is a 500 (server misconfig), never a silent downgrade. ENV-only knobs (no `config/cdmon/server.yaml` edit). **PHASE 1 COMPLETE** — the server can sync + open docs-PRs against a repo it does NOT hold, with a per-repo sealed credential. | 
| GIT-03 | `GitHubTransport` + `from_repo` on both PR transports — PHASE 1 of EPIC GIT | DONE (focused-green; full gate CI) | **31 tests** `tests/unit/test_pr.py` (+12): `GitHubTransport.submit` runs the ATOMIC 6-call git-data flow via ONE injected `_GitHubHttp` leaf — `GET ref/heads/{target}` → `GET commits/{base}` → `POST git/trees` (every healed file inline, `base_tree`) → `POST git/commits` (`parents=[base]`) → `POST git/refs` (`refs/heads/{source}`) → `POST pulls` — asserted method/url/body sequence; `from_env` (loud on missing repo/token); `from_repo` parses github.com→`api.github.com` and GHE host→`/api/v3`, loud on a non-`owner/repo` URL; `GitLabTransport.from_repo` uses the full URL-encoded project path + self-hosted `/api/v4`; explicit-`api_url` override; lazy default-leaf build (stubbed urlopen, no network); `open_docs_pr` drives GitHub end-to-end. **`pr.py` 100%** (142 stmts, 32 branches, 0 partial); ruff+mypy clean. **Dogfood RE-HEALED:** `pr-loop` doc (HASH+REGION) → `monitor --apply` → 0 drift. | GitHub's trees API accepts INLINE `content`, so a single `POST git/trees` creates the blobs — no separate blob step (the atomic 6-call flow, no local checkout). Shared `pr._parse_remote(url)` is the ONE place an adopter `remote_url` is validated (loud `TransportError` → the SSRF/host-allowlist hook GIT-04 leans on). `from_repo(remote_url, token)` is the SERVER's constructor (the central server opens the sealed token + builds the transport), parallel to `from_env` (the CI/client path). GitHub PR labels deferred (the issues API adds them post-create; `open_docs_pr` default labels are `()`). |
| GIT-02 | provider identity fields + sealed-secret Store seam + Alembic 0005 — PHASE 1 of EPIC GIT | DONE (focused-green; full gate CI) | **Store-parity** `tests/integration/test_provider_secret.py` (7 tests × InMemoryStore AND SqlStore): `provider`/`remote_url` round-trip on the identity, sealed `provider_secret` round-trips as opaque bytes, `None` when unset/unknown, rotates in place, set-on-unknown-repo is a no-op (parity), RegisteredRepo projection has no `provider_secret`. `tests/integration/test_db.py` +2: **Alembic 0005 up/down** (provider_secret column appears at head, dropped on downgrade to 0004) + **plaintext NEVER in the stored payload JSON** (sanitize `exclude={"auth_token","provider_secret"}`). New `set_provider_secret`/`repo_provider_secret` lines covered on both stores; ruff+mypy clean; regression suites (test_sinks/test_records/test_server) unaffected by the additive fields. **Dogfood RE-HEALED:** only the `server` doc drifted (HASH+REGION) from the two new Store METHODS → `monitor --apply` healed → 0 drift. | **Pydantic field-additions are hash-INVISIBLE** (a class field is body, not the `class X(Base)` signature line), so `RepoIdentity.provider`/`remote_url` + `RegistrationPayload.provider_secret` drifted NOTHING — only the new Store *methods* did. So **`provider`/`remote_url` ride in the existing `payload` JSON column → NO migration**; the ONLY schema change is the binary `provider_secret` column (Alembic 0005, mirrors 0002 token_hash). **Sealing stays at the route** (the crypto-allowed `[server]` layer): the store persists OPAQUE sealed bytes via a SEPARATE method and never imports `cryptography` — keeping `store.py` pure pydantic/stdlib and isolating the reversible secret from the one-way `token_hash`. InMemoryStore guards `set` on `_repos` to match SqlStore's update-existing-row semantics (parity). |
| GIT-01 | `secrets.py` AES-256-GCM seal/open + `SecretError` — PHASE 1 of EPIC GIT | DONE (focused-green; full gate CI) | **13 tests** (`tests/unit/test_secrets.py`): seal→open round-trip (incl. unicode/empty), random-nonce distinct-ciphertext, tampered/​wrong-key/​too-short → `SecretError`, missing/empty/non-base64/wrong-length KEK → `SecretError`, and **the K0 boundary in a subprocess**: importing `configsync`+`gitfetch`+`monitor`+`pr` pulls NO `cryptography`, and `import custodex.secrets` is lazy (no crypto until seal/open). **`secrets.py` 100%** (41 stmts, 6 branches); ruff fmt+check + mypy clean. `cryptography>=42` added to `[server]`+`[dev]` (installed 48.0.1). **Dogfood RE-HEALED:** `SecretError` on tracked `errors.py` drifted `docs/api/foundation.md` (HASH+REGION) → `cdx monitor --apply --config config/cdmon` healed → `drift.detect` confirms 0 drift. | **The conscious invariant fork:** E-06 stores the bearer token as a one-way sha256 hash; a git credential must be REPLAYED so it is AES-256-GCM SEALED (reversible) instead — documented as unavoidable for any replayed credential (PHASE 2 minted tokens recover most of it). `cryptography` is lazy-imported INSIDE `seal`/`open_secret` and the engine core never imports `secrets.py`, so a core-only install stays K0-clean (subprocess-proven). KEK = single base64 `$CDMON_SECRET_KEY` (no KMS; rotation = manual re-encrypt). `secret_box_from_env(mapping)` takes an injected mapping so tests never touch `os.environ` (K10). |
| GIT-00 | clone-on-demand (`gitfetch.py`) — STEP 0 of EPIC GIT | DONE (focused-green; full gate CI) | **14 tests** (`tests/unit/test_gitfetch.py` 11 + `tests/integration/test_gitfetch.py` 3): `_build_clone_argv`/`_clone_url` token-never-in-argv (github→`x-access-token`, gitlab→`oauth2`, file://+secret stays plain), `_scrub` both branches, `cloned_repo` teardown-on-success AND on cloner-error via an injected fake `_Cloner` (K1/K4), and a REAL `file://` clone (no network) → `run_sync(clone, mode="local")` surfaces the doc/code_ref counts + coverage-on-sync. **`gitfetch.py` 100%** (59 stmts, 10 branches, 0 missed); ruff fmt+check clean; `mypy custodex/gitfetch.py` clean. `configsync.py` UNTOUCHED (K9). | **Deliberate divergence from the grounding-workflow's `clone_runner`-as-`run_git=`-seam proposal:** `_open_repo` checks `local_path.is_dir()` + runs `rev-parse HEAD` BEFORE any injected runner, so a lazy-clone runner needs a stateful path-rewriting forgery. A **`cloned_repo` context manager** that clones FULLY up front then runs `run_sync(clone, mode="local")` with the DEFAULT runner needs ZERO engine change — chosen. Token reaches git ONLY via an ephemeral `GIT_ASKPASS` env helper (never argv/URL). The `file://` clone exercises the real `_GitCloner` subprocess + (with a dummy secret) the askpass branch with NO network, so there is NO `# pragma: no cover` in `gitfetch.py`. Dogfood reheal = no-op (new module, no `docs/api/*` references it yet); its public-symbol documentation + catalog/wiki entries land in the GIT consolidation (heavy `cdx coverage`/`trace`/`wiki` gates starve on this host → CI-verified). |
| ASTRO-04 | retire `dashboard/`; single-origin static is `frontend/dist` only (EPIC ASTRO close-out) | DONE | `dashboard/` deleted (53 files); `_default_static_dir()` → `frontend/dist` only, test renamed `test_default_static_dir_resolves_frontend_dist`; `.gitignore` dashboard lines removed; `standalone.py` + `cdx serve` serve `frontend/dist` via the SAME `_default_static_dir` locator. **GATE:** ruff fmt+check clean (152 files); mypy clean (50 files); `cdx check`/`lint`/`coverage --fail-under 95`/`wiki --check`/`trace --fail-on-gap` all exit 0 (**192/192** have a test AND a demo). No Python test referenced the dir (serving tests use tmp dirs). | The React+Vite SPA is fully superseded by the Astro app under `frontend/`. Historical `.project/slices/F-*.md` (the dashboard era) left intact. Server `GET /wiki` JSON kept (now unused) — retiring it is the ASTRO-02 follow-up (cascades into the golden-catalog dogfood). |
| ASTRO-03 | the console as React islands under `frontend/` | DONE | `dashboard/src/{api,components,hooks,pages,test,types.ts,App.tsx,index.css,routing.ts,schema.review.json}` → `frontend/src/console/`; `<App/>` mounted as a `client:only` HashRouter island at `/` (`ConsoleApp.tsx`) inside `Layout.astro`. `PUBLIC_API_BASE` (default same-origin "") replaces `VITE_API_BASE` in client/AppShell/ConnectionStatus. `astro check` **0 errors** (type-checks all 14 `*.test.tsx` + components + wiki pages); `astro build` bundles the island chunks (`_astro/ConsoleApp.*`); served in-process (`GET /` = console; `/health`,`/repos`,`/openapi.json` still win). **Vitest: 34 passed / 0 failures across the runnable subset; the full 14-file suite could NOT complete on THIS host — load ~38 starves vitest worker startup (forks AND threads both RPC-timeout at 60s) → the suite runs on a CI runner.** Config reverted to the CI default. | Faithful port (no component logic rewritten). 1 hint = React-19 `FormEvent` deprecation (non-blocking). |
| ASTRO-02 | native Astro docs/wiki at `/wiki/*` | DONE | `src/content.config.ts` (glob loader over `feature-doc/*.md`, no copy) + `src/pages/wiki/[...slug].astro` → static `/wiki/{features,traceability,tests,source}` rendered by Astro's markdown pipeline (`astro build` emits all four, incl. the **1.4 MB** Test Wiki page). React `Wiki.tsx`/`Wiki.test.tsx`, `api.wiki()`, `WikiSection`/`WikiPayload`, the `wikiPayload` fixture + the `/wiki` client route + the `App.routing` lazy-wiki test all removed/repointed; AppShell **Wiki** nav → `<a href="/wiki/features">`. Served in-process: `/wiki/features`→"Feature Reference", `/wiki/traceability`→"Traceability". | Retires `render_markdown` + `dangerouslySetInnerHTML` on the **frontend**. Native pages avoid the exact `/wiki` (the server's JSON route), so the canonical entry is `/wiki/features`. **Lesson:** `getStaticPaths`'s helper (`metaFor`) MUST live INSIDE it — Astro hoists `getStaticPaths` into its own scope (else "not defined" at build). |
| ASTRO-01 | Astro foundation + single-origin serving | DONE | `frontend/` Astro app (Astro 6 + `@astrojs/react` + `@astrojs/mdx`, `output:static`); `Layout.astro` design system + a `StatusPill` island (own `_astro/StatusPill.*.js` chunk → hydration proven). `server/app.py`: ONE `StaticFiles(html=True)` mount at `/` placed AFTER every API route (API wins; `/`, `/wiki/*`, `/_astro/*` fall through); `_default_static_dir` → `frontend/dist`. 2 new server tests; the 3 existing serving tests stay green (backward-compatible). `astro check` clean; built + served in-process. | "EPIC F" = the existing dashboard; this re-platform is **EPIC ASTRO**. Mount LAST so it can never shadow a route. Astro's asset dir is `_astro/` (not `assets/`); `spa_index.parent` keeps it type-safe. |
| H-03 | regression corpus from lessons & known limitations — **FINAL EPIC-H slice; EPIC H + the whole EPIC-2 program COMPLETE** | DONE | **GATE:** `.venv/bin/ruff format --check .` → 86 files already formatted; `.venv/bin/ruff check .` → All checks passed; `.venv/bin/mypy custodex` → clean (36 source files); **`.venv/bin/pytest -q --cov=custodex --cov-branch` → 744 passed, 2 deselected (live_llm + `pg`)** (was 722; +22 corpus cases), whole-pkg **98.20%** (≥90 gate met, unchanged — corpus is tests-only). **MARKER:** registered `regression` in `pyproject.toml`; the corpus is auto-tagged via `tests/regression/conftest.py` so it is INCLUDED in the default suite AND `pytest -m regression` selects EXACTLY the 22 corpus cases (verified: only the three `tests/regression/test_corpus_*.py` files collected). **DOGFOOD (tests-only, no tracked-module change):** `cdx check --config cdmon.yaml` → clean exit 0; `cdx coverage --config cdmon.yaml --fail-under 95` → exit 0; `cdx lint --config cdmon.yaml` → exit 0. **BREAK-IT (confirmed bite, reverted, NOT committed):** (a) `heal.locked_region_ids → frozenset()` reds BOTH `test_human_region_never_auto_edited` [B-02] and `test_llm_seeded_fill_then_lock_three_phase` [B-03]; documented in-docstring. | **`tests/regression/` — a curated, executable INDEX of the program's durable invariants** (one guard per learned failure mode from LESSON_LEARNT.md + problems/*). 22 cases across 3 files + a `_fixtures.py` (mirrors the system-test repo) + `conftest.py` (auto-marks `regression`) + `README.md` (case→lesson map). **`test_corpus_pipeline.py` (9):** [CDM-07] audience invalidation (docstring-only drifts eng not user) + the both-audience foil; [CDM-03] heal idempotence/no-trailing-newline; [B-02] human region never auto-edited (fingerprint still refreshes) + advisory persistence; [B-03] llm-seeded fill→lock→report three-phase + the unlocked-regenerates foil; [CDM-07] both-shapes-close-in-one-apply; [C-04] should_sync loop-safety truth table. **`test_corpus_contracts.py` (7):** [C-05] pre-`source_sha` ReviewRecord back-compat + set-field round-trip + versioned-superset schema; [D-01] pre-`note` ResolutionRecord back-compat; [E-01] reporting-never-raises (down transport queues to outbox); [D-06] matched-rule zero-backend-calls (spy) + default-rules-additive. **`test_corpus_selfcoverage.py` (6):** [H-02] self-coverage ≥95 floor + every-waiver-has-a-reason; [H-01/H-04] **NEW GUARD — an UNLISTED `custodex/**` module's public symbol is detected as a gap AND drops self-coverage** (the most likely future self-gate regression, per the H-01/H-04 lesson); [CDM-07] dogfood-in-sync + self-heal-on-a-copy; [CDM-08] dogfood conforms to Layout Standard. **NEWLY-GUARDED vs REFERENCED:** the unlisted-engine-module gap is a genuinely NEW guard (no prior test pinned it); the rest are thin re-assertions/parametrizations against the SAME engine seams the system/heal/drift/schema/sinks/monitor tests use (the corpus is an INDEX, not a copy). **STALE LESSONS:** none found. One refinement recorded: the B-02 human guarantee is enforced by TWO layers — a redundant `preserve` set in `monitor.run` AND the load-bearing modes-derived lock in `heal.locked_region_ids`; clearing only `monitor`'s `preserve` does NOT red the guard, so the break-it notes target the heal layer. No engine code changed (ADDITIVE; K9/K10). |
| E-02 | repo registration client (`cdx register`) — client-side only | DONE | **597 tests pass** (was 583; +14: 11 new `test_registry` — payload-shape via fake transport, optional display fields, dry-run-no-call, missing-url loud, lazy-build POSTs to `/repos` + bearer-from-env, no-token-when-env-unset, `register_repo` default-transport lazy build, `repo_identity_from_config` identity/CI-env-fallback/missing-repo_id-loud, payload forbids unknown keys; +3 `test_cli` — `register --dry-run` prints payload-no-network, `register` submits via stubbed leaf, missing-`repo_id` loud), 1 live deselected; **ruff fmt+check clean; mypy clean (30 files** — new `registry.py`); **changed-module coverage `registry.py`=98%** (0 stmts missed; the real `_UrllibRegisterHttp.request` urlopen is the ONLY `# pragma: no cover` leaf, K4; the one partial branch `118->120` is the inject-the-leaf `if http is None` short-circuit, same pattern as `sinks.py`), `cli.py` `register` paths covered, whole-pkg **98.04%** (≥90 gate met); **NO tracked module touched** (`registry.py`/`cli.py` not tracked; `config.py`/`sinks.py` UNCHANGED) so NO dogfood drift — `cdx check`/`cdx lint` both exit 0; **full suite GREEN (597 passed, 1 deselected)** | **New `registry.py` (client-side, offline default — K0/K4/K6/K8).** `RegistrationPayload(BaseModel, frozen, extra=forbid)`={`schema_version="1.0.0"`,`repo:RepoIdentity`,`default_branch:str\|None=None`,`description:str\|None=None`} — **reuses `RepoIdentity` from `sinks.py`** (NOT a new identity model; same wire identity as `IngestEnvelope`). `RegisterTransport(Protocol)`={`register(payload)->dict`}. `HttpRegisterTransport(url, auth_env=None, *, http:_RegisterHttp\|None=None)`: POSTs to `f"{url.rstrip('/')}/repos"`; bearer read from `auth_env` at register time (same env seam as `HttpSink`); INJECTED `_RegisterHttp` leaf (`request(method,url,*,body,token)->dict`), lazily building a stdlib `_UrllibRegisterHttp` (the only real urlopen, `# pragma: no cover`, K0). `register_repo(identity, *, url, auth_env=None, transport=None, dry_run=False, default_branch=None, description=None) -> dict\|None`: builds the payload; **`dry_run` returns `payload.model_dump(mode="json")` WITHOUT calling the transport** (no url/env needed to inspect); else submits via injected transport, or a default `HttpRegisterTransport(url, auth_env)` when `None` — loud `SchemaError` (K8) on missing/empty `url`. **Shared helper `repo_identity_from_config(cfg:CentralConfig)->RepoIdentity`** (de-dups `make_sink`'s identity build: `repo_id`/`name`/`url` + commit precedence `cfg.repo_commit` else `$CI_COMMIT_SHA`; loud `SchemaError` if `repo_id` missing). NOTE: kept in `registry.py` (which imports `sinks`); `make_sink` was NOT refactored to call it because `sinks.py` importing `registry` would CYCLE (`registry`→`sinks`) — the small dup in `make_sink` is the deliberate trade (the helper exists for `registry`+E-03/future callers). **cli.py:** `cdx register [--config] [--dry-run]` — builds identity from `cfg.central` (loud K8 on missing `repo_id`/`url`), POSTs to `<central url>/repos`; `--dry-run` prints the payload JSON (no network). Pinned `registry.py` in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table + README. K0/K4/K6/K8/K9/K10 upheld. **For E-03 (server `/repos` + `/ingest`):** import `RegistrationPayload` from `custodex.registry` AND `IngestEnvelope` from `custodex.sinks` and validate POST bodies against them DIRECTLY — ONE shared schema each, NO DTOs (K6); `schema_version` ("1.0.0" on both) is the additive-compat hinge. `/repos` consumes `RegistrationPayload` (repo identity + optional `default_branch`/`description`); `/ingest` consumes `IngestEnvelope` (repo identity + `ReviewRecord`). Both carry the SAME `RepoIdentity` (`repo_id` is the server's repo key). Bearer auth: client sends `Authorization: Bearer <token>` (token from the client's `auth_env`); E-05/06 per-repo auth validates it server-side. |
| E-01 | reporting client: repo identity + offline queue + retry (first EPIC-E slice; client-side only) | DONE | **583 tests pass** (was 575; +8 net in `test_sinks` E-01: repo-identified-envelope, down→queue+never-raise, recovery→flush-oldest-first→new, partial-flush re-queue-in-order, retry-within-max, no-outbox-best-effort, missing-`repo_id` loud, commit-from-config + CI-env fallback; existing http-sink tests upgraded to the envelope shape), 1 live deselected; ruff fmt+check clean; mypy clean (**29 files**); **changed-module coverage `sinks.py`=100% `config.py`=100% `schema.py`=100%** — the real `_UrllibClient.post` urlopen stays the ONLY `# pragma: no cover` leaf (K4), whole-pkg 98.02% (≥90 gate met); **dogfood RE-HEALED** (editing tracked `config.py`/`schema.py`/`sinks.py` drifted `docs/api/foundation.md`+`docs/api/remediation.md` → `cdx monitor --apply --config cdmon.yaml` healed HASH+REGION → `cdx check`/`cdx lint` both exit 0); **full suite GREEN POST-reheal (583 passed, 1 deselected)** | **The SHARED versioned wire format (K6) lives in `sinks.py`** (not `schema.py` — keeps `schema.py` the pure review-record source; importing it into `schema.py` would cycle): `RepoIdentity(BaseModel, frozen, extra=forbid)`={`repo_id:str`,`repo_name`/`repo_url`/`commit`:`str|None=None`} and `IngestEnvelope(frozen, extra=forbid)`={`schema_version="1.0.0"`,`repo:RepoIdentity`,`record:ReviewRecord`} — E-03's `/ingest` consumes THIS model directly, NO DTOs. **`HttpSink` upgrade (NEVER raises — K4):** ctor gains `*, repo:RepoIdentity, outbox:Path|None=None, max_retries:int=2`; `emit` (1) wraps record in an envelope, (2) `_drain`s the outbox oldest-first posting one attempt per queued envelope, STOPPING on the first failure and rewriting failed+remainder back (order preserved), (3) only if the backlog drained cleanly `_try_send`s the new envelope up to `max_retries` attempts, (4) on ANY final failure (backlog blocked OR new send exhausted) `_enqueue`s the new envelope so it queues BEHIND the backlog (oldest-first invariant) — every transport exception is swallowed (K4: reporting must not break a heal run). **Outbox = JSONL of `IngestEnvelope` lines**, drained by read-all-then-rewrite-the-undrained-tail (simple, deterministic, single-process sink; `_write_outbox([])` truncates to empty). `outbox=None` ⇒ best-effort (record dropped on failure, still no raise). **`CentralConfig` additive fields** (all default `None`/`2`, old configs load unchanged): `repo_id`/`repo_name`/`repo_url`/`repo_commit`/`outbox`/`max_retries`. **`make_sink` http path:** loud `SchemaError` (K8) if `repo_id` missing; commit precedence = `cfg.repo_commit` **else** `$CI_COMMIT_SHA`; outbox defaults to `.cdmon/outbox.jsonl`. **NullSink/FileSink untouched.** Pinned envelope + `HttpSink` signature in ARCHITECTURE.md BEFORE impl. **For E-02 (registration):** reuse the SAME injected-`_PostClient` transport + never-raise discipline; a `register` payload (likely `RepoIdentity` + repo metadata) posts over the same client shape — `make_sink`'s `RepoIdentity` build (commit precedence) is the model to mirror. **For E-03 (server `/ingest`):** import `IngestEnvelope` from `custodex.sinks` and validate the POST body against it directly — ONE schema, no DTOs (K6); `schema_version` is the additive-compat hinge. |
| D-05/06 | promotion detector + deterministic rule application — **EPIC D COMPLETE** | DONE | 575 tests pass (was 551; +24: 17 `test_promotion` D-05/06 detector+mapping, +3 `test_monitor` D-06 spy-backend, +4 `test_cli` `promotions`), 1 live deselected; ruff fmt+check clean; mypy clean (**29 files** — new `promotion.py`); changed-module coverage **promotion.py=100% monitor.py=100%**, cli.py covers the new `promotions` paths (pre-existing build/open-docs-pr misses unchanged), whole-pkg 97.87% (≥90 gate met); **dogfood RE-HEALED** (editing tracked `monitor.py` drifted `docs/api/remediation.md` → `cdx monitor --apply --config cdmon.yaml` healed HASH+REGION → `cdx check`/`cdx lint` both exit 0); **full suite GREEN POST-reheal (575 passed, 1 deselected)** | **D-05 `promotion.py` (pure, deterministic, NO new dep — K0/K10).** `PromotionCandidate(BaseModel, frozen, extra=forbid)`={`doc_id`,`drift_kind`,`audience:Audience`,`resolution:Resolution`,`count:int`}; `detect_promotions(records, resolutions, *, min_count=3) -> list[PromotionCandidate]`. **Shape key = GENERALIZABLE `(doc_id,drift_kind,audience)` — NOT `surface_hash`** (the exact code state, similar.py's dominant feature, never recurs across edits so it can't ground a recurring rule; documented). Population = RESOLVED only (`reviewlog.resolved_index`, last-write-wins). A shape QUALIFIES iff ≥`min_count` of its resolved records share ONE resolution (unanimous — a single dissent disqualifies). **Promotable = DECISION-shaped only: `invalidated`/`rejected`** (`PROMOTABLE_RESOLUTIONS`); `overridden` (human prose `resolved_text`, doesn't generalize) + `accepted` (already LLM-free) EXCLUDED (documented; future content-rule slice could mine `overridden`). Orphan resolutions ignored; output sorted `(doc_id,drift_kind,audience,resolution)` (K10). **D-06 rule application (opt-in, additive, K4).** `PromotionRule(frozen, extra=forbid)`={`doc_id`,`drift_kind`,`audience`,`verdict:Verdict`}; `rule_from_candidate(c)->PromotionRule` (both decisions → `INVALIDATE`); `rule_for(drift, rules)->PromotionRule|None` (FIRST match on the shape tuple, `drift.kind.value` vs rule string). `Monitor(..., rules: tuple[PromotionRule,...]=())` — **DEFAULT () ⇒ byte-identical to today** (every drift → backend, asserted by spy). On a match, `run` SYNTHESIZES a `BackendResult(rule.verdict, fix=None, cause="promoted rule: …")` **WITHOUT calling `backend.propose`** (the validable goal — spy backend `calls==0`), records it like any other (K5 — still audited), marks it rule-sourced (`cause` `RULE_CAUSE_PREFIX` + `config_snapshot["resolved_by"]="rule"`), `continue`s. **cli.py:** `cdx promotions [--config] [--min-count N] [--json]` read-only view of candidates (pure, K1; "no promotable shapes" when empty). Pinned `promotion.py` + Monitor `rules` in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table + README. K0/K1/K4/K5/K6/K9/K10 upheld. **For EPIC E (central server + DB):** records/resolutions/promotions all flow through the SAME append-only JSONL substrate the `HttpSink` already emits — the server ingests `ReviewRecord`s (now incl. `resolved_by=rule` rule-sourced ones), `ResolutionRecord`s, and can run `detect_promotions` server-side to aggregate promotions across repos; persisting/loading `rules` from a file/server is the documented D-06 follow-up (the in-memory `rules=` path proves the mechanism). |
| D-03/04 | similarity retrieval + agent few-shot exemplars | DONE | 551 tests pass (was 526; +25: 13 `test_similar`, +4 `test_backends` D-04, +5 `test_agent` D-04, +3 `test_monitor` D-04), 1 live deselected; ruff fmt+check clean; mypy clean (**28 files** — new `similar.py`); changed-module coverage **similar.py=100% monitor.py=100%**, backends.py + agent/graph.py covered (whole-pkg 97.88%, ≥90 gate met); **agent tests RAN** (`.venv` has `[agent]` extra — `langgraph 1.2.4` installed, none skipped); **dogfood RE-HEALED** (editing tracked `backends.py`/`monitor.py` drifted `docs/api/remediation.md` → `cdx monitor --apply --config cdmon.yaml` healed HASH+REGION → `cdx check`/`cdx lint` both exit 0); **full suite GREEN POST-reheal** | **D-03 `similar.py` (pure, deterministic, NO new dep — K0/K10).** `Exemplar(BaseModel, frozen, extra=forbid)`={`record:ReviewRecord`,`resolution:ResolutionRecord`,`score:float`}; `rank_similar(target:ReviewRecord, records, resolutions, *, top_n=3) -> list[Exemplar]`. Population = RESOLVED only (`reviewlog.resolved_index`, last-write-wins); target ALWAYS excluded by `record_id`. **Feature-match score** (`FEATURE_WEIGHTS`, descending-distinct): `surface_hash`=5.0, `doc_id`=3.0, `drift_kind`=2.0, `audience`=1.0 (max 11.0). **ReviewRecord has NO `region_id`**, so the spec's region feature is dropped (documented) — the four fields above are the drift's shape. **Total order (K10):** score DESC, then `resolved_at` recency DESC (ISO sorts lexicographically=chronologically; inverted per-codepoint via `_neg_iso` so one ascending sort gives the full order), then `record_id` ASC. `top_n<=0`/empty pop → `[]`; zero-score candidates STILL eligible (rank last). **D-04 additive few-shot (DEFAULT-OFF/DEFAULT-EMPTY).** `FixRequest.exemplars: tuple[Exemplar,...]=()` (default empty ⇒ every backend test unchanged; `build_prompt`+MockBackend IGNORE it — asserted byte-identical/deterministic). `Monitor(use_exemplars=False, resolutions_path=None, exemplar_top_n=3)` — OFF reads NOTHING; ON reads review-log+resolutions ONCE up front (before appends, so a drift is never ranked vs same-run records), per drift builds a feature-only `__target__` ReviewRecord and `rank_similar`s onto the FixRequest. New artifact `agent/prompts/EXEMPLARS.md` (ships with `[agent]` extra); `Artifact.EXEMPLARS`; `select_artifacts` appends it ONLY when `req.exemplars` non-empty; `render_context` appends `_render_exemplars` (empty string when none ⇒ **byte-identical to pre-D-04**, asserted) rendering each exemplar's drift shape + outcome + `resolved_text` (OVERRIDDEN gold body). Pinned all contracts in ARCHITECTURE.md BEFORE impl. K0/K4/K6/K9/K10 upheld. **For D-05/06:** the same substrate (`read_all`+`resolved_index`) + a recurring-shape grouping (group resolved records by the SAME feature tuple `(doc_id,drift_kind,audience,surface_hash)` or a looser shape key; when ≥K share an IDENTICAL resolution, promote → resolve that shape with NO backend call). `rank_similar`'s `_score`/`FEATURE_WEIGHTS` give the shape key; reuse `Monitor`'s default-OFF pattern for the opt-in. |
| D-01/02 | resolution outcome edge + `cdx resolve` (**EPIC-D begins**) | DONE | 526 tests pass (was 502; +24: 7 test_schema ResolutionRecord/enum/schema/back-compat, 10 test_reviewlog resolutions log + join/summary, 7 test_cli `resolve`/`report` resolved-counts), 1 live deselected; ruff fmt+check clean; mypy clean (27 files); changed-module coverage **schema.py=100% reviewlog.py=100%**, cli.py covered for the new `resolve` paths (pre-existing build/open-docs-pr misses unchanged), whole-pkg 97.86% (≥90 gate met); **dogfood RE-HEALED** (editing tracked `schema.py` drifted `docs/api/{foundation,remediation}.md` → `cdx monitor --apply --config cdmon.yaml` healed remediation HASH+REGION → `cdx check`/`cdx lint` both exit 0); **full suite GREEN POST-reheal** | **The outcome edge: a SEPARATE append-only event, not a record mutation (K5).** `schema.py`: `Resolution(str,Enum)` (`accepted\|overridden\|rejected\|invalidated`) + `ResolutionRecord` (frozen, extra=forbid, versioned; `record_id` FK→ReviewRecord, `resolution`, `resolved_text`/`resolved_by`/`resolved_at`(injected, K10)/`note`; `note` appended LAST so a line without it still parses — K6 back-compat asserted by `test_resolution_line_without_future_field_still_parses`) + `resolution_record_schema()`. **Chose to EXTEND `reviewlog.py`** (not a new module): the join needs both logs and they share the JSONL/`SchemaError` machinery — keeps it cohesive. `append_resolution`/`read_resolutions` MIRROR `append`/`read_all` EXACTLY (append mode, parent dirs, blank-line skip, corrupt line→`SchemaError` w/ line no., K8; `DEFAULT_RESOLUTIONS_PATH=.cdmon/resolutions.jsonl`). Join: `resolved_index(resolutions)->dict[record_id,ResolutionRecord]` **LAST-WRITE-WINS** (a record resolved twice → the LAST appended event wins; the log stays append-only so a correction is a new line, never a mutation — `test_resolved_index_last_write_wins`). `summarize_with_resolutions(records,resolutions)->{total,resolved,unresolved,by_resolution(sorted,K10)}` ignores orphan resolutions (a `record_id` not in the review log can't inflate counts). **cli.py:** module-level `_now()` seam (mirrors `monitor._default_now`; monkeypatched in tests for deterministic `resolved_at`); `cdx resolve RECORD_ID --resolution R [--by][--text][--note][--config][--log]` validates the id EXISTS in the review log (loud `SchemaError`→`error:`+Exit(1), no traceback, K8), appends a `ResolutionRecord` (timestamp via `_now`), prints `resolved <id> as <R>`; `report` aggregate view additively gains `resolved`/`unresolved`/`by_resolution` (joins resolutions). Pinned schema/log/CLI signatures in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table (`resolve`, `report` counts) + README. K5/K6/K8/K9/K10 upheld. **For D-03 (`similar.py`):** the substrate is `read_all(review-log) + read_resolutions(resolutions) → resolved_index` (last-write-wins) — a RESOLVED record is one whose `record_id` is in that index; rank/score by joining `ReviewRecord.drift_kind`/`doc_id`/surface against past resolved ones; `ResolutionRecord.resolved_text` (OVERRIDDEN) is the human exemplar body D-04 feeds the agent. |
| CDM-00 | bootstrap | DONE | venv+install OK; ruff fmt+check clean; mypy clean; 1 smoke test passes | pyproject (pydantic/typer/pyyaml + dev), package custodex, cli entry `cdx`, .project specs K0–K10 + ARCHITECTURE contracts |
| CDM-01 | config + init | DONE | 24 tests pass; ruff fmt+check clean; mypy clean; coverage of errors.py/config.py/cli.py = 100% (whole-pkg 100% as only CDM-01 modules exist so far); K0/K8/K10 upheld | created `custodex/errors.py`, `custodex/config.py`, `custodex/cli.py`, `tests/test_config.py`, `tests/test_cli_init.py`; frozen+extra=forbid pydantic v2 models, YAML/JSON loader wrapping all parse/validation errors in ConfigError, documented `CONFIG_TEMPLATE` (2 docs/both audiences/all selectors) round-trips, `cdx init [--path] [--force]` |
| CDM-02 | extract | DONE | 53 tests pass; ruff fmt+check clean; mypy clean; coverage extract.py = 99% (whole-pkg 99.72%, ≥90 gate met); K0/K3/K10 upheld | created `custodex/extract.py`, `tests/test_extract.py`; AST-only extraction (never imports target, K0) of functions/async/classes/methods(`Class.method`)/module vars with reconstructed signatures (posonly/`*args`/kw-only/`**kw`/annotations/returns, class bases+keywords), `is_public` (dunders like `__init__` treated public), docstrings via `ast.get_docstring`; `build_document_surface` does sub-file selection (symbols incl. class→methods / lines-overlap / names→variables / whole-file) deduped by (path,name,lineno) ordered by (name,lineno); audience filter (K3) drops private symbols for user-guide; deterministic `surface_hash` sha256[:16] over sorted clock-free payload (K10) — eng-guide includes docstrings, user-guide excludes them; `ExtractionError` on missing/unreadable/syntax-error (K8) |
| CDM-03 | drift + heal | DONE | 81 tests pass; ruff fmt+check clean; mypy clean; coverage blocks.py=100% drift.py=100% heal.py=93% manifest.py=98% (whole-pkg 98.82%, ≥90 gate met); K1/K2/K3/K7/K8/K10 upheld | created `custodex/manifest.py`, `custodex/blocks.py`, `custodex/drift.py`, `custodex/heal.py`, `tests/test_manifest.py`, `tests/test_drift.py`, `tests/test_heal.py`. manifest: `parse_doc` splits optional YAML front matter (loud `DriftError` on non-mapping/malformed YAML, K8), `regions`/`set_region` parse+edit `<!-- CDM:BEGIN id -->`/`<!-- CDM:END id -->` markers (DriftError on unterminated/duplicate/nested/mismatched/orphan-END, K8) preserving bytes outside markers (K7), `stored_fingerprint`/`set_fingerprint` on `meta.cdm.fingerprint`, `render_doc` re-renders FM+body. blocks: deterministic sorted `symbol_table` (escaped cells, K10), `expected_region` (`symbols`→table, unknown→None), `REGION_KEYS={"symbols"}`. drift: detect-only/pure (K1, asserted side-effect-free), docs graded against code surface (K2); MISSING_DOC/HASH/REGION (each healable) + UNHEALABLE (unknown region id, healable=False); audience set on every Drift; **K3 proven end-to-end** by two-doc test (same file, user+eng) where a docstring/private-only edit drifts eng-guide HASH but leaves user-guide clean (filter lives in extract; asserted via detect). heal: `regenerate_regions` rewrites known regions from surface + refreshes fingerprint, idempotent (2nd call False, detect() clean after, K7); `apply_fix` types the fix structurally via a `ProposedFixLike` Protocol (does NOT import schema.py, not yet built — CDM-04) handling region-body + whole-doc shapes, idempotent. |
| CDM-04 | schema + log + sinks | DONE | 117 tests pass; ruff fmt+check clean; mypy clean; coverage schema.py=100% reviewlog.py=100% sinks.py=94% (whole-pkg 98.54%, ≥90 gate met); K0/K4/K5/K6/K8/K10 upheld | created `custodex/schema.py`, `custodex/reviewlog.py`, `custodex/sinks.py`, `tests/test_schema.py`, `tests/test_reviewlog.py`, `tests/test_sinks.py`. schema: `Verdict` enum (FIX/INVALIDATE/ESCALATE); `ProposedFix` (frozen, extra=forbid; satisfies `heal.ProposedFixLike`); `ReviewRecord` (frozen, extra=forbid) with `schema_version="1.0.0"` (K6) and BOTH original drift + proposed fix (K5), timestamps injected ISO strings (K10); `review_record_schema()` = `model_json_schema()` (one source of truth, K6); `new_record_id` = sha256[:12] of doc_id\x00surface_hash\x00detected_at — deterministic + input-sensitive (K10). reviewlog: append-only JSONL (`append` opens in 'a', creates parent dirs, never rewrites lines, K5); `read_all` parses each line→`ReviewRecord` (missing file→[], blank lines skipped, corrupt line→loud `SchemaError` with line no., K8); `summarize` counts by verdict/audience/doc_id + total, sorted keys (deterministic, K10). sinks: `Sink` Protocol; `NullSink` (default, offline K4); `FileSink` appends JSONL (offline central stand-in, read-backable via reviewlog); `HttpSink(url, auth_env, *, client=None)` POSTs record JSON, bearer from `os.environ[auth_env]` read at emit time, HTTP client INJECTED (fake in tests, NO network, K4), lazily builds a stdlib-only `_UrllibClient` (K0, no `requests`) when client is None; `make_sink` resolves none→Null / file→FileSink (SchemaError if path missing) / http→HttpSink (SchemaError if url missing, K8). HttpSink tested only via injected fake asserting url+body+Authorization header; the only uncovered lines are `_UrllibClient.post`'s real `urlopen` call (K4 forbids exercising it). |
| CDM-05 | backends | DONE | 163 tests pass; ruff fmt+check clean; mypy clean; coverage backends.py=100% (whole-pkg 98.84%, ≥90 gate met); K0/K4/K8 upheld | created `custodex/backends.py`, `tests/test_backends.py`. Contract per ARCHITECTURE.md: `FixRequest`/`BackendResult` (frozen, extra=forbid), `Backend` Protocol, shared audience-aware `build_prompt` (states audience + user-guide→INVALIDATE / eng-guide→FIX decision rule, includes drift kind/detail/region/diff + current doc text + code surface via `blocks.symbol_table` + JSON-only contract instruction), robust `parse_backend_json` (balanced-brace scan tolerant of prose/```json fences + strings-with-braces + escaped quotes; loud `BackendError` on no-JSON/unbalanced/invalid-verdict/missing-key/malformed-fix, K8). `MockBackend` deterministic (K4): healable REGION drift w/ known renderer→FIX (body == `expected_region`), user-guide HASH drift whose detail mentions docstring/comment/private→INVALIDATE (K3), else→ESCALATE. `ClaudeCodeBackend(*, command, model, timeout_s, runner)`: builds argv (default `["claude","-p",<prompt>]` +`--model`; configured `command` template substitutes a `{prompt}` token in place else appends prompt), calls INJECTED `runner(argv, prompt, timeout_s)`→`parse_backend_json`, wraps any failure in `BackendError`; default runner = stdlib `subprocess.run` isolated in `_default_process_runner` (nonzero exit→BackendError). `ApiBackend(*, model, api_key_env, timeout_s, client)`: builds prompt, calls INJECTED `client(model=,prompt=,timeout=)`→`parse_backend_json`; default client lazily built from `_anthropic_messages_call` (stdlib `urllib`, NO `anthropic` pkg, K0), raises `BackendError` if `$ANTHROPIC_API_KEY` unset. `make_backend`: mock→MockBackend, claude-code→ClaudeCodeBackend(cfg.command/model/timeout_s), api→ApiBackend(cfg.model or default model, timeout_s). NO real subprocess / NO network in tests (K4) — runner/client always faked; the only `# pragma: no cover` lines are the real `subprocess.run` leaf and the real `urllib.urlopen` Anthropic call leaf (K4 forbids exercising them) + one defensive non-dict guard unreachable after the balanced-brace extractor. |
| CDM-06 | monitor + cli | DONE | 183 tests pass (26 new: 12 test_monitor + 14 test_cli); ruff fmt+check clean; mypy clean (14 files); coverage monitor.py=99% cli.py=100% (whole-pkg 98.92%, ≥90 gate met); K1/K4/K5/K7/K8/K10 upheld | created `custodex/monitor.py`, `tests/test_monitor.py`, `tests/test_cli.py`; extended `custodex/cli.py`. monitor: `HandledDrift`/`MonitorResult` (frozen, extra=forbid); `Monitor(config, config_dir, *, backend=None, sink=None, now=None, log_path=None)` — backend defaults to `make_backend(config.backend)` (mock, K4), sink to `make_sink(config.central)`, `now` to a `datetime.now(timezone.utc).isoformat()` callable INJECTED in tests with a fixed ISO string (K10), `root=config_dir/config.root`, default log `<config_dir>/.cdmon/review-log.jsonl` (overridable). `check()` = pure delegate to `drift.detect` (K1, asserted byte-unchanged). `run(*, apply=None)`: detect → per drift build `FixRequest` (surface via `build_document_surface`, doc_text read or "" for MISSING_DOC) → `backend.propose` → build `ReviewRecord` (deterministic `new_record_id(doc_id, surface_hash, now)`, detected/resolved from `now()`, `config_snapshot={"backend","root"}`) → `reviewlog.append` + `sink.emit` ALWAYS (K5: INVALIDATE/ESCALATE never dropped) → if `effective_apply` (=`apply_default` when None) and verdict==FIX and fix→`apply_fix` (opt-in, K5) → re-detect for `remaining`. Idempotent (K7): 2nd run on clean doc finds no drift, writes no record. cli: extended same `app` with `surface` (id/audience/symbol-count, `--json` dumps surface list), `check` (exit 1 on drift / 0 clean), `monitor` (`--config`, `--apply/--no-apply`→None uses apply_default; exit 1 if remaining), `report` (summarize default log as JSON), `schema` (`--out` optional, indented JSON from `review_record_schema()`, K6); all take `--config` (default cdmon.yaml, config_dir=parent); every `CodeDocMonitorError` printed as `error: <msg>` to stderr + non-zero exit, no traceback (K8). Offline+deterministic throughout (MockBackend default, injected now); NO network in any test (K4). |
| CDM-07 | e2e + dogfood + docs | DONE | 198 tests pass (10 system + 4 dogfood + 1 extract-elision new); ruff fmt+check clean; mypy clean (14 files); whole-pkg coverage 99.2% (≥90 gate met); `cdx check --config cdmon.yaml` exit 0 | **ends EPIC-CDM.** SELF-HEAL COMPLETENESS FIX: a realistic code change raises BOTH a HASH (fingerprint) + REGION drift; the mock previously ESCALATEd the HASH so `monitor --apply` left drift behind. Added pure `heal.render_corrected(doc_text, surface)` (regions regenerated + fingerprint refreshed, reusing the same logic as `regenerate_regions` via shared `_corrected`) + `manifest.parse_text` (string twin of `parse_doc`); MockBackend HASH→FIX(whole-doc=render_corrected) so the loop FULLY self-heals (verified: 2×FIX, remaining=[]). Updated 2 CDM-05 backend tests to the refined contract (HASH→FIX not ESCALATE; ESCALATE now demonstrated via UNHEALABLE). QUALITY: `extract._value_repr` elides long/multi-line variable values to `...` (a 2 KB `CONFIG_TEMPLATE` no longer bloats a doc cell; short values kept verbatim so CDM-02 tests still pass). created `tests/test_system.py` (shared file→user+eng docs: public-sig change drifts BOTH + heals BOTH; docstring-only edit drifts ONLY eng (extraction-level audience invalidation); private-symbol change invisible to user guide; records written+emitted to FileSink central; UNHEALABLE→ESCALATE stays in remaining; backend swap mock→claude-code via injected fake runner = same orchestration; CLI check/monitor/report/schema exit codes + clean error on bad config), `tests/test_dogfood.py` (real cdmon.yaml resolves against real code, shared schema.py referenced ≥2 ways, checked-in docs in sync, full self-heal on a copy). DOCS: `cdmon.yaml` dogfood config (3 docs over the package, schema.py shared via sub-file symbols), `docs/api/{foundation,pipeline,remediation}.md` (healed, in sync), `docs/REVIEW_RECORD_SCHEMA.json` (emitted public schema), README status/schema/dogfood sections. |

| CDM-08 | layout standard + lint | DONE | 224 tests pass (17 layout + 6 cli + 1 system + 1 dogfood new); ruff fmt+check clean; mypy clean (15 files); coverage layout.py=100% (whole-pkg 98.98%, ≥90 gate met); K0/K7/K10 upheld | **standardizes the file structure of managed .md/.html docs.** Wrote `docs/LAYOUT_STANDARD.md` (v1.0.0): canonical anchor order (front matter → `#` title → `>` purpose → prose → `CDM:BEGIN/END` regions), managed front-matter schema (`cdm.schema_version`/`audience`/`fingerprint`), unified marker grammar with helium's `HELIUM:AUTOGEN … START/END` documented as an alias, and the HTML-twin pairing rule (1:1 `X.md`→`X.html`, embedded `*-md-sha256` body hash, derived-not-edited). Created `custodex/layout.py` (pure): `LayoutCode`/`LayoutIssue`, `md_source_hash` (CRLF-normalized sha256[:16], matches helium so the embedded hash is portable), `embedded_md_hash` (reads `code-doc-md-sha256` or `helium-docs-md-sha256`), `lint_doc` (front-matter + title/purpose + region declaration consistency), `lint_html_twin` (HTML_MISSING/NOT_DERIVED/STALE), `lint_config` (file reads; skips missing docs—`check` owns existence—reports malformed FM as MALFORMED_STRUCTURE), `scaffold_doc` (renders a fully-conformant in-sync doc from spec+surface), `stamp_doc_meta` (front-matter auto-fix). Added `manifest.stamp_standard_meta` (sets static keys, **preserves** `fingerprint` so heal is untouched—zero blast radius) and `DocumentSpec.html` flag. CLI: `cdx lint [--fix]` (structure gate, orthogonal to `check`'s content gate; `--fix` stamps static FM, structural issues still reported) + `cdx new-doc <id> [--force]` (scaffolder). Dogfood docs stamped with `schema_version`+`audience` (heal preserves them); `cdx lint` + `cdx check` both exit 0. Live demo: scaffold→lint clean→check clean→break purpose→lint flags MISSING_PURPOSE→corrupt FM→`lint --fix` restamps→re-scaffold→clean. |

## Open BLOCKED items
_(none)_

## EPIC H + EPIC-2 PROGRAM COMPLETE

With H-03 the whole EPIC-2 program is **COMPLETE**. The build went from a standalone
single-repo drift monitor (EPIC-CDM) to a full code→documentation observability
platform: lossless file/symbol coverage with ownership + waivers (EPIC A), four
region-authority modes — generated / llm / human / llm-seeded with a per-region
content lock (EPIC B), a docs-PR loop with a structural loop-breaker (EPIC C), a
learning layer that retrieves few-shot exemplars and auto-promotes recurring human
verdicts into zero-backend-call rules (EPIC D), a central FastAPI + SQLAlchemy
server with a versioned wire schema, per-repo auth, and computed status/health/
telemetry views (EPIC E), a React dashboard (EPIC F), adopter CI templates + a
proven external-repo e2e (EPIC G), and finally self-dogfooding to 100% engine
doc-coverage behind a hard `--fail-under 95` gate plus this regression corpus
(EPIC H). H-03's `tests/regression/` turns the program's hard-won lessons into a
runnable, self-documenting index of durable invariants — each case tagged with the
lesson it guards — so a learned failure mode that has a corpus guard can never
silently return. The whole platform is offline-testable (no network, no real LLM in
the default suite), deterministic, ruff+mypy clean, ≥90% test coverage, and
dogfoods its own config end to end. **744 tests** (722 + 22 corpus).

## EPIC-CDM result

A standalone, reusable code→documentation drift monitor. Config (YAML/JSON) groups
code files — to function/line/variable granularity — onto audience-tagged logical
documents; the engine extracts an audience-aware surface, detects drift, and a
pluggable LLM backend (mock default / headless claude-code / Anthropic api, chosen
by config) FIXes or INVALIDATEs each drift; every drift+verdict+fix is written to an
append-only review log and emitted to a central sink via a versioned public schema.
Audience requirement met at the strongest level: a user-guide's surface excludes
docstrings/private/local symbols, so a comment change produces NO user-guide drift
while the eng-guide flags it. A Document Layout Standard (v1.0.0) then standardizes
the *shape* of the docs themselves — canonical skeleton, unified marker grammar
(helium's spelling as a documented alias), front-matter schema, and the html-twin
pairing rule — machine-checked by `cdx lint` and scaffolded by `cdx new-doc`,
orthogonal to `check`'s content gate. Offline-testable end to end (no network, no
real LLM); ruff+mypy clean; ≥90% coverage; dogfoods its own config. 224 tests.

| CDM-09 | source:index layer | DONE | 279 tests pass (7 test_index new); ruff fmt+check clean; mypy clean (17 files); coverage index.py=100% (whole-pkg 97.3%, ≥90 gate met); dogfood check+lint clean | **implemented the `source='index'` region the records/templates work had stubbed** (`render_template` deferred it: "rendered by the index-aware layer"). New `custodex/index.py`: `render_index(template, index_spec, config, root)` renders a table over the config's OTHER documents (index excludes itself; `template.kind` optionally filters by audience), each column's `field` selecting a synthetic per-doc value (`doc_id`/`title` [linked to the html twin or .md]/`summary` [the `>` purpose]/`link`/`audience`/`path`). Wired the seam at three points: `drift.detect` renders index regions via the all-docs layer (not the single surface); `FixRequest` gained `index_body` and `monitor.run` pre-renders it so the `MockBackend` REGION path applies it — `monitor --apply` self-heals an index region exactly like a code-backed one. `DocumentSpec.code_refs` now defaults to `()` (an index doc has none). Documented as LAYOUT_STANDARD.md §7 (field table + audience filter). Dogfooded: `cdmon.yaml` gained an `api-index` template + `docs/api/index.md` landing page over foundation/pipeline/remediation. Live demo on a copy: re-purpose a doc → `check` flags `api-index` REGION drift → `monitor --apply` FIX → clean, index reflects the change. Built ON TOP of the other session's finished records/templates architecture (no parallel mechanism); the earlier reverted `index_of`/`collection.py` approach was abandoned in favour of their `source:index` design.

| CDM-10 | real-LLM CI gate | DONE | 287 tests pass + 1 live (real `claude -p`, 18s) deselected by default; ruff fmt+check clean; mypy clean (17 files); whole-pkg coverage 97.07% (≥90 gate met); dogfood re-healed + clean | **fixes a real-vs-mock divergence found by driving the headless `claude-code` backend on live dogfood drift.** A real `claude -p` reply to a HASH drift filled BOTH fix shapes (regenerated region AND full corrected `new_doc_text`); the old `apply_fix` precedence (region first) applied only the region, leaving the fingerprint stale → residual HASH drift, so `monitor --apply` was NOT single-pass idempotent against a real LLM (the mock never reproduces this — it returns one shape). FIX: (1) `heal.apply_fix` now prefers `new_doc_text` when both shapes are present (it's the authoritative full result and the only shape that refreshes the fingerprint); single-shape behaviour unchanged, so existing tests pass. (2) `backends.build_prompt` gained a per-drift-kind `Fix shape:` instruction (REGION → region fields only; HASH/whole-doc → `new_doc_text` only) so a well-behaved model fills exactly one shape; apply_fix precedence is the safety net. TESTS: offline regression at unit level (`test_heal.test_apply_fix_prefers_whole_doc_when_both_shapes_present`) + monitor level (`test_system.test_both_shapes_fix_self_heals_in_one_pass`, an injected backend that reproduces the both-shapes reply and asserts `remaining == ()`); plus a quarantined real-LLM e2e (`tests/test_live_llm.py`, marker `live_llm` excluded from the default suite via `addopts -m "not live_llm"`, K4 preserved) whose backend is **resolved from a config file** (`CDMON_LIVE_BACKEND`), skips when the configured backend's prerequisite (claude CLI / `$ANTHROPIC_API_KEY`) is absent, and asserts a real model self-heals BOTH a HASH and a REGION drift in one `monitor --apply`. CI: `.gitlab-ci.yml` — `tests:offline` (every push/MR: ruff+mypy+pytest+cov) and `tests:live-llm` (`pytest -m live_llm`, scheduled gate + manual, gated on `$ANTHROPIC_API_KEY`). Verified live: the real `claude -p` run passes the e2e in ~18s.

| CDM-11 | LangGraph remediation agent + separated .md artifacts | DONE | 322 tests pass (28 test_agent new); ruff fmt+check clean; mypy clean (23 files); agent subpackage coverage 100% (whole-pkg 97.40%, >=90 gate met); dogfood re-healed + clean | **turns the single-shot remediation prompt into a deterministic LangGraph workflow whose prompt is composed from separated, lazily-loaded Markdown artifacts, with a config-only model runtime.** New `custodex/agent/` subpackage: `prompts/{AGENT,PROTOCOL,TOOL,PERSONA}.md` (the recipe / wire-contract / fix-shapes / voice, in the juansync-synapse artifact style), `prompts.PromptLibrary` (lazy, cached, front-matter-stripped loader; loud BackendError on a missing artifact, K8), `runtime.resolve_driver` (the model leaf: `claude-code` headless CLI default | `api` Anthropic | `local` OpenAI-compatible endpoint; reuses the existing subprocess/urllib leaves so the only uncovered lines stay the real syscalls, K4), `graph.build_graph` (a compiled `StateGraph`: `select -> compose -> invoke -> parse`, a bounded `parse->compose` re-ask loop <= `max_parse_retries`, and a `fail` node that raises BackendError; fully deterministic, K10), `backend.AgentBackend` (satisfies the same `Backend` Protocol so `Monitor` is unchanged). `select_artifacts` is the "only when needed" rule (TOOL only for a healable drift, PERSONA only when enabled+present). CONFIG: new `AgentConfig` (driver/model/command/api_key_env/base_url/prompts_dir/use_persona/max_parse_retries/timeout_s) on `MonitorConfig.agent`; `BackendConfig.kind` gains `"agent"`; `make_backend(cfg, agent)` resolves it via a LAZY import of the subpackage so `langgraph` is an opt-in `[agent]` extra and K0 holds for the core (`mock`) path. TESTS: all offline with an INJECTED fake driver (no subprocess/network) — happy path, INVALIDATE/no-fix, the one-retry re-ask + nudge, retry-exhaustion -> BackendError, the three driver resolutions + their loud misconfig errors, artifact lazy-load/cache/loud-missing, the only-when-needed selection, config round-trip, `make_backend` wiring, and an end-to-end `Monitor.run(apply=True)` that heals a doc through the agent graph. Dogfood re-healed (config.py + backends.py edits drifted foundation+remediation). |
| A-01 | inventory.py: repo code-file discovery | DONE | 342 tests pass (+20 test_inventory, was 322), 1 live deselected; ruff fmt+check clean; mypy clean (24 files); inventory.py coverage 100% (errors.py 100%; whole-pkg 97.49%, ≥90 gate met); dogfood re-healed + `cdx check`/`lint` exit 0 | new pure stdlib-only module `custodex/inventory.py`: `CodeFile`/`Inventory` (frozen, extra="forbid"), `DEFAULT_INCLUDE`/`DEFAULT_EXCLUDE`, `discover_files(root, *, include, exclude) -> Inventory`. Glob matching is an in-house glob→regex translation (`_translate`) giving **`**/` = zero-or-more leading segments** — required because plain `fnmatch` and 3.11 `PurePath.match` cannot match a top-level `.venv`/`.git` against `**/.*/**`/`**/.venv/**`. A file is kept iff it matches ≥1 include AND 0 exclude (K0); output sorted by `path`, inherently deduped (one CodeFile per os.walk file, not per matching glob), no FS mutation, no wall-clock (K1/K10). `language` from a tiny ext table (`.py`/`.pyi`→python); a matched file with an unknown extension is kept as `language="unknown"` (losslessness). Added `InventoryError(CodeDocMonitorError)` to errors.py + the `## inventory.py` ARCHITECTURE section (pinned before impl). Loud (K8): missing root / file-as-root raise `InventoryError`. NOT wired into cli/config/monitor (later slices). Editing errors.py drifted docs/api/foundation.md → re-healed via `cdx monitor --apply`. |
| A-02 | inventory.py: symbol-level inventory | DONE | 353 tests pass (+11 test_inventory, was 342), 1 live deselected; ruff fmt+check clean; mypy clean (24 files); inventory.py coverage 100% (whole-pkg 97.51%, ≥90 gate met); dogfood `cdx check`/`lint` exit 0 (no reheal needed) | extended `custodex/inventory.py` with `FileSymbols`/`SymbolInventory` (frozen, extra="forbid") and `discover_symbols(inventory: Inventory, root: Path) -> SymbolInventory`. For each `CodeFile` in `Inventory.files` order, a `language == "python"` file is reduced to its symbols by **reusing `extract.extract_file(root / path)`** (no AST re-impl — K0); any non-python file is kept with `symbols=()` (losslessness — tracked, never dropped). Output preserves `Inventory.files` order AND `extract_file`'s symbol order (K10); pure, no FS mutation (K1). **Unparseable-file policy:** a syntax-error/unreadable python file lets `extract.ExtractionError` propagate, aborting the scan (loud — K8); a resilient `--skip-unparseable` mode is deferred to `.project/problems/A-02.md`. Added the `## inventory.py` A-02 block to ARCHITECTURE.md (pinned before impl). No new dependency. Real fixture mini-repo (`pkg/a.py` public fn + `_helper` + `Widget.method`, `pkg/sub/b.py`, non-py `notes.txt` → `language="unknown"`, `symbols=()`); asserts exact per-file symbol-name sets + `is_public` flags + `SymbolInventory.files` order == `Inventory.files` order. **No dogfood drift** (inventory.py is unwired and not in cdmon.yaml's tracked docs; errors.py untouched). |
| A-03 | coverage.py: ownership resolver | DONE | 365 tests pass (+12 test_coverage, was 353), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); coverage.py coverage 100% (whole-pkg 97.60%, ≥90 gate met); dogfood `cdx check` exits 0 (no reheal needed) | new pure dependency-free module `custodex/coverage.py`: `OwnedFile`/`OwnedSymbol`/`CoverageReport` (frozen, extra="forbid") + `resolve_coverage(config: MonitorConfig, inv: SymbolInventory) -> CoverageReport`. Crosses the symbol inventory with `config.documents[*].code_refs` — a **file is owned** iff some `code_ref.path == file.path`; a **symbol is owned** iff some ref on that file *selects* it, tested by **reusing `extract._select(full, ref.symbols, ref.lines, ref.names)`** and matching by `(name, lineno)` — so whole-file/`symbols`(class→methods)/`lines`-overlap/`names` semantics come for free (no selection re-impl — K0, K1: no FS access, inventory already carries the Symbols). `CoverageReport` exposes `documented/undocumented_files`, `documented/undocumented_symbols` (gap basket = PUBLIC & unowned), `percent_files`, `percent_public_symbols` (universe = public symbols; 100.0 on empty universe — no zero-div). **Two deliberate rules:** (1) **ownership ignores audience** — a user-guide ref over a file owns its PRIVATE symbols too (a deliberate divergence from `build_document_surface`, which filters; audience governs the hash/surface, not whether code is referenced); (2) **private symbols tracked losslessly in `symbols` but never counted** in the gap basket or `percent_public_symbols`. `arg_signature` NOT applied (narrows a surface, not ownership). All tuples sorted (files by path; symbols by (path,name,kind)); owners sorted+deduped (K10). Real fixture mini-repo (a.py whole-file→D1, b.py symbols=[Foo]→D2 [Foo+Foo.method owned, sibling bar a gap, _secret private], c.py unreferenced); asserts exact baskets/owners/percentages (files 2/3, public symbols 4/6=66.67%). Added `## coverage.py` ARCHITECTURE section (pinned before impl). NOT wired into cli/config/monitor (A-04/A-05). **No dogfood drift** (coverage.py unwired, not in cdmon.yaml's tracked docs). |
| A-05/06 | `cdx coverage` CLI + `--fail-under` gate + CI step | DONE | 388 tests pass (+7 test_cli, was 381), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); whole-pkg coverage 97.65% (≥90 gate met); the new `coverage` command's lines fully covered (cli.py 93% — remaining misses are pre-existing build/lint/new-doc/`__main__` lines, not this slice); dogfood `cdx check`/`lint` exit 0 (cli.py is not a tracked doc module → no reheal) | **READ-ONLY `coverage` command (K1) wiring `discover_files(include/exclude from cfg.coverage) → discover_symbols → resolve_coverage` and rendering both percentages + the three baskets.** `cli.py` (not a dogfood-tracked module): new `coverage(config, --json/json_out, --fail-under/fail_under: float\|None=None)`. Human render (`_coverage_lines`, deterministic K10): `files: X% documented (n/m)`, `public symbols: X% documented (n/m)`, then `documented N`, `undocumented N` + gap list (`path::name (kind)`), `waived N` + list (`path::name — reason`), and (when non-empty) the file-level undocumented + waived baskets. `--json` (`_coverage_payload`): `report.model_dump(mode="json")` (lossless `files`/`symbols`) PLUS the derived `@property` baskets/percentages added explicitly (pydantic does NOT dump `@property`, so `percent_*`/`undocumented_*`/`waived_*` are injected by hand) → round-trippable, a test parses stdout back. `--fail-under N` exits 1 iff `report.percent_public_symbols < N`, else 0; **absent ⇒ always exit 0** (informational like `report`, not a gate like `check`). Bad config → `error: <msg>` to stderr + Exit(1), no traceback (K8 — copied the established per-command try/except). **CI (A-06):** added an INFORMATIONAL, non-blocking `cdx coverage --config cdmon.yaml` step to `tests:offline` in `.gitlab-ci.yml` with a comment to enable `--fail-under` once H-02 raises self-coverage (NOT a hard gate today — the engine's own inventory/coverage/... modules are undocumented until EPIC H). **Real smoke on THIS repo:** `files: 22.2% documented (12/54)`, `public symbols: 18.2% documented (113/622)`, exit 0; `--fail-under 90` → exit 1, `--fail-under 5` → exit 0; JSON round-trips (`percent_public_symbols=18.17`, 509 gaps). The low numbers are expected and motivate EPIC H. Updated SPEC.md CLI table + README + cli.py module docstring. Tests (7, pinned-Typer CliRunner + `monkeypatch.chdir` per CDM-01): baskets+gap+waiver-reason output, JSON round-trip, fail-under above/below/absent exit codes, bad-config clean error, and file-level basket render (whole-file waiver + unreferenced file). |
| A-04 | config `coverage:` section + waivers | DONE | 381 tests pass (+16: 12 test_config, 4 deltas across test_coverage; was 365), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); changed-file coverage 100% (config.py 100%, coverage.py 100%; whole-pkg 97.64%, ≥90 gate met); **dogfood re-healed** (`cdx monitor --apply` healed foundation HASH+REGION after editing the tracked `config.py`) then `cdx check`/`cdx lint` both exit 0 | additive `coverage:` config + waiver-folding resolver. **config.py:** new `WaiverEntry{path:str glob, symbol:str|None=None, reason:str REQUIRED}` and `CoverageConfig{include, exclude, waive=()}`, both frozen+extra="forbid"; `MonitorConfig.coverage: CoverageConfig = CoverageConfig()` (defaulted → old configs without `coverage:` still load, K6). **Import-cycle note:** `inventory` imports `extract` imports `config`, so config CANNOT import inventory — the include/exclude defaults are inlined as `_DEFAULT_INCLUDE/_EXCLUDE` and a lock-step test (`test_coverage_defaults_match_inventory`) asserts they equal `inventory.DEFAULT_INCLUDE/EXCLUDE`. `CONFIG_TEMPLATE` gained a commented `coverage:` block (include/exclude/waive whole-file + single-symbol examples) that still round-trips. **coverage.py:** `OwnedFile`/`OwnedSymbol` gain `waived_reason: str|None=None`; `resolve_coverage` folds `config.coverage.waive` — an UNOWNED file/symbol whose path matches a waiver glob (reuses `inventory._translate`) and (for a symbol) whose name == entry.symbol (None ⇒ whole-file, waives file + all its symbols) is stamped with `reason` and moved to new `waived_files`/`waived_symbols` baskets, OUT of `undocumented_*`. `percent_files`/`percent_public_symbols` drop waived from BOTH numerator and denominator (universe = total − waived → fully-waived-or-documented = 100%). **Only unowned (and, for symbols, public) gaps are waivable:** waiver on a documented item = inert (documented wins, `waived_reason` stays None); on a private symbol = inert (never in public basket); matching nothing = **silently inert** (chosen over loud: the A-05 scan scope legitimately removes a waived path from the inventory, so a "stale" waiver is normal — the only loud failure is a missing `reason`, K8, caught at load). **Additive guarantee proven:** default empty `waive` → `test_default_empty_waive_is_identical_to_a03` asserts the report `==` the A-03 path, and ALL pre-existing test_coverage A-03 tests pass unchanged. Pinned ARCHITECTURE.md (config + coverage sections) before impl. Out of scope (A-05): no `cdx coverage` CLI. |
| A-07/08 | gap→owner suggestions + coverage manifest writer | DONE | 401 tests pass (+13: 8 test_coverage A-07, 5 test_cli A-08; was 388), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); coverage.py 99% / new manifest code in cli.py fully covered (whole-pkg 97.67%, ≥90 gate met); dogfood `cdx check`/`cdx lint` exit 0 (coverage.py/cli.py not tracked → no reheal) | **A-07 — pure deterministic gap→owner suggester (Decision 1: a heuristic in `coverage.py`, NOT the `Backend` Protocol — no LLM, no I/O, no new dep; K0/K10).** New `OwnerSuggestion{path, name: str\|None, suggested_doc_id, is_new_doc, reason}` (frozen, extra="forbid") + `suggest_owners(report, config) -> tuple[...]`. For each PUBLIC, unowned, non-waived gap (`report.undocumented_symbols` — private & waived gaps never suggested): if any doc already owns a SIBLING symbol in that file → suggest the **lowest** such doc id, `is_new_doc=False`, reason "sibling symbols already in <doc>"; else (fully-unowned file) → propose a NEW doc id via `_proposed_doc_id` (**scheme: `pkg/sub/mod.py → pkg-sub-mod`** — drop `.py`/`.pyi`, replace `/`→`-`; full path keeps ids unique across same-named modules, yields an id/fs-safe token), grouping all of that file's gaps under it, `is_new_doc=True`, reason "no doc references <F>". Sorted by `(path, name)` (K10). **A-08 — `cdx coverage --write [PATH]` (Decision 2: a dedicated regenerable manifest at `.cdmon/coverage.json` [gitignored], NOT injected into the hand-authored `cdmon.yaml`; K1/K7/K10).** `--write` is a bool flag + an optional positional `[PATH]` Argument (typer 0.26 has no native optional-value option; the `flag_value`/`is_flag=False` click idiom isn't exposed and `click` isn't importable under K0, so bool-flag-plus-positional is the clean parse: `--write` bare → default path, `--write out.json` → custom). It writes `_coverage_manifest_text` = the A-05 `_coverage_payload` PLUS a top-level `suggestions` list, `json.dumps(indent=2, sort_keys=True)+"\n"`. **Idempotent (K7, mirrors `heal.regenerate_regions`):** reads the existing file first; equal content → no rewrite, prints "coverage manifest unchanged"; else writes (mkdir -p parents) + prints the path. Default `coverage` stays read-only (K1); `--write` composes with `--json`/`--fail-under` (stdout/exit unaffected). **Idempotency smoke on THIS repo:** 1st `cdx coverage --config cdmon.yaml --write` → "wrote coverage manifest to .cdmon/coverage.json"; 2nd (no source change) → "coverage manifest unchanged" (content+mtime identical); both exit 0. Manifest valid JSON, 524 suggestions. `.cdmon/` is gitignored so the artifact never dirties the repo; generated `coverage.json` removed after the smoke. Pinned ARCHITECTURE.md (coverage.py + cli.py sections) before impl; updated SPEC.md CLI table + README. Tests: A-07 sibling-suggestion + lowest-id-when-several + new-doc-grouping + private-excluded + waived-excluded + sorted + deterministic + frozen/forbid-extra; A-08 write/parse-back, second-write "unchanged"+identical, custom path, read-only default, valid-JSON-sorted-keys. **EPIC A complete.** |
| B-01 | config.py: region authority modes (schema) | DONE | 410 tests pass (+10 test_config B-01, was 401), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); config.py coverage 100% (whole-pkg 97.69%, ≥90 gate met); **dogfood re-healed** (`cdx monitor --apply` healed foundation HASH+REGION after editing tracked `config.py`) then `cdx check`/`cdx lint` both exit 0 | **SCHEMA + VALIDATION + ACCESSOR ONLY — no drift/heal behavior change (that's B-02/03/04).** New `RegionMode(str, Enum)` = `generated`(DEFAULT)/`llm`/`human`/`llm-seeded` (str-valued for clean YAML/JSON round-trip; same style as `Audience`). `DocumentSpec` gains `region_modes: dict[str, RegionMode] = {}` (region id → mode; absent ⇒ generated, so old configs load unchanged — K6 additive) plus a pydantic `@model_validator(mode="after")` enforcing **every `region_modes` key ∈ `region_keys`** — an undeclared region raises `ValueError`, surfaced as `ConfigError` by `load_config`'s existing `ValidationError` wrap (K8). Accessor `DocumentSpec.mode_for(region_id) -> RegionMode` returns the declared mode or `RegionMode.GENERATED` — the single seam B-02/03/04 consume (no scattered `.get(..., GENERATED)`). `CONFIG_TEMPLATE` gained a commented `region_modes:` example under the eng-guide doc (still round-trips). Models stay frozen+extra="forbid". Pinned ARCHITECTURE.md (`config.py` `RegionMode`+`region_modes`+`mode_for`) before impl; added LAYOUT_STANDARD.md §7 "Region authority modes" (mode table + example; renumbered the index-regions section to §8). Tests: enum values, yaml+json round-trip with `region_modes`, absent-default via `mode_for` (returns GENERATED for any region), `mode_for` declared-vs-fallback, unknown mode string → ConfigError, mode naming an undeclared region → ConfigError, frozen, template still round-trips. **B-02 NOTE:** heal must NOT overwrite `human` regions — it calls `spec.mode_for(region_id)` and skips/advises when it returns `RegionMode.HUMAN`. |
| B-03 | manifest/heal/drift: `llm-seeded` lock via per-region content hash | DONE | 438 tests pass (was 419; +19: 8 test_manifest, 6 test_heal, 3 test_drift, 2 test_system), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); changed-module coverage drift.py=100% monitor.py=100% manifest.py=98% heal.py=98% (whole-pkg 97.75%, ≥90 gate met); **dogfood re-healed** (`cdx monitor --apply` healed pipeline+remediation HASH+REGION after editing tracked manifest.py/heal.py/drift.py) then `cdx check`/`cdx lint` exit 0; full suite green POST-reheal | **Mode-agnostic lock + B-02 advisory-persistence fix.** `manifest.py`: `region_body_hash(body)->str` (sha256[:16], CRLF-normalized — mirrors `layout.md_source_hash`, asserted equal; K10), `stored_region_hash(doc,id)`/`set_region_hash(meta,id,value)` additive under `cdm.region_hashes` (survives `set_fingerprint`, which copies the whole `cdm` map — zero blast), and the **SINGLE SHARED** lock predicate `region_is_locked(doc,id,current_body)` = stored hash present AND `region_body_hash(current_body) != stored` (CDM-07 one-truth; drift+heal both call it). `heal.py`: `_corrected`/`render_corrected`/`regenerate_regions`/`apply_fix` gain `modes: Mapping[str,RegionMode]|None`; new `locked_region_ids(doc,modes)` (= human ∪ human-edited llm-seeded) is auto-added to `preserve`, and heal STAMPS `region_hashes[id]` for every authored region (generated/unlocked-llm-seeded → hash of written body) and for human regions (hash of current body → persistent advisory), keeping a *locked* llm-seeded region's existing stamp (re-stamping would falsely unlock). `monitor.run` passes the full `region_modes` to `apply_fix` so the real `--apply` path locks+stamps at the write boundary. `drift.detect`: an `llm-seeded` region behaves like `generated` while unlocked (REGION healable=True) and like `human` once locked (REGION healable=False on code move, "human-owned" advisory, UNHEALABLE suppressed for a locked no-renderer); the human advisory now PERSISTS across a fingerprint heal — fires while a stamped human region's body still matches its stamp, clears when the body changes — **fixing B-02's known limitation**. Default (no `modes`, no `region_hashes`) == B-02/EPIC-A byte-for-byte (all prior tests green UNCHANGED, K9). Validable goal proven by `test_system.test_llm_seeded_fill_then_lock_three_phase` (fill→stamp, human-edit→locked re-heal byte-identical+idempotent, locked+code-move→REGION healable=False) plus its foil `test_llm_seeded_unlocked_regenerates_on_code_move`. Pinned the new manifest/heal signatures in ARCHITECTURE.md before impl. K2/K5/K7/K8/K9/K10 upheld. |
| B-04/05 | mixed-authorship e2e + lint of region modes (**EPIC B COMPLETE**) | DONE | 449 tests pass (was 438; +11: 7 test_layout region_states, 2 test_cli lint-modes, 2 test_system mixed-authorship), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); changed-module coverage layout.py=97% (new B-05 code fully covered; the 4 remaining partial branches are pre-existing index/scaffold paths), cli.py=93% (pre-existing misses); whole-pkg 97.67% (≥90 gate met); **dogfood re-healed** (`cdx monitor --apply` healed foundation HASH after editing tracked `config.py`'s `RegionMode` docstring — also flushed accumulated B-02/B-03 symbol-table lag on pipeline/remediation) then `cdx check`/`cdx lint` exit 0; full suite green POST-reheal | **B-04 — four authority modes coexist in ONE doc + the interim `llm` rule.** `test_system.test_mixed_authorship_four_regions_e2e`: a single fixture doc carries `gen` (generated default) + `human` + `seeded` (llm-seeded) + a declared `llm` region (each renderer-backed via a `region_templates` `symbols`-source table with a `signature` column so a sig change moves the body). After a public-signature change + `monitor --apply`: `gen`+`llm` regenerated (`c=0` in the rendered signature), `human` byte-identical + REGION healable=False advisory (persists across the fingerprint heal), `seeded` locked (human-edited) byte-identical, second `--apply` idempotent. **INTERIM `llm` rule (decided + encoded + documented): a region declared `llm` behaves EXACTLY like `generated`** — drift/heal have NO `llm` branch, so a renderer-backed `llm` region is mechanically rendered + kept in sync and a no-renderer one surfaces UNHEALABLE/ESCALATE (loud, K8 — never a silent stale doc). This needed ZERO new engine code (the `llm` enum value already falls through to the generated path); documented additively in `config.RegionMode` docstring + LAYOUT_STANDARD §7 with a note that **B-06** replaces it with real backend prose authoring. The SAFE choice: a pure-`llm` region has no human prose to clobber yet and the doc never silently rots. **B-05 — `cdx lint --modes` per-region authority STATE surface (NOT a new gate).** New pure `layout.RegionState` (frozen, extra=forbid; `doc_id`/`region_id`/`mode`/`has_renderer`/`locked`/`advisory`) + `region_states(doc, spec, *, known)` (one per region present-AND-declared, in `region_keys` order, using `spec.mode_for` + `manifest.region_is_locked` + `known_region_ids` — NO re-validation, the modes map is validated at config-load) + `config_region_states(config, config_dir)` (file-reading driver; skips missing/malformed docs like `lint_config`). `cdx lint --modes` prints `doc::region — mode [renderer\|no-renderer] [locked] [advisory]` and **keeps lint's existing structural pass/fail** (the flag adds an info view, never changes the exit code — proven by `test_lint_modes_does_not_suppress_structural_failures`). Documented `cdm.region_hashes` (the per-region content hash driving human/llm-seeded behaviors) + the interim `llm` rule + the lint state surface in LAYOUT_STANDARD §7. Pinned `RegionState`/`region_states`/`config_region_states` + the interim `llm` rule in ARCHITECTURE.md before impl. K0/K2/K5/K7/K8/K9/K10 upheld. **EPIC B is COMPLETE** (generated/human/llm-seeded fully working e2e; pure-`llm` deferred to B-06). |
| B-02 | heal/drift/monitor: `human` regions reported, never auto-edited | DONE | 419 tests pass (was 410; +9 across test_heal/test_drift/test_system), 1 live deselected; ruff fmt+check clean; mypy clean (25 files); whole-pkg 97.67% (≥90 gate); **dogfood re-healed** (`cdx monitor --apply` → remediation REGION→FIX) then `cdx check`/`cdx lint` exit 0 | **End-to-end `human` mode.** The "engine never authors a human region" guarantee lives at the heal WRITE boundary, not the backend: `apply_fix(doc_path, fix, *, preserve=frozenset())` re-injects the doc's CURRENT body for every preserved region before writing a whole-doc fix (so even a backend that returned whole-doc text overwriting a human region cannot clobber it), and a region-shaped fix targeting a preserved id is a no-op. `preserve` also threads through `_corrected`/`render_corrected`/`regenerate_regions` (skip those ids; fingerprint can still move). `monitor.run` computes `preserve = {rid for rid in spec.region_keys if spec.mode_for(rid) is HUMAN}` and passes it to `apply_fix`. `drift.detect`: a human renderer-backed region is flagged `REGION, healable=False` ("human-owned … review manually") **only when the fingerprint is stale (code moved)** — a human body always differs from the generated render, so body-vs-render can't be the signal; the code-change (fingerprint) is. A human no-renderer region suppresses UNHEALABLE (intentional, not an error). Default empty `preserve` ⇒ byte-identical to EPIC-A (all prior heal/drift/monitor/system tests green unchanged). **Completed by the orchestrator after the slice subagent hit the session limit mid-TDD (red tests written, impl pending) — see `.project/problems/B-02-HANDOFF.md`.** Reconciled one self-contradictory subagent test (`test_drift` stale-human expected a flag with the fingerprint in-sync, which conflicts with `test_system`'s baseline-clean): fixed it to trigger via a code change (CODE_V2). **KNOWN LIMITATION (for B-03/B-04):** once `monitor --apply` heals the HASH (refreshes the fingerprint), the human region's review signal clears even if the human never updated their prose — a per-region content-hash (the B-03 llm-seeded machinery) is the proper fix for a persistent advisory. |
| B-06 | drift/backends/heal/monitor: pure-`llm` (no-renderer) prose authoring (**EPIC-2 LAST DEFERRAL CLOSED**) | DONE | 759 tests pass (was 744; +15 net: 3 test_drift, 7 test_backends, 3 test_heal, 1 new test_system e2e, 1 `[B-06]` regression guard, minus the renderer-backed-`llm` e2e comments retuned in place), 2 deselected; ruff fmt+check clean; mypy clean (36 files); changed-module coverage backends.py=100% drift.py=100% (heal.py=98%, pre-existing partial), whole-pkg 98.21% (≥90 gate met); **dogfood re-healed** (`cdx monitor --apply` healed foundation HASH+REGION after editing tracked drift.py/backends.py/heal.py/monitor.py/config.py docstring) then `cdx check`/`cdx lint` exit 0 and `cdx coverage --fail-under 95` exit 0; full suite green POST-reheal | **Replaced the interim `llm`==`generated` rule with real backend prose authoring for a NO-renderer `llm` region.** Five seam changes, all ADDITIVE (K6): (1) `FixRequest.region_mode: RegionMode = RegionMode.GENERATED` — `monitor.run` sets it from `spec.mode_for(drift.region_id)` (GENERATED for a whole-doc/no-region drift); every pre-B-06 FixRequest/test unchanged. (2) `drift.detect` (`region_id not in known` branch): a no-renderer region whose `mode is LLM` is **prose-authored, not unknown** — code moved (`stored != current` whole-doc fingerprint) → a healable `REGION` drift (detail `"llm-authored region {id!r} is stale; backend will re-author from the current surface"`); code unchanged → NO drift (its prose legitimately differs from any render, so it is NOT graded against one); a non-`llm` no-renderer region keeps today's `UNHEALABLE` (loud, K8). (3) `MockBackend.propose` new rule between rule 1 and 2: a no-renderer `llm` REGION drift → `FIX` with `new_region_body` = `_authored_prose(surface)`, a DETERMINISTIC, IDEMPOTENT, audience-aware sentence enumerating the public symbols (K2/K3/K10 — same surface ⇒ identical body ⇒ a re-author is a clean no-op, K7). (4) `build_prompt` appends a prose clause ("LLM-authored PROSE … do NOT emit a symbol table") for an `llm` REGION request — real backends only; the mock + existing single-shot prompt tests stay green. (5) **heal idempotence point VERIFIED, no code change needed:** `_corrected`/`render_corrected` already PRESERVE a no-renderer region's body byte-identical (the `if region_id not in known: continue` skip), so the whole-doc HASH fix never blanks the prose — added guard tests (`test_render_corrected_preserves_no_renderer_llm_region`, `..._whole_doc_never_blanks_llm_region`) to pin it. The four-goal validable goal is proven by `test_system.test_pure_llm_no_renderer_authored_e2e` (re-author on code move → idempotent second `--apply` no-op → human region byte-identical → `apply=False` keeps it surfaced+recorded, never silently dropped) and guarded by `[B-06]` in `tests/regression/test_corpus_pipeline.py`. The renderer-backed `llm` interim case (`test_mixed_authorship_four_regions_e2e`) is UNCHANGED behavior and still asserted (its "interim" comments retuned to "renderer-backed -> like generated"). Records: ARCHITECTURE.md "B-04 interim" note replaced with B-06 behavior; `config.RegionMode` docstring + LAYOUT_STANDARD §7 (`llm` no longer ==generated); ROADMAP B-06 ticked + EPIC B note updated. **DEVIATION from spec:** Goal-4 ("no authoring path still surfaces it") is demonstrated via `monitor.run(apply=False)` (the drift stays in `remaining` + is recorded, K5/K8) rather than a `regenerate_regions` mechanical heal — because per the spec's own fingerprint-keyed design a mechanical heal REFRESHES the fingerprint, which would (correctly) clear the per-doc staleness signal; surfacing-without-applying is the honest, design-consistent loudness proof. **EPIC-2's last deferral is CLOSED.** K2/K3/K4/K5/K6/K7/K8/K9/K10 upheld. |
| C-01/02 | `cdx sync-pr` doc-patch producer + offline CI `docs:gate` (**EPIC-C begins**) | DONE | 464 tests pass (was 449; +15: 11 test_syncpr incl. the `.gitlab-ci.yml` parse test, +5 test_cli `sync-pr`, 1 deselected live); ruff fmt+check clean; mypy clean (26 files); syncpr.py coverage 98% (1 partial branch: the dry-run "file existed after delete" guard), cli.py 94%, whole-pkg 97.70% (≥90 gate met); `cdx check --config cdmon.yaml` exit 0 (syncpr.py/cli.py not dogfood-tracked → no reheal); **dry-run smoke on THIS repo:** `cdx sync-pr --dry-run --config cdmon.yaml` → empty patch (stdout), `clean` (stderr), exit 0, and a follow-up `cdx check` still exits 0 (no mutation, K1) | **The git-free, fully-testable core of the docs-PR flow (the bot that opens the MR is C-03).** New `custodex/syncpr.py`: `SyncResult{patch:str, changed_paths:tuple[str,...], summary:str}` (frozen, extra="forbid") + `sync_pr(monitor: Monitor, *, dry_run=False) -> SyncResult`. It orchestrates AROUND `Monitor` with **zero new heal/backend logic**: snapshot each configured doc's text (or `None` if missing) BEFORE → `monitor.run(apply=True)` (heals in place via the existing pipeline) → read AFTER → for every changed doc build a deterministic `difflib.unified_diff` (`fromfile="a/<spec.path>"`, `tofile="b/<spec.path>"`, `lineterm=""`, sorted by path; a newly-created file diffs against `""`, an add). **Authority correctness is FREE** — because it reuses `monitor.run`, B-02/B-03 region authority is honored automatically: a `human`/locked-`llm-seeded` body is never authored, so it never appears in the patch (regression-guarded by `test_sync_pr_human_region_body_not_in_patch`). **`dry_run` (K1) — restore-INCLUDING-delete:** after computing the patch, the document tree is put back byte-for-byte: each pre-existing doc rewritten to its before-text AND **any file the run NEWLY created (before=None, e.g. a healed MISSING_DOC stub) DELETED** — proven byte-identical by before/after snapshots (`test_sync_pr_dry_run_*`, the stub-delete path exercised via an injected `_CreatingBackend` that authors a whole-doc fix so the run actually creates the file). Idempotent (K7): clean repo / second run after apply → empty patch + `summary=="clean"`. Offline (mock backend default, K4); deterministic (K10). **cli.py:** `cdx sync-pr [--config] [--out FILE] [--dry-run]` — default applies + prints the patch to stdout (or writes `--out`) + summary to stderr, exit 0; `--dry-run` computes the same patch with NO mutation; malformed config → `error:` + Exit(1), no traceback (K8 — reused the established per-command try/except). **.gitlab-ci.yml (C-02):** new fast OFFLINE `docs:gate` job (stage `test`, MRs + branches) running `cdx check --config cdmon.yaml` + `cdx lint --config cdmon.yaml` — fails the pipeline on content/structure drift, orthogonal to `tests:offline`; a comment notes C-04 will add the loop-safety path rule so bot doc-only commits don't re-trigger. A test `yaml.safe_load`s the file + asserts the job's shape (still valid YAML). Pinned the `syncpr.py` contract in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table + README. K1/K4/K7/K8/K10 upheld. |
| C-03 | bot-PR opener — `cdx open-docs-pr` (injected GitLab transport) | DONE | 483 tests pass (was 464; +19: 15 `test_pr.py`, +4 `test_cli` `open-docs-pr`); ruff fmt+check clean; mypy clean (27 files); **`pr.py` coverage 100%** (only `# pragma: no cover` lines = the real `urllib.urlopen` leaf in `_UrllibGitLabHttp.request` + the `_NullTransport.submit` never-called guard), whole-pkg 97.78% (≥90 gate met); dogfood RE-HEALED (cli.py edit drifted `docs/api/foundation.md` → `cdx monitor --apply` → `cdx check`/`lint` both clean, exit 0); **dry-run smoke on THIS repo:** `cdx open-docs-pr --dry-run --config cdmon.yaml` → `clean — nothing to open`, exit 0 (dogfood clean → empty sync → no-op, no transport built, no network) | **Turns a `SyncResult` (C-02) into a docs MR behind an INJECTED transport (K4).** New `custodex/pr.py`: `MergeRequestPlan{source_branch,target_branch="main",title,description,files:tuple[(path,content),...],labels=()}` (frozen, extra="forbid"); `PRTransport` Protocol (`submit(plan)->dict`); `GitLabTransport` (DEFAULT, **stdlib urllib only — K0, no python-gitlab/requests**) with `from_env(project_env="CI_PROJECT_ID", token_env="CDMON_GITLAB_TOKEN", api_env="CI_API_V4_URL")` raising a loud `TransportError` (new errors.py subclass) on a missing/empty var (K8); `plan_docs_pr(sync, root, *, target_branch="main", ref=None, branch_prefix="cdmon/docs-sync", labels=()) -> MergeRequestPlan|None` (None when `sync.patch==""`; reads each `changed_paths` file's CURRENT healed content from `root` into `files`; **deterministic branch `f"{prefix}-{sha256(patch)[:12]}"`, K10**; `ref` lands in title + description = provenance for C-05); `open_docs_pr(sync, root, *, transport, dry_run=False, **plan_kw) -> dict|None` (empty→None no-op; dry_run→`plan.model_dump()` with NO transport call; else `transport.submit`). **Inject-the-leaf (CDM-04/05):** `GitLabTransport.submit` runs the canonical GitLab 3-call flow — POST `/repository/branches` (source off target), POST `/repository/commits` (one commit, one `update` action per healed file), POST `/merge_requests` — each through ONE injected `_GitLabHttp` leaf; the single real `urllib.request.urlopen` lives in `_UrllibGitLabHttp.request` (the only `# pragma: no cover`). Tests: fake `PRTransport` asserts the EXACT plan; fake `_GitLabHttp` asserts the 3 ordered calls + bodies + token; the build-default branch is covered by stubbing `_UrllibGitLabHttp.request` (so "build the real client" runs with zero network); the missing-env loud error asserted for both `CI_PROJECT_ID` + `CDMON_GITLAB_TOKEN`. **cli.py:** `cdx open-docs-pr [--config] [--dry-run] [--target] [--ref]` — heals via `sync_pr` (in `--dry-run` uses `sync_pr(dry_run=True)` so NOTHING is written), empty sync prints `clean — nothing to open` (no transport), `--dry-run` prints the plan JSON via a module-level `_NullTransport` (never submitted to — no env needed), else builds `GitLabTransport.from_env()` (loud K8) + submits + prints the MR `web_url`. pr.py/cli.py NOT dogfood-tracked except cli's appearance in `foundation` (re-healed). Pinned `pr.py` in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table + README. K0/K4/K8/K10/K9 upheld. |
| C-04/05 | loop-safety (`should_sync`) + provenance (`source_sha`) — **EPIC C COMPLETE** | DONE | 502 tests pass (was 483; +19: 5 test_syncpr `should_sync` truth-table + 1 `.gitlab-ci.yml` loop-guard parse, 4 test_schema `source_sha`/back-compat/schema, 2 test_monitor stamping, 7 test_cli `should-sync` exit codes + `monitor --ref` precedence), 1 live deselected; ruff fmt+check clean; mypy clean (27 files); changed-module coverage **schema.py=100% monitor.py=100% syncpr.py=99%** (1 partial branch = the pre-existing dry-run restore guard), cli.py=94% (the 14 misses are all PRE-EXISTING `build`/`open-docs-pr` paths, not new code), whole-pkg 97.80% (≥90 gate met); **dogfood RE-HEALED** (editing tracked `schema.py`+`monitor.py` drifted `docs/api/{foundation,remediation}.md` → `cdx monitor --apply --config cdmon.yaml` healed remediation HASH+REGION → `cdx check`/`cdx lint` both exit 0); **full suite GREEN POST-reheal (502 passed, 1 deselected)** | **C-04 loop-safety — the structural loop-breaker.** New pure `syncpr.should_sync(changed_files: Iterable[str], config: MonitorConfig) -> bool`: managed set = `{PurePosixPath(d.path) for d in config.documents}`; returns `True` iff ANY changed file is NOT a managed doc path, `False` when EVERY changed file is a managed doc (a bot doc-only commit → SKIP heal) OR the set is empty. POSIX-normalized via a `_norm` helper (back-slash→`/`, `./`/`.`-segments collapsed) so `./docs/x.md`, `docs/x.md`, `docs\x.md` all compare equal. Read-only + deterministic (K1/K10), provider-agnostic (just the changed-file list). **cli.py:** `cdx should-sync [--config] [FILES...]` — exits **0 (PROCEED)** when `should_sync` True, **1 (SKIP)** when False; reads FILES from args OR newline-separated stdin (`git diff --name-only \| cdx should-sync`); malformed config → `error:` + Exit(1) (K8). **.gitlab-ci.yml:** new `docs:heal` job (branch pushes, manual+allow_failure) pipes `git diff --name-only $CI_COMMIT_BEFORE_SHA HEAD` into `cdx should-sync` and `exit 0`s (no MR) on a doc-only commit — breaking PR→heal→PR structurally — else `cdx monitor --apply --ref $CI_COMMIT_SHA` + `cdx open-docs-pr --ref $CI_COMMIT_SHA`; still valid YAML (asserted). **C-05 provenance — ADDITIVE schema (K6).** `schema.py`: `ReviewRecord` gains `source_sha: str | None = None` APPENDED LAST (existing field order untouched; default None keeps pre-C-05 records valid — a legacy JSONL line WITHOUT the field still `model_validate_json`s, proven by `test_pre_c05_record_without_source_sha_still_parses`; `cdx schema` re-emits it). `monitor.py`: `Monitor.__init__(..., source_sha: str | None = None)` stored on `self._source_sha`; `_record_for` stamps `source_sha=self._source_sha` on EVERY record (default None ⇒ today's records unchanged, all prior CDM-04/06 record tests green UNCHANGED). **cli.py:** `cdx monitor [--apply] [--ref/--source-sha REF]` — **ref precedence (one source of truth): explicit `--ref`/`--source-sha` → else `$CI_COMMIT_SHA` → else None**; the SAME ref flows to `open-docs-pr --ref` so the record's `source_sha` and the MR title/description agree. Pinned both signatures + the ref-precedence rule in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table (`should-sync`, `monitor --ref`) + README. K1/K6/K8/K9/K10 upheld. **EPIC C is COMPLETE** — the PR-driven loop is closed end-to-end and SAFE. **EPIC D note:** `cdx resolve` + a resolution/outcome field on the record builds directly on this proven schema additivity (append-last + default → old logs parse). |
| E-03 | central FastAPI server skeleton (`custodex.server`, `[server]` extra) — server-side of E-01/E-02 | DONE | **608 tests pass (was 597; +11 test_server), 1 deselected (live_llm)**; ruff format --check + ruff check both clean; mypy clean (33 source files); **server pkg 100% coverage** (`server/__init__.py` 100%, `server/app.py` 100%, `server/store.py` 100% — the only `# pragma: no cover` is the `main()`/`if __name__` uvicorn-launch leaf), whole-pkg **98.07%** (≥90 gate met); **lazy boundary verified** (`import custodex` pulls in NO `fastapi` and NOT the `server` subpackage — asserted via a `sys.modules` check); dogfood `cdx check --config cdmon.yaml` exit 0 (`server/` not tracked in cdmon.yaml → NO reheal needed); **TestClient round-trip smoke (`.venv/bin/python -c`):** `POST /repos`→201 `{repo_id}`, `POST /ingest`→202 `{record_id}`, `GET /repos`→`['acme/widget']`, `GET /repos/acme%2Fwidget/records`→200 n=1 **byte-for-byte True** (the record round-trips through the SHARED `ReviewRecord` unchanged), unknown-repo ingest→404 | **The CENTRAL side of E-01/E-02: a FastAPI app that ingests repo registrations + review records over the SHARED, versioned schemas — NO hand-written DTOs (K6).** New `custodex/server/` subpackage behind the `[server]` pip extra (`fastapi>=0.115`, `uvicorn[standard]`, `httpx`); imported LAZILY so `import custodex` core needs no fastapi (K0, mirrors `[agent]`). **`store.py`** (pure pydantic/stdlib, NO fastapi): `RegisteredRepo` (frozen, extra="forbid"; reuses the shared `RepoIdentity`) + the **`Store` Protocol** (the E-04 seam: `add_repo`/`get_repo`/`list_repos`/`add_record`/`records_for`) + dict-backed `InMemoryStore` (deterministic INSERTION order, K10; repeat `add_repo` UPDATES in place). **`app.py`** `create_app(store: Store | None = None) -> FastAPI` — store DI'd via a FastAPI dependency (None → default `InMemoryStore`; tests/E-04 override it). Routes validate against the SHARED models DIRECTLY (`registry.RegistrationPayload` / `sinks.IngestEnvelope`), so a malformed body → 422 and an extra key → 422 (the models are extra="forbid", K8): `POST /repos`→201 `{repo_id}`, `POST /ingest`→202 `{record_id}`, `GET /repos`→`list[RegisteredRepo]`, `GET /repos/{repo_id:path}/records`→`list[ReviewRecord]`. **`{repo_id:path}` converter** so an org/name `repo_id` (e.g. `acme/widget`) is captured whole, not split on `/`. **Unknown-repo policy (chosen + documented + tested): ingest never auto-registers** — registration is explicit (E-02); an `/ingest` (or records GET) for an unregistered `repo_id` is a loud 404 (K8), not a silent create. **pyproject:** added `[project.optional-dependencies] server` (core deps UNCHANGED) + the same 3 deps to `[dev]` so the gate runs the server tests; `cdx-server = custodex.server.app:main` console script (and documented `uvicorn ...:create_app --factory`); ruff `B008` per-file-ignore for `server/app.py` (FastAPI `Depends(...)` defaults, exactly like typer's in cli.py); mypy `ignore_missing_imports` override for `uvicorn` (no stubs; referenced only in the `# pragma: no cover` launch leaf — NOT a blanket `# type: ignore`); a TARGETED `filterwarnings` ignore for ONLY the Starlette `httpx`/`httpx2` `StarletteDeprecationWarning` (warnings NOT globally silenced). **test_server.py** gated with `pytest.importorskip("fastapi")` so the core suite SKIPS gracefully without the extra (`.venv` HAS fastapi → tests RUN here); covers register→ingest→read round-trip (byte-for-byte through the shared schema), `GET /repos` list, 404 unknown-repo ingest + records, 422 malformed/extra-key bodies, default-store path, in-place repeat-register, insertion-order records. Pinned the `server/` contract (`Store`/`RegisteredRepo`/`InMemoryStore`/`create_app`/routes/unknown-repo policy) in ARCHITECTURE.md BEFORE impl; added a "Central server" section to SPEC.md + README. K0/K4/K6/K8/K10/K9 upheld. **E-04 note:** swap `InMemoryStore`→a SQLAlchemy/Postgres store behind the SAME `Store` Protocol (offline tests on in-memory SQLite + a `pg` marker for real Postgres); `create_app(store=...)` + `app.dependency_overrides[get_store]` are the injection seams. **E-05/06 note:** `GET .../records` gains query filters (status/audience) and `/ingest` gains a per-repo bearer dependency (open now). |
| E-04 | SQLAlchemy DB store (Postgres-first) + Alembic — swaps `InMemoryStore` behind the SAME `Store` Protocol | DONE | **623 tests pass (was 608; +15 test_db), 2 deselected (live_llm + the new `pg` marker)**; ruff format --check + ruff check clean; mypy clean (34 source files, ZERO sqlalchemy `# type: ignore`); **`server/db.py` 100% coverage**, whole-pkg **98.12%** (≥90 gate met); **migration up/down PROVEN** two ways — (a) pytest `command.upgrade(cfg,"head")`/`downgrade(cfg,"base")` on a temp SQLite DB asserts the 4 tables appear then disappear, (b) CLI smoke `CDMON_DATABASE_URL=sqlite:///tmp.db alembic upgrade head` → `[coverage_snapshots, records, repos, resolutions]` then `downgrade base` → dropped; **`pg` DESELECTED by default** (test_db: 16 collected / 1 deselected; `-m pg` collects+SKIPs cleanly with no PG url); **core import clean** (`import custodex` pulls in NO sqlalchemy and NOT the server subpackage — `sys.modules` assert); dogfood `cdx check`/`cdx lint --config cdmon.yaml` both exit 0 (`server/`, `db.py`, `alembic/` NOT tracked → no reheal) | **Swaps E-03 `InMemoryStore` for `SqlStore(engine)` behind the SAME `Store` Protocol — `app.py`/routes UNTOUCHED (proven: the E-03 server contract re-runs against `create_app(SqlStore(sqlite_engine))` via TestClient, identical behavior incl. unknown-repo 404).** New `custodex/server/db.py` (SQLAlchemy 2.0 declarative): 4 tables `repos`/`records`/`resolutions`/`coverage_snapshots`. **"Indexed columns + full JSON" hybrid (K6 additivity):** each record/resolution row stores the FULL shared pydantic model in a portable JSON column (`_json_type()` = `JSON().with_variant(JSONB(), "postgresql")` → JSONB on PG, JSON on SQLite) — SOURCE OF TRUTH on read — PLUS indexed scalar columns (`repo_id`/`doc_id`/`verdict`/`drift_kind`/`audience`/`detected_at`/`source_sha`) for E-05 SQL filters; an ADDED schema field round-trips with NO migration (witnessed by the C-05 `source_sha` round-trip test). `SqlStore` implements `Store` (`add_repo` UPSERTs on `repo_id`; `list_repos`/`records_for` ORDER BY surrogate `id` = insertion, K10) + extras `add_resolution`/`resolutions_for`/`add_coverage_snapshot`/`coverage_snapshots_for` (endpoints land E-05). `engine_from_url(url)` gates in-memory SQLite onto `StaticPool`+`check_same_thread=False` (else each connection gets a FRESH empty DB → "no such table"); `create_all` (dev/tests) + Alembic (prod) both derive from the SAME `Base.metadata`. **`alembic/`** env reads `$CDMON_DATABASE_URL` (env var WINS over the `alembic.ini` placeholder; programmatic `set_main_option` is the test path) + `render_as_batch=True`; `0001_initial` mirrors the models 1:1 via the shared `_json_type()`. **pyproject:** `sqlalchemy>=2.0`+`alembic`+`psycopg[binary]` added to `[server]`+`[dev]` (core deps UNCHANGED, K0; SQLite needs no driver — psycopg only for real PG); `addopts` → `-m "not live_llm and not pg"`, `pg` marker registered. **`.gitlab-ci.yml`:** new opt-in `tests:pg` job (`postgres:16` service, `CDMON_DATABASE_URL=postgresql+psycopg://...`, `pytest -m pg`; manual/scheduled, mirrors `tests:live-llm`) — offline job stays SQLite-only. **test_db.py** gated `importorskip("sqlalchemy")`: SqlStore contract, FULL `ReviewRecord` JSON round-trip incl. `source_sha`, resolutions+coverage CRUD, cross-instance + on-disk persistence (proves DB not dict), Alembic up/down, the E-03 server re-run against SqlStore, the `pg`-marked PG contract (skipif no PG url). Pinned `db.py`/`SqlStore`/`alembic/` in ARCHITECTURE.md BEFORE impl; updated SPEC.md "Central server" with a DB+migrations subsection. K0/K4/K6/K9/K10 upheld. **E-05/06 note:** the indexed scalar columns are exactly the E-05 query-filter keys (filter in SQL, re-validate the JSON to the shared model); per-repo bearer auth (E-06) hangs off `/ingest` — the store is now durable. |
| E-05/06 | JSON query API (filtered records/resolutions/coverage/status) + per-repo bearer auth — **EPIC E COMPLETE** | DONE | **648 tests pass (was 623; +25: +20 test_server E-05/06, +9 test_db, minus refactors), 2 deselected (live_llm + `pg`)**; ruff format --check + ruff check clean; mypy clean (34 source files, ZERO `# type: ignore`); **`server/app.py` 99% (the only partial is the `isinstance(ratio,…)` false branch), `server/db.py` 100%, `server/store.py` 100%**, whole-pkg **98.16%** (≥90 gate met); **migration up/down PROVEN with the new column** — `command.upgrade(cfg,"head")` then assert `token_hash ∈ inspect(repos).columns`, `downgrade(cfg,"0001_initial")` then assert `token_hash ∉` columns (tables remain), `downgrade base` drops all; **single Alembic head** `0002_token_hash` (verified `alembic heads`); **`pg` DESELECTED by default** (23 collected / 1 deselected in test_db); **auth accept/reject PROVEN** — right Bearer→202, wrong→403, missing→401, unknown-repo→404, token-less repo open→202, re-register existing token-protected repo requires the token (401/403/201), `auth_token` NEVER returned on reads, reads need NO token; **core import clean** (`import custodex` pulls in NO fastapi/sqlalchemy — `sys.modules` assert); dogfood `cdx check`/`cdx lint --config cdmon.yaml` both exit 0 (`server/`, `registry.py` not tracked → no reheal) | **E-05 query API + E-06 per-repo bearer auth — the FINAL EPIC-E slices.** **E-05:** `GET /repos/{repo_id:path}/records?verdict=&drift_kind=&audience=&doc_id=&limit=&offset=` — filters map 1:1 to the E-04 INDEXED scalar columns (SQL `WHERE` on `SqlStore`; equivalent dict filtering on `InMemoryStore`), then rows are re-validated from the FULL JSON column (K6 source-of-truth on read); ORDER BY surrogate `id` = insertion (K10); `limit<=0`/`offset<0`→422 (FastAPI `Query(gt=0)`/`Query(ge=0)`). Plus `GET .../resolutions?record_id=` (`list[ResolutionRecord]`, scoped to the repo's record_ids), `GET .../coverage` (`list[dict]` snapshots), `GET .../status` → **`RepoStatus`** (frozen, extra=forbid): `{repo_id, total_records, by_verdict (all 3 verdict keys zero-filled), escalations, unresolved (records with no ResolutionRecord), last_detected_at (max), coverage_ratio (latest snapshot's "ratio")}`. **`RepoStatus` is the ONE allowed response DTO** — a COMPUTED AGGREGATE VIEW, not a parallel copy of a stored shared model, so K6's "no DTOs for the SHARED schema" does NOT apply (the record/resolution endpoints still return the SHARED `ReviewRecord`/`ResolutionRecord` directly); documented in ARCHITECTURE.md + SPEC.md. **Store Protocol grew** (both impls): filtered `records_for(repo_id, *, verdict/drift_kind/audience/doc_id/limit/offset)`, `resolutions_for_repo(repo_id, record_id=None)`, `coverage_for`, `repo_token_hash`; `InMemoryStore` gained dict-backed resolutions/coverage stores + `add_resolution`/`add_coverage_snapshot` helpers (parity with `SqlStore`'s E-04 extras). **E-06 auth model (CHOSEN + documented): client-provides-at-register, server-stores-the-HASH.** `RegistrationPayload` gained a WRITE-ONLY `auth_token: str|None=None` (ADDITIVE, K6; default None keeps pre-E-06 payloads valid); the server hashes it (one shared `store.hash_token` = sha256 hex) onto a NEW nullable `repos.token_hash` column (additive Alembic migration `0002`, `down_revision="0001_initial"`, up=`add_column`/down=`drop_column`) and NEVER stores or returns the plaintext — `SqlStore.add_repo` dumps the payload JSON with `exclude={"auth_token"}` and `InMemoryStore` keeps only the hash; `RegisteredRepo` never carries it (asserted: `auth_token ∉` any read). A re-register that carries a token ROTATES the hash; one that omits it leaves the existing hash. **`require_repo_token` (inline `_verify_token` dep on `POST /ingest` + re-register of an EXISTING repo):** unknown repo→404 (checked first), repo has a token_hash but `Authorization: Bearer` missing/not-Bearer→401, header present but sha256≠stored→403, match (or token_hash is None = repo registered open)→pass. **READS ARE OPEN** (no token on any `GET`) — documented for the dashboard. The client (`HttpSink`/`registry`) ALREADY sends `Authorization: Bearer <auth_env>` — header name/format match confirmed, no client change needed (only `register_repo` gained an `auth_token=` passthrough). Pinned the endpoints + `RepoStatus` + auth dependency + the token column/migration in ARCHITECTURE.md BEFORE impl; updated SPEC.md "Central server" table + query/auth prose. K4/K6/K8/K9/K10 upheld. **EPIC E COMPLETE.** **For EPIC F (React+Vite SPA):** consume these 4 read endpoints (records/resolutions/coverage/status) — all OPEN (no bearer needed for GETs); only `POST /ingest` + re-register need `Authorization: Bearer`. `RepoStatus` is the dashboard summary shape; `by_verdict` always has all 3 keys. |
| F-01 | dashboard scaffold (Vite+React+TS) + repos-status page — FIRST EPIC-F slice (FRONTEND-ONLY) | DONE | **FRONTEND GATE (Node v24.15.0 / npm 11.12.1, `cd dashboard`):** `npm install` exit 0 (committed `package-lock.json`, 194KB; `dashboard/node_modules` + `dashboard/dist` gitignored at both `dashboard/.gitignore` and repo root); **`npm run lint`** (ESLint flat config + typescript-eslint) → exit 0, clean; **`npm run test:run`** (Vitest 3.2.6 + @testing-library/react + jsdom) → **9 tests passed (2 files), NO network** (5 client unit tests w/ a fake `fetch`, 4 `Repos` component tests w/ a fake client: renders repos+status, loading state, error state, empty state); **`npm run build`** (`tsc -b && vite build`) → exit 0, **ZERO TS errors**, `dist/` produced (index.html + hashed JS/CSS, gzip JS 60KB). **PYTHON GATE GUARD (unchanged — no Python touched):** `.venv/bin/pytest` → **648 passed, 2 deselected**; `.venv/bin/cdx check --config cdmon.yaml` → "clean — no drift detected" exit 0 (dashboard is TS, NOT in cdmon.yaml; cdx scans `**/*.py` only). | **First EPIC-F slice: a standalone `dashboard/` Vite+React 19+TypeScript SPA that READS the OPEN EPIC-E endpoints.** Files: `dashboard/{package.json,package-lock.json,index.html,vite.config.ts,eslint.config.js,tsconfig*.json,.gitignore,README.md}` + `src/{types.ts,App.tsx,main.tsx,index.css,vite-env.d.ts}`, `src/api/client.ts`, `src/pages/Repos.tsx`, tests `src/api/client.test.ts` + `src/pages/Repos.test.tsx`, `src/test/{setup.ts,fixtures.ts}`. **API client contract (`src/api/client.ts`):** `class ApiClient({baseUrl?, fetchImpl?})` with `listRepos(): Promise<RegisteredRepo[]>` (GET `{base}/repos`) + `repoStatus(repoId): Promise<RepoStatus>` (GET `{base}/repos/{id}/status`); `fetchImpl` is INJECTABLE so tests pass a fake — NO MSW, NO sockets. `baseUrl` defaults to `import.meta.env.VITE_API_BASE ?? "/api"`; repo_id path segments are `encodeURIComponent`'d but **`/` is preserved** (server route is `{repo_id:path}`, so `acme/widget` → `/api/repos/acme/widget/status`); non-2xx → thrown `ApiError {status, url}`. **Types (`src/types.ts`)** hand-mirror the server: `RepoIdentity`, `RegisteredRepo`, `Verdict = "ok"|"review"|"escalate"`, `RepoStatus {repo_id,total_records,by_verdict,escalations,unresolved,last_detected_at?,coverage_ratio?}`. **`Repos.tsx`** fetches `listRepos()` then `repoStatus(id)` per repo (Promise.all), renders a table (repo id/name, total, ok/review/escalate, unresolved, coverage %) with explicit loading (`role=status`) / error (`role=alert`) / empty states; the client is an injectable PROP (`api?: ReposApi`, default = singleton) so component tests inject a fake. **`vite.config.ts`** dev `server.proxy` maps `/api`→`http://127.0.0.1:8000` (strips `/api`) so `npm run dev` + `uvicorn custodex.server.app:app` compose; Vitest config (jsdom, globals, `src/test/setup.ts` for jest-dom matchers + cleanup). Pinned the "Dashboard (EPIC F)" section in ARCHITECTURE.md BEFORE building. **GOTCHAS (see LESSON_LEARNT):** (1) TS 5.7.3 does NOT know `erasableSyntaxOnly` (that's TS 5.8+) — removed it from both tsconfigs (the newest `react-ts` template assumes 5.8). (2) Vitest 2.x ships a NESTED `vite` copy → `tsc -b` fails with duplicate-vite type-identity errors on the `test:` key in `vite.config.ts`; **bumped Vitest to 3.2.6** (which uses the top-level vite 6) and imported `defineConfig` from `vitest/config` → build clean. **For F-02/03:** REUSE this `ApiClient` + `src/types.ts` (add `recordsFor(repoId, filters)`/`resolutionsFor`/`coverageFor` methods over the same OPEN GETs); the injectable-fake-client test pattern (no MSW) works for every page; generate `ReviewRecord`/`ResolutionRecord` TS from `cdx schema` rather than hand-writing. Introduce a real router (react-router) when the 2nd route lands — `App.tsx` is a single-route stub now. |
| F-02/03 | drift-timeline + coverage views in the React dashboard (routing + 2 read views) — FRONTEND-ONLY | DONE | **FRONTEND GATE (Node v24.15.0 / npm 11.12.1, `cd dashboard`):** `npm install` exit 0 (added `react-router-dom@7.17.0`, `package-lock.json` updated); **`npm run lint`** (ESLint flat config + typescript-eslint) → exit 0, clean; **`npm run test:run`** (Vitest 3.2.6 + @testing-library/react + jsdom) → **26 tests passed (5 files), NO network** (was 9; +17: 4 new client tests for records/resolutions/coverage URL+query building, 7 `RepoDetail` (timeline rows, resolution badge join, filter re-query w/ captured args ×2, loading/error/empty), 4 `Coverage` (% + 3 baskets, empty, loading, error), 2 `App` routing (click repo→detail, click coverage→coverage view via stubbed global fetch); existing Repos tests rewrapped in `MemoryRouter`); **`npm run build`** (`tsc -b && vite build`) → exit 0, **ZERO TS errors**, `dist/` produced (gzip JS 74KB). **PYTHON GATE GUARD (unchanged — only `cdx schema` run, which writes a JSON file under `dashboard/`):** `.venv/bin/pytest` → **648 passed, 2 deselected**; `.venv/bin/cdx check --config cdmon.yaml` → exit 0 (no drift; dashboard is TS, not scanned). | **Two read views over the OPEN E-05 endpoints + routing.** New files: `dashboard/src/pages/{RepoDetail,Coverage,RepoRoute}.tsx` + their `.test.tsx`, `src/hooks/useApi.ts`, `src/routing.ts`, `src/schema.review.json` (generated), `src/App.routing.test.tsx`. Changed: `src/App.tsx` (router), `src/main.tsx` (`<BrowserRouter>`), `src/pages/Repos.tsx` (`<Link>` per repo + coverage link), `src/api/client.ts` (3 methods + lazy global fetch default), `src/types.ts`, `src/test/fixtures.ts`, `src/pages/Repos.test.tsx` (MemoryRouter), `package.json`/`package-lock.json`. **Routing:** `react-router-dom` ^7; `/` Repos, splat `/repos/*`→`RepoRoute` which reconstructs a slash-bearing repo id from the tail and dispatches to `Coverage` (`…/coverage` suffix) or `RepoDetail`. `src/routing.ts` has `linkToRepo`/`linkToCoverage` (segment-encoded, slash-preserving). **Client:** `recordsFor(repoId, filters?: {verdict,drift_kind,audience,doc_id,limit,offset})`/`resolutionsFor`/`coverageFor` over the OPEN GETs; filters→`URLSearchParams` dropping `undefined`/`""` (so `offset:0` is sent). Default client now resolves `globalThis.fetch` LAZILY so `vi.stubGlobal('fetch',…)` reaches the singleton in the App routing test. **Types:** `RecordVerdict`(`FIX|INVALIDATE|ESCALATE`)/`Audience`/`ProposedFix`/`ReviewRecord` GENERATED from `.venv/bin/cdx schema --out dashboard/src/schema.review.json` (schema.py::ReviewRecord, K6 source of truth) — interfaces mirror its `properties`. `cdx schema` emits ONLY the review record, so `Resolution`/`ResolutionRecord` hand-written from schema.py and `CoverageSnapshot` from the server's OPAQUE coverage dict (`store.coverage_for→list[dict]`; only `ratio` contractual, app.py reads it; baskets optional/defensive) — noted honestly in types.ts. **Pages:** `RepoDetail` controlled filter selects/input feed a `useApi` dep → re-query; rows = doc/drift_kind/verdict/detected_at/source_sha + resolution badge (resolutions joined by `record_id`, else `unresolved`). `Coverage` = latest snapshot `ratio`% + documented/undocumented/waived baskets; empty→"No coverage reported yet". Shared `useApi(loader, deps)` hook (loading/error/ready, cancels stale loads). **GOTCHAS (see LESSON):** (1) react-router `:param` can't capture a slash, so a slash-bearing repo id (`org/name`) needs a splat route + a dispatcher, not `:repoId`/`:repoId/coverage`. (2) the shared `apiClient` singleton captured `fetch` at construction → `vi.stubGlobal` didn't reach it; fixed by defaulting to a lazy `(...a)=>globalThis.fetch(...a)`. (3) `user-event` is NOT installed — used `fireEvent.change` for filter changes. **For F-04/05:** see report — F-04 resolve = a NEW server mutation endpoint (POST `…/resolutions`) needs WRITE auth (E-06 bearer) + a UI form posting a `ResolutionRecord`; F-05 health = a NEW server metrics endpoint + a chart. || F-04/05 | resolve action (full-stack write path) + health metrics overview — **EPIC F COMPLETE** (SPANS server + frontend) | DONE | **PYTHON GATE:** `.venv/bin/ruff format --check .` + `ruff check .` clean; `.venv/bin/mypy custodex` clean (34 source files, ZERO `# type: ignore`); **`.venv/bin/pytest -q --cov=custodex --cov-branch` → 658 passed, 2 deselected (live_llm + `pg`)** (was 648; +10 test_server: 6 resolve [persist+reflect, 404 unknown repo, 404 unknown record, auth 403/401/202 matrix, 422 malformed, open-when-no-token], 4 health [404 unknown, zeroed empty, exact arithmetic mttr=90.0/rate=0.25, mttr-null]); **`server/app.py` 99%** (only partial is the pre-existing `_compute_status` `69->71` branch), **`server/store.py` 100%**, whole-pkg **98.19%** (≥90 gate met); `cdx check --config cdmon.yaml` exit 0 (no reheal — `server/` not tracked). **FRONTEND GATE (Node v24.15.0 / npm 11.12.1, `cd dashboard`):** `npm install` exit 0 (no new deps); **`npm run lint`** → exit 0 clean; **`npm run test:run`** → **38 tests passed (6 files), NO network** (was 26; +12: 4 new client [GET /health, POST resolve w/ Bearer+JSON-body capture, 403 ApiError, +existing], 4 `RepoDetail` resolve [POST captures resolution/note/token, success badge reflect, missing-token friendly validation blocks POST, error surfaced], 4 `Health` [stat cards, null-MTTR em-dash, loading, error], 1 `App` routing [click health link→Health view]); **`npm run build`** (`tsc -b && vite build`) → exit 0, **ZERO TS errors**, gzip JS 75KB. | **Final EPIC-F slices — the FIRST dashboard WRITE path + a metrics overview, SPANNING server + frontend.** **Server:** `POST /repos/{repo_id:path}/resolutions` (body = the SHARED `ResolutionRecord`, NOT a DTO — K6) token-protected via the SAME `_verify_token` as `/ingest` (404 unknown repo / 401 missing bearer / 403 wrong bearer / 404 if `record_id` is not a record of this repo — loud K8) → `store.add_resolution(rec)` → 202 `{record_id}`. `GET /repos/{repo_id:path}/health` → **`RepoHealth`** (frozen, extra=forbid, a COMPUTED VIEW like `RepoStatus` — K6's no-DTO rule does NOT apply): `{repo_id, total, escalations, escalation_rate (=escalations/total, 0.0 when total==0), unresolved, overrides (resolutions with resolution=="overridden"), resolved (distinct record_ids with a resolution), mttr_seconds (mean resolved_at−detected_at over resolved records; null if none)}`. `_compute_health` reads via existing `records_for`/`resolutions_for_repo`; `_parse_iso` handles a trailing `Z`; first-resolution-per-record wins the MTTR delta (insertion order, K10). `add_resolution` PROMOTED onto the `Store` Protocol (both stores already implemented it). Files: `custodex/server/app.py` (RepoHealth + `_compute_health` + `_parse_iso` + 2 routes), `server/store.py` (Protocol gains `add_resolution`), `tests/test_server.py` (+10). **Frontend:** `ApiClient.resolve(repoId, rec, token)` (POST + `Authorization: Bearer` via a new `postJson` helper, returns `{record_id}`) + `healthFor(repoId)` (OPEN GET). `RepoDetail` gains a single Token input + a per-unresolved-row `<ResolveControl>` (resolution `<select>` + optional note + Resolve button) → builds a `ResolutionRecord` (`resolved_at` injected via `new Date().toISOString()` at the page edge, NOT in pure code) → `api.resolve` → optimistic badge; a MISSING token is a friendly inline `role=alert` ("A token is required…") and NEVER POSTs. New `src/pages/Health.tsx` renders `RepoHealth` as stat cards (escalation rate %, MTTR humanised s/m/h/d, "—" when null). `routing.ts` gains `linkToHealth`; `RepoRoute` dispatches a `…/health` suffix → `Health`; `Repos.tsx` adds a health `<Link>`. New type `RepoHealth` in `types.ts`. Files: `dashboard/src/{api/client.ts,types.ts,routing.ts,pages/RepoDetail.tsx,pages/RepoRoute.tsx,pages/Repos.tsx,test/fixtures.ts}` + NEW `pages/Health.tsx`; tests `api/client.test.ts`, `pages/RepoDetail.test.tsx`, NEW `pages/Health.test.tsx`, `App.routing.test.tsx`. Pinned the 2 endpoints + `RepoHealth` shape + client methods in ARCHITECTURE.md BEFORE impl (server "Central server" + "Dashboard" sections). K4/K6/K8/K10 upheld. **EPIC F COMPLETE.** **For EPIC G (deployability):** the dashboard is a static `dist/` (Vite build) over the OPEN GETs + the one Bearer-protected write — `cdx init --central <url>` should emit a `cdmon.yaml` HttpSink + the `VITE_API_BASE`; package the SPA build + a uvicorn entrypoint; an external example repo should register WITH an `auth_token` to exercise the write path. |
| G-01/02 | `cdx init --central <url>` (HTTP-reporting bootstrap) + `cdx doctor` (offline preflight) — FIRST EPIC-G slices | DONE | **PYTHON GATE:** `.venv/bin/ruff format --check .` + `ruff check .` clean; `.venv/bin/mypy custodex` clean (36 source files, ZERO `# type: ignore`); **`.venv/bin/pytest -q --cov=custodex --cov-branch` → 684 passed, 2 deselected (live_llm + `pg`)** (was 658; +26: +18 test_doctor [run_checks unit: Check model frozen/typed, deterministic order, good-config no-FAIL, missing-doc→PASS, missing-code-ref→WARN, central file PASS/FAIL, http no-url/no-repo-id→FAIL, token unset→WARN/present→PASS, backend api key absent→WARN/present→PASS, claude-code CLI absent→WARN/present→PASS, agent-extra present→PASS/absent→WARN; CLI: good→exit0, http-no-url→exit1, malformed→exit1 stderr] + 8 test_cli_init [init without --central BYTE-IDENTICAL to CONFIG_TEMPLATE, --central round-trips + make_sink builds HttpSink, --token-env/--repo-url, --repo-id defaults to cwd name, both audiences carried]); **`doctor.py` 99%** (only partial is the `_check_documents` inner-loop `64->63` exhaust branch), whole-pkg **98.15%** (≥90 gate met). **DOGFOOD:** editing `config.py` (tracked by the `foundation` doc) drifted `docs/api/foundation.md` → rehealed via `cdx monitor --apply --config cdmon.yaml` (HASH+REGION FIX applied); `cdx check` + `cdx lint --config cdmon.yaml` both exit 0 (`cli.py`/`doctor.py` themselves NOT tracked). **SMOKE (tmp dir):** `cdx init --central http://localhost:8000 --repo-id demo/x` wrote a `sink: http` block (url/repo_id/auth_env=CDMON_CENTRAL_TOKEN/outbox); `cdx doctor --config cdmon.yaml` →<br>`PASS  config — config loaded and validated`<br>`WARN  documents — code ref file(s) not found under root: src/myproject/api.py, …/cli.py, …/constants.py, …/core.py`<br>`PASS  backend — mock backend (deterministic, offline) — always runnable`<br>`WARN  central — central http -> http://localhost:8000 (repo demo/x); token env $CDMON_CENTRAL_TOKEN is unset (records send unauthenticated)`<br>exit 0 (template's placeholder code refs + the unset token are WARN, never FAIL). | **First EPIC-G (deployability) slices: make cdx DROPPABLE into any repo.** **G-01 — `cdx init --central URL [--repo-id ID] [--token-env VAR] [--repo-url URL]`:** WITHOUT `--central` the offline starter template is written BYTE-IDENTICAL to today's `CONFIG_TEMPLATE` (existing init tests stay green — additive, K9). WITH `--central` a new pure `config.central_config_template(*, url, repo_id, token_env="CDMON_CENTRAL_TOKEN", repo_url=None)` swaps the template's offline `central:` block for an HTTP-reporting one (`sink: http` + url + repo_id + auth_env + outbox `.cdmon/outbox.jsonl`) that ROUND-TRIPS through `load_config` AND satisfies `make_sink`'s http reqs (repo_id present → builds a real `HttpSink`). `--repo-id` defaults to the cwd dir name (`Path.cwd().name`; loud K8 if empty). `write_template(path, content=None)` grew an optional `content` arg so the offline path is unchanged. **G-02 — `cdx doctor [--config]` + new `custodex/doctor.py`:** `run_checks(config, config_dir) -> list[Check]` is pure-ish (reads `os.environ`/`shutil.which`/`importlib.util.find_spec` only; NO network, K1/K4) and DETERMINISTIC (K10). `Check` = frozen pydantic `{name, status: CheckStatus(PASS|WARN|FAIL), detail}`. Fixed order: **config** (always PASS — the CLI already loaded it), **documents** (missing doc FILE → PASS, it's heal-creatable, NOT a failure; a missing CODE REF file → WARN), **backend** (mock→PASS; claude-code→`claude` on $PATH else WARN; api→`$ANTHROPIC_API_KEY` set else WARN; agent→PASS + a separate check), **central** (none/file→PASS, file w/o path→FAIL; http mirrors `make_sink`'s loud K8 reqs as FAIL on missing url/repo_id, plus WARN when `auth_env` is set but that env var is unset = token-presence), and for the agent backend an **agent-extra** check (langgraph importable→PASS else WARN). The CLI loads the config (a malformed one is a loud K8 error → exit 1 BEFORE checks), prints `STATUS  name — detail` per check, exits 0 unless any FAIL. **GRADING PHILOSOPHY (the G-02 lesson):** a merely-ABSENT runtime prereq (no CLI, no key, unset token, optional extra not installed) is WARN — the config is valid, this machine just can't RUN that path; only a STRUCTURALLY-broken config is FAIL. A `--ping` connectivity probe (injected transport) is explicitly OUT of scope — default doctor is OFFLINE. Pinned the `init` flags + `central_config_template` + the `Check`/`run_checks` contract + the `doctor.py` module in ARCHITECTURE.md BEFORE impl; updated SPEC.md CLI table (init row + new doctor row) + README. K0/K1/K4/K8/K9/K10 upheld. **For G-03/04:** `cdx init --central` is the adopter bootstrap; `cdx doctor` is the CI preflight (exit 0 unless FAIL → use it as a CI gate step). The CI templates (G-03) should run `cdx doctor` then `cdx check`/`cdx lint`; the external example repo (G-04) should `cdx init --central <TestClient url> --repo-id <id>`, then `cdx register` (E-02; the server needs an `auth_token` for the E-06 write path), then report via the HttpSink and assert the records land on a TestClient server. NOTE: `--token-env` only NAMES the env var — the adopter still exports the token; doctor WARNs (not FAILs) when it's unset so a CI job without the secret still passes preflight. |
| G-03/04 | adopter CI templates (`templates/ci/`) + external example repo proven end-to-end (`examples/external-repo/`) — **FINAL EPIC-G slices, EPIC G COMPLETE** | DONE | **GATE:** `.venv/bin/ruff format --check .` → 78 files already formatted; `.venv/bin/ruff check .` → All checks passed; `.venv/bin/mypy custodex` → clean (35 source files); **`.venv/bin/pytest -q --cov=custodex --cov-branch` → 693 passed, 2 deselected (live_llm + `pg`)** (was 684; +9: 6 test_ci_templates [both YAML safe_load to a mapping; templates reference ONLY real cdx subcommands — parsed from the actual `script`/`run` command lines, NOT YAML comments; gate job runs doctor+check+lint; docs-PR job guards with should-sync + heals with monitor + opens open-docs-pr; gitlab has both jobs; github has both jobs under `jobs:`] + 3 test_example_external [committed fixture in-sync; full adopter loop heals→reports→queries with the RIGHT bearer token (records land server-side, round-trip the shared schema, outbox drains empty); WRONG token rejected server-side (every ingest 403'd → records queue to the outbox, nothing lands; a raw ingest of the queued envelope is 403 w/ the wrong token and 202 w/ the right one)]); whole-pkg **98.18%** (≥90 gate met). **DOGFOOD:** NO tracked-module change (templates + example fixture + tests only) — `cdx check --config cdmon.yaml` + `cdx lint --config cdmon.yaml` both exit 0 unchanged; `cdx coverage --config cdmon.yaml` unaffected (still 79 engine files — `examples/external-repo/src/widget.py` is a DIFFERENT tree, not scanned); pytest does NOT collect the example (testpaths=["tests"]). **E2E PROOF:** the external repo registered (with an `auth_token`) + its 1 healed REGION-fix record landed on the in-process TestClient central server (`GET /repos` shows `acme/widget`; `GET /repos/acme%2Fwidget/records` returns the healed record, re-validating to the SHARED `ReviewRecord`); a WRONG bearer token is rejected 403 server-side so nothing lands and the record queues to the HttpSink outbox. All offline (K4, no socket). | **Final EPIC-G slices — adopter CI + the capstone external e2e.** **G-03 — `templates/ci/`:** `gitlab-ci.adopter.yml` + `github-actions.adopter.yml`, each shipping a `cdmon-gate` job (`cdx doctor` → `check` → `lint`, on MR/PR + branch/push) and a default-branch `cdmon-docs-pr` job (`cdx register` → a `should-sync` loop-guard piping `git diff --name-only` → `cdx monitor --apply --ref` → `cdx open-docs-pr --ref`), commented for the `CDMON_CENTRAL_TOKEN` CI secret. `templates/ci/README.md` documents adoption in 3 steps + that the token is a protected/masked secret. **Template-honesty test** (`tests/test_ci_templates.py`): parses the YAML, collects the ACTUAL command lines (GitLab `script` lists + the `&cdmon-setup` anchor; GitHub `jobs.<id>.steps[].run`), strips trailing `#` comments, and asserts every `cdx <token>` is a real command from `typer.main.get_command(app).commands.keys()` — so renaming/removing a command (verified: `check`→`verify` reds the test) breaks the template test, keeping the templates honest. **G-04 — `examples/external-repo/`:** a self-contained adopter — `src/widget.py` (2 public fns + a constant) + `docs/api.md` (eng-guide, managed `symbols` region, committed IN-SYNC via `cdx new-doc` + a real purpose line) + `cdmon.yaml` (http `central:` block, repo_id `acme/widget`, auth_env `CDMON_CENTRAL_TOKEN`) + a README. `tests/test_example_external.py` copies it under tmp_path, mutates `widget.py` (so `check` reports drift), heals via `monitor --apply`, then registers the repo + reports the healed records to an in-process FastAPI **TestClient** central server. **HttpSink→TestClient wiring is in the TEST, not the package** (K0): a `_TestClientPostClient` adapts the injected `post(url,*,data,headers)` shape onto `TestClient.post(content=…)` and RAISES on ≥400 (so the sink's K4 outbox path fires on a rejected token); a `_TestClientRegisterHttp` adapts the register transport's `_RegisterHttp.request` leaf, wrapped in a real `HttpRegisterTransport("http://central", http=…)` (TestClient resolves the URL by path). Bearer token via `monkeypatch.setenv(auth_env, token)` (E-06). Asserts repo+records land with the right token; the WRONG token is 403'd server-side. **GOTCHA (see LESSON):** a raw `TestClient.post(content=bytes)` without a `Content-Type: application/json` header is a 422 (FastAPI won't parse the body), masking the 403 — the sink sets it via `_headers()`, but a hand-rolled raw post must add it. **EPIC G COMPLETE.** **For EPIC H:** the engine's OWN new modules (cli/doctor/sinks/registry/server/inventory/coverage) are still UNDOCUMENTED in the dogfood config — `cdx coverage --config cdmon.yaml` reports ~12% public-symbol coverage (135/1106), so H-02 (self-dogfood expansion) has a large, real target; the template-honesty test is itself a tiny self-improvement guard worth generalizing (surface CLI/contract drift as a dry-run issue). |
| H-02 | self-dogfood: document the engine's own new modules + lock a coverage CI gate | DONE | **Doc self-coverage 42.2%→100.0% public symbols** (engine-scoped: 316/316 documented, 0 gaps, 4 waived). Before this slice the scan was unscoped (12.2% over a 1106-symbol universe that included tests/examples/alembic/dashboard); scoped to `custodex/**/*.py` (the actual engine surface) it baselined at 42.2% (135/320). 8 new eng-guide docs added, all 23 undocumented engine modules now owned. `cdx coverage --fail-under 95` exits 0; `cdx check`/`cdx lint` exit 0; ruff fmt+check clean; mypy clean (35 files); full suite green (459→461 tests: +2 dogfood threshold/waiver tests); whole-pkg test-coverage 98.18% (≥90 gate, unchanged — DOC coverage is a different metric). `.gitlab-ci.yml` informational coverage step flipped to the hard `--fail-under 95` gate. | **Self-dogfood mechanics.** Scope FIRST: the coverage scanner defaults to `**/*.py`, which pulled in `tests/`, `examples/`, `alembic/`, and `dashboard/node_modules/` — inflating the denominator to 1106 and burying the real signal. Adding `coverage.include: ["custodex/**/*.py"]` scopes the metric to the engine's own documentable surface (NOT a waiver — those non-engine trees are out-of-scope, not justified gaps). Then it was purely mechanical: `new-doc <id>` scaffolds a conformant doc (TODO purpose + generated symbol table) → add the `DocumentSpec` + `code_refs` to `cdmon.yaml` → edit ONLY the `>` purpose line → `monitor --apply` heals fingerprints AND auto-fills the `api-index` landing page (the `source: index` region regenerates from the documents list — no manual index edit). 8 docs (coverage-system, layout-build, pr-loop, central-client, learning, server, agent-workflow, ops) cover all 23 modules. **Waived (4 symbols, 3 files):** the package `__init__.py` re-export aggregators (`__all__`/`__version__`) — every symbol they re-export is documented in its HOME module, so the aggregator itself is not a documentable surface; each waiver carries a reason (asserted by a new test). Durability: `tests/test_dogfood.py::test_dogfood_self_coverage_meets_committed_threshold` resolves the real report and asserts `percent_public_symbols >= 95`, mirroring the CI gate so a new undocumented engine symbol fails BOTH. |
| H-01/04 | server `GET /repos/{id}/telemetry` (underperformer view) + `cdx surface-gaps` (coverage gaps → tracker issue, injected transport) | DONE | **GATE:** `.venv/bin/ruff format --check .` + `ruff check .` clean; `.venv/bin/mypy custodex` clean (36 files, ZERO `# type: ignore`); **`.venv/bin/pytest -q --cov=custodex --cov-branch` → 722 passed, 2 deselected (live_llm + `pg`)** (was 693; +~29: H-01 telemetry [404, empty, exact rates + worst-first ordering, promotion candidates, SqlStore parity] in test_server.py; H-04 plan/dry-run/no-op/frozen + GitLab+GitHub from_env (3 each) + injected-POST + lazy-leaf in new test_issues.py; 5 `surface-gaps` CLI tests in test_cli.py). **NEW-CODE COVERAGE:** `issues.py` 98% (only the two `# pragma: no cover` urllib leaves + a label branch), `server/app.py` 99%, whole-pkg **98.20%** (≥90 gate). **DOGFOOD:** edited `server/app.py` (server doc) + `cli.py` (ops doc) + ADDED `issues.py` → added it to `cdmon.yaml`'s `pr-loop` doc `code_refs`; `cdx monitor --apply --config cdmon.yaml` rehealed (pr-loop/server/ops HASH+REGION FIX); `cdx check` + `cdx lint` + `cdx coverage --config cdmon.yaml --fail-under 95` ALL exit 0 (doc self-coverage stays 100% — issues.py now owned). **SMOKE:** `cdx surface-gaps --dry-run --config cdmon.yaml` → "no coverage gaps — nothing to surface" exit 0 (the dogfood has 0 gaps post-H-02). | **Telemetry view math + issue transport.** **H-01:** `RepoTelemetry`/`ShapeStat` are COMPUTED views (like `RepoStatus`/`RepoHealth`, not stored shared models — K6's no-DTO rule doesn't apply), built in `app.py::_compute_telemetry` from `records_for` + `resolutions_for_repo` (NO new Store method — both stores already expose the reads). Shape key = `(drift_kind, audience)` (coarser/cross-doc, vs promotion's `(doc_id,drift_kind,audience)`). `override_rate` = OVERRIDDEN resolutions / shape count, FIRST-resolution-per-record wins (insertion order, mirrors `_compute_health`); `escalation_rate` = ESCALATE / count. Worst-first ordering (K10) = `(-escalation_rate, -override_rate, drift_kind, audience)`. `promotion_candidates` REUSES the pure `detect_promotions` server-side. SqlStore works unchanged (the view only calls the two Protocol reads). **H-04:** new `issues.py` mirrors `pr.py` 1:1 — INJECTED `IssueTransport`, `plan_coverage_issue(report, suggestions)->IssuePlan|None` (None on no gaps; deterministic body grouping every undocumented public symbol under its A-07 suggested owner, owners sorted), `open_coverage_issue(...,transport,dry_run)`. GitLab (PRIVATE-TOKEN, `/projects/<id>/issues`) + GitHub (Bearer, `/repos/<repo>/issues`) transports each have a `from_env` (loud `TransportError` K8) and a stdlib-urllib leaf that is the ONLY `# pragma: no cover` line; lazy-build + missing-env covered with the real POST stubbed. `cdx surface-gaps [--config] [--dry-run] [--provider gitlab|github]` reuses the coverage CLI's discover→resolve→suggest, a `_NullTransport` for dry-run (no env needed), loud K8 on a bad provider. **LESSON (document a NEW engine module):** adding `issues.py` WITHOUT adding it to `cdmon.yaml` would have failed the `--fail-under 95` self-gate — a new engine module is itself a doc gap; the fix is one `code_refs` line in a fitting doc (`pr-loop`, the PR/issue-transport home) + a reheal. |

| P-01 | Extractor protocol + flag-gated body-AST fingerprint tier | DONE | full suite green (97.81% branch cov); 21 new tests (test_extract registry+golden+body+gating, test_drift body-tier integration, test_system e2e); ruff fmt+check clean on touched files; mypy clean (45 files); dogfood re-healed (pipeline/remediation/layout-build) + `cdx check`/`lint` exit 0 | **(a)** new `Extractor` Protocol + language-keyed registry in `extract.py` (`PythonAstExtractor` default, `register_extractor`/`get_extractor`); `extract_file` is now a thin wrapper over `get_extractor("python")` — pure refactor, golden `surface_hash` literals frozen (`user-guide 9e9171f520832497`, `eng-guide f688a396c2e8be27`). **(b)** opt-in body-AST tier: `Symbol.body_hash` (`ast.dump(include_attributes=False)`, docstring stripped → insensitive to comments/formatting), folded into `surface_hash(*, include_body=...)` as an ADDITIVE key (byte-invisible when OFF; NEVER applied to user-guide, K3). Flag = `MonitorConfig.fingerprint_body_tier` (default False; mirrored on `IndexFile` + lifted in `load_bundle`); threaded one-shared-truth through drift/heal/layout/monitor/cli + `FixRequest.fingerprint_body_tier`. No `schema_version` bump (wire shape unchanged; deferred to P-02); `config_snapshot` self-describes the tier when ON. |
| P-02 | Tiered `SurfaceFingerprint` + which-tier-moved reporting + `schema_version` 1.1.0 | DONE | full suite green (**1354 passed, 3 deselected, 97.44% branch cov**); new tests (test_extract fingerprint/tiers/drifted_against, test_manifest tiers round-trip+additive, test_drift which-tier-moved+fallback, test_schema 1.1.0+drifted_tiers back-compat, test_monitor record drifted_tiers, test_system e2e stamp+report); ruff fmt+check clean on touched files; mypy clean (45 files); dogfood re-healed (pipeline) + `cdx check`/`lint` exit 0 | **Structure:** `SurfaceFingerprint(signature, docstring, body, composite)` (frozen) + `DocumentSurface.fingerprint(*, include_body)`; `composite` IS the unchanged `surface_hash()` (golden literals still hold → NO re-baseline, every stored `cdm.fingerprint` valid), `surface_hash` delegates to it. Per-tier digests are diagnostic: **signature** folds in `records`; **docstring** eng-only; **body** opt-in (P-01 flag), `None` for user-guide (K3). **Storage:** additive `cdm.fingerprint_tiers` via `manifest.set_fingerprint_tiers`/`stored_fingerprint_tiers` (composite stays in `cdm.fingerprint`); heal+layout stamp BOTH from one `fingerprint()` call (one-shared-truth). **Reporting:** `drift.detect` sets `Drift.drifted_tiers` (which tier moved; `()` + composite-only fallback for an old doc with no stored tiers). **Schema (the P-01-deferred bump):** `ReviewRecord.schema_version` → `1.1.0` + additive `drifted_tiers` (appended last; old `1.0.0` lines still parse); `monitor` threads it onto every record. |
| P-03 | Registry-routed symbol extraction by `CodeRef.lang` (proves K0) | DONE | full suite green (cov ≥90% branch); 6 new test_extract tests (explicit-lang route, auto-suffix infer, suffix-map, unknown→ExtractionError, python-still-default back-compat); ruff fmt+check clean on touched files; mypy clean (45 files); dogfood re-healed (pipeline) + `cdx check`/`lint` exit 0 | `_symbols_for_ref` now resolves `get_extractor(_symbol_language(ref)).extract(...)` — the hardcoded `extract_file` (Python) path is GONE. `_symbol_language(ref)` = explicit `ref.lang`, else `auto` → `_SYMBOL_LANG_BY_SUFFIX.get(suffix, "python")` (pre-P3 behaviour for `.py`/auto preserved; golden composites unchanged). `register_extractor(ext, *, suffixes=())` self-maps `suffix → language` so a registered language works for `lang: auto`. `CodeRef.lang` opened `Literal[...]` → `str` (back-compat loosening: all old values still valid; unknown language now loud at extraction via `get_extractor`, K8, not config-load). **K0 proof:** a test registers a non-Python stub extractor and `build_document_surface` returns its symbols for both an explicit `lang` ref and an `auto` ref resolved by suffix — ZERO engine edit; a real heavy parser (tree-sitter/regex) is a follow-on registration behind an optional extra. |
| P-04 | Anchors — region ⇄ stable symbol identity (**EPIC P COMPLETE**) | DONE | full suite green (cov ≥90% branch); new tests (test_extract anchor_id determinism/rename/move-survival/Symbol.anchor_id, test_manifest region_anchors round-trip+additive, test_drift body-stable/added/removed/pre-P4-fallback, test_system stamp+classify e2e); ruff fmt+check clean on touched files; mypy clean (45 files); dogfood re-healed (pipeline) + `cdx check`/`lint` exit 0 | `extract.anchor_id(name)` = sha256[:16] of the QUALIFIED NAME (never lineno) + `Symbol.anchor_id` property → identity stable across a pure code move, changed by a rename. heal+layout stamp `cdm.region_anchors[id]` (sorted, additive — preserves fingerprint/tiers/region_hashes) for the symbol-table region (`blocks.REGION_KEYS`), one-shared-truth with the fingerprint stamp, independent of B-03 modes. `drift.detect` adds `Drift.anchors_added`/`anchors_removed`: diffs the current surface's anchor set against the stamped set on a HASH drift → EMPTY delta = same identities (move/reorder or internal change → re-bind; pair with P2 `drifted_tiers` to see "body changed"), nonempty = symbol added/removed/renamed (a rename reads as removed+added — the honest, lossless signal). Pre-P4 docs (no stored anchors) classify with an empty delta (back-compat). Out of scope (follow-on): `lines`-ref re-bind by identity, rename tracking, anchors on `ReviewRecord`. |
| P-05 | First REAL non-Python extractor — `ShellExtractor` (sh/bash) behind the P-03 seam | DONE | full suite green (**1390 passed, 3 deselected, 97.48% branch cov**; +19 over P-04); 18 new test_extract shell tests + 1 test_system e2e; ruff fmt+check clean on touched files (extract.py/test_extract.py/test_system.py), mypy clean (45 files); new shell lines fully covered (unbalanced-brace fallback, no-space comment, line-1 header, OSError read all asserted); dogfood re-healed (pipeline — new public `ShellExtractor`) + `cdx check`/`lint`/`coverage --fail-under 95` all exit 0 (self-coverage 100%, 503/503) | The registry's FIRST production non-Python extractor: `ShellExtractor` (`language="shell"`) parses sh/bash function defs via the stdlib `re` module ONLY — no heavy dep, core dep surface unchanged (K0), offline gate intact (K4). Recognizes `name() {…}` (POSIX) and `function name {…}`/`function name() {…}` (bash); each → `Symbol(kind="function", signature=f"{name}()", is_public=leaf-name rule, docstring=leading `#` comment block (shebang excluded), lineno/end_lineno via brace-depth scan, arg_names=(), body_hash=None)`. Pure & import-free — the script is read as text, never sourced (K1/K0). **K0 proof:** registered by DEFAULT (`register_extractor(ShellExtractor(), suffixes=(".sh", ".bash"))`) — a `.sh`/`.bash` ref with `lang: shell` OR `lang: auto` resolves it with ZERO engine edit (`_symbol_language`/`_symbols_for_ref`/`build_document_surface` untouched). **K3 proven e2e:** eng-guide folds the leading-comment docstring in (editing it moves the eng hash); a user-guide drops `_`-prefixed helpers AND excludes docstrings (same edit → no move); a body comment is neither signature nor docstring → no move. Out of scope (follow-on registrations behind the now-proven seam): a shell body tier (`body_hash` stays None — body tier is Python-AST-only), here-doc/quoted-brace exactness, tcl + real tree-sitter, `lines`-ref anchor re-bind. |
| R-01 | EPIC R kickoff — golden feature-catalog schema + loader (`featurecatalog.py`) | DONE | full suite green (cov ≥90% branch, 97.44%); 19 new tests in test_featurecatalog (model frozen/extra-forbid/id-pattern/empty-modules/status-enum; by_id miss; by_subsystem; render purity; load aggregate+sort, duplicate-id loud, unknown-module loud, missing/empty-dir loud, malformed-yaml loud, bad-id-names-file, real seed loads); ruff fmt+check clean, mypy clean (46 files); dogfood re-healed (errors.py → foundation api doc HASH/REGION) + `cdx check`/`lint`/`coverage --fail-under 95` exit 0 (new module added to coverage waiver, reason = R-07 wiki owns its api doc) | First slice of EPIC R (Reference, Traceability & Wiki). The golden reference of every cdx *feature* now has a single machine-readable source of truth: `feature-doc/catalog/*.yaml` (one file per subsystem). `featurecatalog.Feature` is a frozen `extra="forbid"` pydantic model (id matches `FEAT-<SUBSYS>-<NNN>`, non-empty `modules`, status enum, + `demos`/`tests` slots filled by R3/R5). `load_catalog(dir, *, known_modules=None)` aggregates every `*.yaml` (sorted), validates, and is LOUD via the new `errors.CatalogError` (K8) on: missing/empty dir, malformed yaml, a field/validation failure (names the file), a duplicate id across files, or a feature naming a module not in `known_modules` (typo guard). Deterministic: files read sorted, result sorted by id (K10). `render_features_md` renders the human `feature-doc/FEATURES.md` (grouped by subsystem, demo/test columns) — pure, byte-stable, never hand-edited (the yaml is the only prose source → nothing to drift). Seeded with 6 real, source-verified `extract` features. No wire/schema change (K6); no new dep (K0). Out of scope: full catalog population (R-02), traceability matrix (R-03), demo/test/source mapping (R3–R6), `cdx wiki` CLI (R-08). |
| R-02 | Populate the golden feature catalog — all subsystems (**186 features**) | DONE | whole-catalog `load_catalog(feature-doc/catalog, known_modules=<real set>)` loads clean (186 features, 18 subsystems, 0 dup ids, every module ref real); 19 test_featurecatalog tests green incl. the real-seed integration test (now over 186); headline public symbols across every subsystem probed real via import `hasattr`; `feature-doc/FEATURES.md` rendered; `cdx check`/`lint` still exit 0 (feature-doc/ is not tracked source so no dogfood drift) | Orchestrated fan-out: 16 subagents (one per subsystem cluster) each read their real source + ARCHITECTURE/SPEC, enumerated distinct user-meaningful features (not one-per-function), VERIFIED every named symbol via import probes, and wrote ONLY their own `catalog/<subsystem>.yaml`; the `load_catalog` loader was the cross-file consistency gate (unique ids, real modules, schema). Counts: agent 8, backends 8, cli 22, config 12, configv2 15, coverage 10, drift 10, extract 6, heal 9, layout 9, learn 6, manifest 9, monitor 9, pr 11, quality 9, record 13, reference 2, server 18. Orchestrator did the authoritative whole-dir load + a consolidated headline-symbol probe + spot-check; normalized the `server` cluster's id-prefix⇄subsystem (two client-side registry features moved from `subsystem: registry` to `server`). `feature-doc/README.md` documents the catalog-is-source / FEATURES.md-is-rendered contract. Out of scope: traceability matrix (R-03), demo/test slot wiring (R3/R5), `cdx wiki` (R-08), an automated "no orphan public capability" completeness assertion (needs the R-06 source index). |
| R-03 | Traceability engine (`traceability.py`) + `cdx trace` CLI | DONE | full suite green (**+27 new tests in test_traceability**, branch cov 97.47%; traceability.py 98%); ruff fmt+check clean, mypy clean (47 files); dogfood re-healed (new `cdx trace` command drifted the cli api doc → `cdx monitor --apply` HASH+REGION FIX) + `cdx check`/`lint`/`coverage --fail-under 95` ALL exit 0 (new module added to coverage waiver, reason = R-07 wiki owns its api doc; self-coverage 100% 42/42 files, 505/505 symbols); `cdx trace` on the real tree exits 0 and reports 0/186 covered (expected — real tests/demos un-annotated until R-04/R-05) | The engine that PROVES the catalog's promise: every feature has >=1 test AND >=1 demo. `traceability.py` (frozen pydantic `extra=forbid`, module-level compiled `FEATURE_REF_RE`/`_TAG_RE`, sorted/deterministic K10, loud, `__all__`): `scan_refs(root, kind, *, suffixes=('.py','.md'))` walks a tree (sorted, recursive), reads each file as TEXT (never imports — K1), and parses the inline tag convention `Feature(s): <id>[, <id>...]` (case-insensitive marker) into `FeatureRef(feature_id, path[repo-rel posix], kind, line[1-based])`. The MARKER disambiguates evidence from prose — a bare `FEAT-id` mention with no `Feature:` marker is NOT a reference (tested explicitly both ways). `EvidenceKind` enum = TEST/DEMO/SOURCE. `build_matrix(catalog, *, tests_root, demo_root, source_root=None)` crosses scanned refs against catalog ids → `TraceMatrix` with `tests_for`/`demos_for`/`features_without_test`/`features_without_demo`/`unknown_refs` (a tagged id NOT in the catalog → loud gap, K8) / `is_complete`. `render_matrix_md` = pure byte-stable per-feature table + Gaps section. `cli.py` gains `cdx trace [--catalog --tests-root --demo-root --json --fail-on-gap]`: loads `feature-doc/catalog` with pkgutil-discovered `known_modules`, scans `tests/`+`demo/`, prints a human summary; `--json` emits the matrix; `--fail-on-gap` exits nonzero iff not complete (else always exit 0). NOT wired into any CI gate yet (that is R-07) — against the real tree it correctly reports ~100% gaps until R-04/R-05 annotate demos/tests. No new dep (K0); no wire/schema change (K6). Out of scope: annotating real tests/demos (R-04/R-05), node-level attribution (R-05 AST), source inline tags (R-06), `cdx wiki` + the CI gate (R-07). |
| R-04 | Demo 1:1 feature mapping (`demo/DEMOS.md`) | DONE | validable goal MET — `build_matrix(catalog, tests_root=tests, demo_root=demo).features_without_demo() == ()` AND `unknown_refs() == []` (probe printed both empty); **51 demo cases** authored, 197 demo refs over all **186** features, every one of the **18** subsystems has ≥1 case; **4 new tests** in `tests/test_demo_traceability.py` (exists / scan non-empty / no-unknown / features_without_demo==()) green; existing `test_demo_e2e` + `test_demo_walkthrough` stay green (16); FULL gate green — `ruff format --check` (138 files) + `ruff check` clean, mypy clean (47 files), `pytest -q --cov --cov-branch` exit 0, **branch cov 97.50%** (≥90%), 1440 collected/3 deselected; `cdx check`/`lint` exit 0 (DEMOS.md lives under demo/, NOT tracked source → no api-doc drift, no reheal) | The DEMO side of the traceability matrix is now COMPLETE. `demo/DEMOS.md` is the demo↔feature catalog: a header explaining the `Features:` tag convention `cdx trace` scans, then 51 `DEMO-NNN` OBSERVABLE cases grouped by 12 user journeys (A detect→heal, B apply-fix, C link→generate, D coverage+gap→issue/ticket, E doctor/adopt, F config-v2 layout/index/sync, G central server+register+dashboard, H backends+agent, I record/log/sinks, J extractor seam+shell, K reference/traceability self-doc, L property invariants). Each case: Title, What it shows, How to observe (a `walkthrough.py` step id, an exact `cdx <cmd> --config demo/config/cdmon` invocation, a checked-in config/doc artifact, or — for opt-in paths: live LLM api/claude-code/agent backends, the Postgres SqlStore, server routes — a DOCUMENTED reproducible recipe), and an honest `Features:` tag line. NO catch-all case: the rich detect→heal scenario (DEMO-001..005) honestly tags config/extract/drift/heal/manifest/monitor/backends/record across several cases; opt-in features each get a self-contained reproducible recipe (a documented demo case counts). Honesty audited — moved `cdx should-sync`/`should_sync` (FEAT-CLI-009/FEAT-PR-003) off the git-mode case onto the PR-family case (DEMO-051) where they belong, and pointed the git-mode case at FEAT-CONFIGV2-012 (the sync engine it actually exercises). No new dep (K0); no source-module change (walkthrough.py already covered every honest tag — no extension needed); `cdx trace --fail-on-gap` deliberately NOT wired into any gate (R-07). Out of scope: TEST-side tags (R-05 — `features_without_test` stays full), node-level attribution (R-05), `cdx wiki` + CI gate (R-07). |
| R-05 | Test taxonomy — physically reorganize `tests/` into boundary dirs (`unit`/`integration`/`system`/`smoke`, keep `regression`) + path-auto-applied markers + marker-lint | DONE | validable goal MET — all 65 former `tests/test_*.py` moved into exactly one boundary dir (**25 unit, 24 integration, 15 system, 1 smoke**); 62 of 65 are git renames (R) preserving history, 3 (`test_demo_traceability`/`test_featurecatalog`/`test_traceability`) were untracked R-02..R-04 working-tree files so land as new files in their boundary dir; new root `tests/conftest.py` auto-applies the boundary marker by path (`pytest_collection_modifyitems`) + snapshots the final marker set (`pytest_collection_finish`, `trylast`) for the lint; `regression/` keeps its own `regression` marker (no boundary marker); 4 markers registered in pyproject `[tool.pytest.ini_options].markers`, `addopts` UNCHANGED; new `tests/smoke/test_boundaries.py` marker-lint (2 tests) asserts no stranded `test_*.py` directly in `tests/` AND every collected item carries EXACTLY one of {unit,integration,system,smoke,regression}; **baseline 1440 passed / 3 deselected → post-move 1442 passed / 3 deselected** (= 1440 + the 2 lint tests, SAME pass set); `pytest -m {unit/integration/system/smoke}` select **612/615/192/3** (+ regression 23) = 1445 collected, each non-empty; FULL gate green — `ruff format --check` + `ruff check` clean, mypy clean (47 files), branch cov **97.50%** (≥90), `cdx check`/`lint`/`coverage --fail-under 95` all exit 0 (tests are not tracked source → no api-doc drift, no reheal) | R-05 follows the depth-independence-FIRST safety protocol. Step 1: new `tests/_repo.py` exposes `REPO_ROOT` via upward `pyproject.toml` search; replaced all 16 repo-root anchors (`Path(__file__).resolve().parents[1]` / `.parent.parent`, ~15 files) so file depth is irrelevant — gated green BEFORE any move. Step 2: `__init__.py` in each boundary dir keeps `tests.<boundary>.<module>` importable (tests is a package). Step 3: rewrote the 5 cross-test imports to the new paths. Step 4: `git mv` per the classification (untracked files fell back to plain `mv`). Step 5: root conftest + marker-lint. Classification is by DOMINANT boundary read from each module's docstring + signals (CliRunner/TestClient/subprocess): pure single-module → unit (incl. `pr`/`issues`/`agent` — INJECTED transport, no orchestration; `traceability`/`featurecatalog`/`records`/`index`/`templates` — pure render/parse); module-to-module seam or cross-module config → integration (incl. `build`/`doctor`/`report`/`generate`/`apply_fix` even though they touch a CliRunner — the seam, not e2e, dominates; `config_v2`/`config_index`/`docstyle` provide shared `_write_tree`/`_write_templates` helpers); CLI/server driven e2e on a REAL fixture/demo/example → system (`cli`/`cli_sync`/`demo_*`/`dirlayout_e2e`/`e2e_ticket_coverage`/`example_*`/`ci_templates`/`dogfood`/`standalone`/`server_launch`/`live_llm`); trivial import wiring → smoke. Close calls: `test_demo_traceability` (drives no CLI — it's a pure `build_matrix` over the demo dir using `REPO_ROOT`, so INTEGRATION not system despite the "demo" name); `test_e2e_ticket_coverage` (docstring says "system/integration" — chosen SYSTEM, it stands up a full TestClient app over a real seeded surface); `test_pr`/`test_issues` (look integration-y but use an injected transport and exercise one module's logic → unit). Out of scope (R-06/R-07): per-test `Boundary:`/`Feature:` annotation + test wiki, splitting mixed files. |
| R-06 Part A | Test-wiki extractor engine (`testwiki.py`) — `collect_tests` + `render_test_wiki_md` (Part B annotation deferred) | DONE | validable goals 1 & 2 MET; **20 new tests in `tests/unit/test_testwiki.py`** green (TDD red→green confirmed first via `ModuleNotFoundError`); FULL gate green — `ruff format --check` (147 files) + `ruff check` clean, mypy clean (48 files), `pytest -q --cov --cov-branch` exit 0 (testwiki + full-suite exit 0), branch cov **97.49%** (≥90), **testwiki.py 97%** (only 2 defensive lines uncovered: the all-blank-docstring `_summary` fallthrough + the `not path.is_file()` rglob guard); `cdx check` exit 0 (clean — no drift; testwiki.py is not a tracked-doc module so NO reheal needed), `cdx lint` exit 0, `cdx coverage --fail-under 95` exit 0 (100% files 42/42, 100% symbols 505/505; testwiki.py + its 5 public symbols added to the coverage waiver, reason = R-07 wiki owns its api doc); **real-tree `collect_tests(Path('tests'))` → 70 modules, 1361 cases, 0 UNKNOWN** (by boundary: unit 26, integration 24, system 15, regression 3, smoke 2 — proves the R-05 dirs classify) | The TEST-side extractor that turns the test tree into a wiki with NO second source of truth. `testwiki.py` (frozen pydantic `extra=forbid`, `__all__`, sorted/deterministic, loud — mirrors `featurecatalog`/`traceability` style): `TestBoundary(str, Enum)` = UNIT/INTEGRATION/SYSTEM/SMOKE/REGRESSION/UNKNOWN; `TestCase` (nodeid, path[repo-rel posix], name, boundary, summary, features); `TestModule` (path, boundary, module_features, cases). `collect_tests(tests_root)` walks sorted, `ast.parse`s each `test_*.py` (NEVER imports/execs — K1), collects top-level AND class-nested `def test_*`/`async def test_*` (nodeid `path::func` or `path::Class::func`), pulls `ast.get_docstring` summary (first non-blank line, "" if none), resolves boundary from the first known dir in the path (else UNKNOWN), and unions each function's `Feature:`/`Features:` tags with the module docstring's `Features:` tags — REUSING `traceability._TAG_RE`/`FEATURE_REF_RE` (imported, not duplicated). Deterministic: modules sorted by path, cases in source order (K10); missing root → `()` (not an error); a genuinely unparseable file → loud `CatalogError` (K8, reused). `render_test_wiki_md(modules)` is pure/byte-stable: grouped by boundary (enum order) → module → case row with summary + ``feature`` links, then a per-feature "Tested by (feature index)" mapping each id → sorted nodeids. **CONVENTION for Part B annotators:** a class-nested test is `path::Class::func` (top-level is `path::func`); a no-docstring test yields `summary=""` (rendered as "(no docstring)") and inherits ONLY the module-level `Features:` tags. No new dep (K0 — stdlib `ast`); `__test__ = False` set on the three `Test*`-prefixed classes so pytest doesn't try to collect them. Out of scope: Part B (annotating real test files with module-level `Features:` tags so `features_without_test()==()`), the `cdx wiki` CLI command (R-08), the source index/wiki (R-07). |
| R-06 | Test annotation + test wiki (`testwiki.py`) + every-feature-has-a-test | DONE | full suite **1464 passed, 3 deselected**, 97.49% branch; testwiki.py 97% (20 unit tests); ruff/mypy clean; `build_matrix(tests,demo).is_complete() == True` (features_without_test=(), features_without_demo=(), unknown_refs=[]); `cdx check`/`lint`/`coverage --fail-under 95` exit 0 | Part A: `testwiki.collect_tests` AST-parses the test tree (NEVER imports it, K1) → frozen `TestModule`/`TestCase` (nodeid incl. class-nested, boundary from the R-05 dir, docstring first-line summary, `Feature:` refs reusing traceability's `_TAG_RE`); `render_test_wiki_md` → byte-stable wiki (`feature-doc/wiki/TEST_WIKI.md`, 70 modules / 1363 cases) grouped by boundary→module + a per-feature "tested by" index. Single source of truth = the test's own docstring + a module-level `Features:` tag (no duplication). Part B: 3-subagent fan-out annotated all 70 test files (unit+smoke 97 / integration 99 / system+regression 136 distinct features, honest file-level mapping); a gap-closer added 2 real CLI tests (`cdx build` html-twin happy path; `cdx serve` loud-no-config guard) + tagged FEAT-HEAL-003 (known_region_ids) and FEAT-CONFIGV2-010 (_v2base frontmatter primitives) where already tested. Result: the FULL traceability matrix is green — every one of the 186 features has ≥1 demo AND ≥1 test. `cdx trace --fail-on-gap` would now exit 0 (wired into the gate in R-08). testwiki.py added to the dogfood coverage waiver. Out of scope: `cdx wiki` regen command + CI gate (R-08); source index/wiki (R-07). |
| R-07 | Source index + source wiki (`srcindex.py`) + the "no orphan public capability" guarantee | DONE | completeness probe over the REAL tree+catalog prints `features_without_module_match() == ()`, `modules_without_feature() == ()`, AND `build_matrix(...).is_complete() == True`; **18 new tests in `tests/unit/test_srcindex.py`** green (1482 collected, 1479 passed / 3 deselected `pg`), **srcindex.py 100%** (74/74 stmts, 24/24 branches); FULL gate green — `ruff format --check` (149 files) + `ruff check` clean, mypy clean (49 files), `pytest -q --cov --cov-branch` exit 0, branch cov **97.67%** (≥90); `cdx check`/`lint` clean + `cdx coverage --fail-under 95` exit 0 (100% files 42/42, 100% symbols 505/505; srcindex.py + its 7 public symbols added to the dogfood coverage waiver) | The deferred R-02 "no orphan public capability" check is now REAL. `srcindex.py` (frozen pydantic `extra=forbid`, `__all__`, sorted/deterministic, loud — mirrors featurecatalog/traceability/testwiki style): `ModuleIndex(module, path[repo-rel posix], public_symbols[sorted top-level public names], features[sorted catalog ids whose `modules` name this module])`; `SourceIndex(modules)` with `features_without_module_match()` (a catalog feature naming a module the package no longer ships → empty for the real tree) + `modules_without_feature()` (the orphan check). `build_source_index(pkg_root, catalog)` REUSES `inventory.discover_files`/`discover_symbols` (no AST re-impl — K0), folds each file into its TOP-LEVEL module (first path component; stem for a top-level file; the package's own `__init__.py` re-export aggregator is skipped, a subpackage `__init__.py` folds into its parent), aggregates each module's public, DOT-FREE symbol names (a `Class.method` is nested, not module surface), and joins to the catalog by module name. The catalog (feature→modules) join is carried as a `PrivateAttr` set via `object.__setattr__` so the frozen index's serialized surface stays clean. `render_source_wiki_md` = pure byte-stable per-module path/symbols/feature-links + a Coverage summary naming any orphan. **RECONCILED the catalog**: the real orphan list was `srcindex`/`testwiki`/`traceability` (the reference machinery that documents everything else was itself un-catalogued); added 4 honest `reference` features — FEAT-REFERENCE-003 (traceability matrix engine), -004 (testwiki AST extractor), -005 (source index + no-orphan check), -006 (rendered source wiki) — verified the existing joins (`errors`→CONFIG-012, `blocks`→HEAL-002/003, `build`→LAYOUT-008/009, `generate`→CONFIGV2-013, `index`→CONFIGV2-009/015, `_v2base`→CONFIGV2-010) all match by module name. To keep `build_matrix(...).is_complete()` True, added demo cases DEMO-052 (003/004) + DEMO-053 (005/006) to `demo/DEMOS.md` and tagged `Features:` on `test_traceability.py` (003), `test_testwiki.py` (004), `test_srcindex.py` (005/006); regenerated `feature-doc/FEATURES.md` (now 190 features). No new dep (K0); no wire/schema change (K6); NO CLI command (that is R-08). Out of scope: `cdx wiki` rendering srcindex to `feature-doc/wiki/SOURCE_WIKI.md` + the traceability CI gate (R-08). |
| R-08 | `cdx wiki` (regenerate all 4 EPIC-R wikis from single sources) + `--check` freshness gate + `cdx trace --fail-on-gap` & `cdx wiki --check` wired into CI — **EPIC R close-out** | DONE | validable goals MET — `cdx wiki` writes the four canonical artifacts (`feature-doc/FEATURES.md` + `wiki/{TEST_WIKI,SOURCE_WIKI,TRACEABILITY}.md`) and a second run reports all four `unchanged` (idempotent K7); `cdx wiki --check` exit **0** on the fresh tree, **1** after a wiki is touched (K8); `cdx trace --fail-on-gap` exit **0** on the real tree (**191/191 features have both a demo AND a test**, `build_matrix(...).is_complete() == True`). FULL gate green: `ruff format --check` (152 files) + `ruff check` clean, mypy clean (50 source files), `pytest -q --cov --cov-branch` exit 0, branch cov **97.67%** (≥90); **`wiki.py` 100%** (41/41 stmts, 4/4 branches). **14 new tests** — 9 unit (`tests/unit/test_wiki.py`) + 5 system (`tests/system/test_wiki_cli.py`). Dogfood re-healed (`cdx monitor --apply --config config/cdmon` healed the cli api doc HASH/REGION after the new `wiki` command); `cdx check`/`lint`/`coverage --fail-under 95` exit 0; `wiki.py` added to the dogfood coverage waiver. CI: 2 offline steps added to `docs:gate` (`cdx wiki --check`, `cdx trace --fail-on-gap`, K4). | **One regeneration entry point, one output set.** `wiki.py`: `WIKI_TARGETS: dict[Path, Callable[[Path], str]]` is the SINGLE source of the four output paths → render thunks (each takes a repo root, returns byte-stable markdown), so `cdx wiki` and `--check` can never diverge. `regenerate(repo_root, *, write) -> list[(posix_path, changed_or_stale)]` (sorted, K10): renders each target, compares to disk; WRITE mode writes changed/missing (mkdir parents) — a second run is a no-op (K7) — and CHECK mode (`write=False`) writes NOTHING and reports stale = missing-or-differs (the CI gate, K8). The catalog + `known_modules` (pkgutil) are loaded per render thunk; the three catalog-dependent renders reuse `featurecatalog.render_features_md`/`srcindex`/`testwiki`/`traceability` renderers (no new dep, K0). `cli.py` `wiki(check)` is a thin wrapper mirroring `index --check` exit conventions. **ADDED FEAT-REFERENCE-007** ("cdx wiki regeneration + freshness gate", modules `[wiki]`) to `reference.yaml` + DEMO-054 + the test tags so the matrix stays complete (191 features now). Closes EPIC R: the golden reference, its demo/test 1:1 mappings, and the source/test/traceability wikis all regenerate from one source each and are gated against drift. |
| EPIC R | ✅ **COMPLETE** — the golden reference (catalog → FEATURES.md), traceability (feature × demo × test × source), test/source/traceability wikis, and the `cdx wiki` regeneration + `cdx trace --fail-on-gap`/`cdx wiki --check` CI gates | DONE | R-01..R-08 all DONE; 191 catalogued features, every one with a demo + a test (`is_complete() == True`); wikis regenerate from single sources and are gated offline (K4) in `docs:gate`. cdx now applies its own code↔doc discipline to its own documentation system. | The closing slice made EPIC R self-sustaining: a new feature/module/demo/test that breaks the 1:1 mapping, or a wiki that drifts from its source, now fails CI rather than landing silently. |
| R-09a | Global **`GET /wiki`** server endpoint — serves the committed EPIC-R wikis rendered to HTML (python/server only) | DONE | validable goal MET — `GET /wiki` returns `{"sections":[{"id","title","html"},...]}` for the four committed wikis (features, traceability, tests, source) in deterministic `WIKI_SECTIONS` order, public/no-auth (mirrors `/config/templates`), a missing section file omitted, an absent `feature-doc/` → `{"sections": []}` (K8). **6 new integration tests** in `tests/integration/test_server.py` (all-sections rendered+ordered with `<h1`/`<table` markup + exact `{id,title,html}` key set; missing-file-omitted; empty-dir empty; nonexistent-dir empty; absent-feature-doc via monkeypatched `_wiki_dir`→None empty; public-no-auth) — TDD red (route 404 / `create_app() unexpected kwarg wiki_dir`) confirmed FIRST, then green. FULL gate green: `ruff format --check` (152 files) + `ruff check` clean, mypy clean (50 source files), `pytest -q --cov --cov-branch` exit 0, **branch cov 97.68%** (≥90), the new route + both helpers covered. `cdx check`/`lint`/`coverage --fail-under 95`/`wiki --check`/`trace --fail-on-gap` ALL exit 0 (192/192 features have a demo + a test, `is_complete()==True`). Dogfood: regenerated the wikis (`cdx wiki`) + rehealed the server api doc (`cdx monitor --apply` HASH/REGION FIX) after the new route + `create_app` param. | The server side of R-09. `server/app.py`: a module-level `WIKI_SECTIONS` tuple `((id,title,relpath)...)` is the SINGLE source of the section set + order (shared with the R-09b dashboard contract). `_wiki_dir()` mirrors `_default_static_dir()` — resolves `<repo root>/feature-doc` if it exists, else `None`. `_load_wiki_sections(wiki_dir)` is pure (no clock, K10): `None`→`[]`, else for each section whose `wiki_dir/relpath` is a file, read it and append `{id,title,html}` with the body rendered by the engine's OWN dependency-free `build.render_markdown` (no new dep, K0; imported lazily so `import app` stays cheap), skipping missing files. `create_app` gains an injectable `wiki_dir: Path | None = None` keyword (default-`None` auto-resolves to `_wiki_dir()` inside, so the live `cdx serve` / `build_standalone_app` paths serve the real wikis with NO extra wiring — verified the default `client` fixture serves all 4 real wikis); tests inject a tmp dir (no real-FS coupling, K4). `@app.get("/wiki")` is GLOBAL + PUBLIC (no `_verify_token`, like `/config/templates`) → `{"sections": _load_wiki_sections(resolved_wiki_dir)}`. Catalog: added **FEAT-SERVER-019** ("Feature-wiki endpoint", subsystem server, modules `[server]`, constraints `[K0,K4,K8,K10]`) to `feature-doc/catalog/server.yaml`, tagged on the `test_server.py` module docstring `Features:` line + extended DEMO-026 (the standalone dashboard serves the same `/wiki`) so the matrix stays complete (192 features now). DID NOT touch `dashboard/` (the parallel R-09b agent owns the TS). |
| R-09b | Dashboard **Wiki** nav item + lazy-loaded full Wiki frontend page (`GET /wiki` consumer; dashboard/TS only) | DONE | dashboard gate green — `npm run test:run` **141 passed / 15 files** (was 134; +7 new: 1 `client.test.ts` `wiki()`, 5 `Wiki.test.tsx`, 1 `App.routing.test.tsx` lazy-nav), `npm run lint` clean (eslint exit 0), `npm run build` succeeds (tsc -b type-check + vite build) and **code-splits the Wiki page into its own chunk** (`dist/assets/Wiki-*.js` ≈ 2 kB, separate from the main bundle) — proving the lazy boundary. TDD: tests written first, confirmed red (`client.wiki is not a function`, missing `Wiki` module/route), then implemented to green. | The EPIC-R wikis now surface in the console. `types.ts` gains `WikiSection {id,title,html}` / `WikiPayload {sections}`; `api/client.ts` gains `wiki(): Promise<WikiPayload>` (GET /wiki, OPEN read, mirrors `configTemplates`). **"Show Wiki first":** `AppShell` adds an always-visible **Wiki** `<Link to="/wiki">` in the Reference group (DocIcon, active when `pathname==="/wiki"`) + `pageLabel("/wiki")="Wiki"`. **"Switch on click":** `App.tsx` routes `/wiki` to `const Wiki = lazy(() => import("./pages/Wiki"))` inside a `<Suspense fallback={<p role="status">Loading…</p>}>` boundary, so the Wiki bundle + its `api.wiki()` fetch fire ONLY on navigation. `pages/Wiki.tsx` (the full frontend): `useApi(() => api.wiki())` then a docs layout — a sticky LEFT **section rail** (the wikis as `<button>`s, the selected one `--active`, each with a cheap heading-count badge derived from the HTML by a pure regex scan, K10) + a capped RIGHT **prose pane** rendering the selected section's server-pre-rendered HTML via `dangerouslySetInnerHTML` (a safe fragment per the R-09 contract). Local `useState(selectedId)` switches sections client-side off the single payload (default = first section). Loading (`role="status"`) / error (`role="alert"`) / EMPTY (`sections.length===0` → "No wiki available — run `cdx wiki`") states; injectable `api?` prop defaulting to `apiClient` (no network in tests). Styled under a cohesive `/* — Wiki — */` block in `index.css` (emerald accents, panel/nav voice, design tokens, responsive). No new npm dependency (K0). DID NOT touch python/server (the parallel R-09a agent owns `GET /wiki`). |
| EPIC OWN | **Ownership & accountability** — peg a human/team to every monitored document; central roster MIRROR flags departed owners; reassignment (config=truth) is the fix | PLANNED | Pinned: ARCHITECTURE.md §"EPIC OWN" (full signatures), CONSTRAINTS K2 scope note (ownership = config-as-truth per K0, orthogonal to K2), slices OWN-00..OWN-06. Central Postgres CONFIRMED already wired (`store_from_env`→`$CDMON_DATABASE_URL`→alembic→`SqlStore`; SQLite twin + `pg`); EPIC OWN adds migration 0006 (roster + ownership_mirror). Two tiers: `DocumentSpec.owner/team/dri` (per-repo config = truth, own-by-team + DRI indirection) + central roster mirror (mark-departed cascades orphan detection across repos). Pure `ownership.py` (`resolve_ownership`/`detect_orphans`, no clock); `cdx ownership` CLI (read-only); `ReassignOwnerEdit` via generate-to-disk; `GET /repos/{id}/ownership` + admin-token roster routes; Ownership frontend page; demo seeds a real departure→orphan. | OWN-00 plan slice — docs only, gate untouched. Each OWN-NN: TDD red-first, full gate green (ruff+mypy+pytest ≥90% branch + dogfood reheal + `cdx trace`/`wiki --check` + `astro check`), new `ownership` catalog subsystem grown in lockstep with demo+test evidence. |
| OWN-01 | Config ownership fields (`DocumentSpec.owner/team/dri`) + pure `ownership.py` (`resolve_ownership` + `Identity`/`RosterSnapshot`/`EffectiveOwner`) | DONE | TDD red→green (**10 new `tests/unit/test_ownership.py`**, confirmed red via `ModuleNotFoundError` first); FULL gate green — `ruff format --check` (174 files) + `ruff check` clean, mypy clean (54 src), `pytest --cov --cov-branch` exit 0, **branch cov 97.71%** (≥90), **ownership.py 100%** (48/48 stmts, 8/8 branches); owner/team/dri round-trip byte-identically (defaults dropped, K7). Dogfood: `cdx check` clean — the optional fields don't change the fingerprinted AST surface, so NO docs/api reheal needed; `ownership.py` added to the index.yaml coverage waiver (api doc owned by cdx wiki / feature catalog). EPIC-R gates green: **FEAT-OWNERSHIP-001/002** added (`feature-doc/catalog/ownership.yaml`, 20th subsystem) + DEMO-059/060 + tagged tests → `cdx trace --fail-on-gap` **201/201**, `cdx wiki --check` fresh. Demo `core-api` doc now owned by demo-team with DRI dana. | Tier-1 ownership-of-record = config (the K0 truth / K2 scope note); own-by-team + DRI indirection so a departure is "DRI vacant", not orphaned. `accountable = dri→owner→team→inherited unit owner`; `durable = team→owner→inherited`. The roster (`Identity`/`RosterSnapshot`) is the injected OFFLINE mirror — `is_active(None)`/unknown = False (an owner the roster can't vouch for is not active). Pure, sorted, clock-free (K1/K10). |
| OWN-02 | Pure orphan detection — `ownership.detect_orphans` + `OwnershipStatus`/`OwnershipFinding` | DONE | TDD red→green (+10 tests in `test_ownership.py`, 20 total, red via missing-symbol import first); FULL gate green — ruff format/check + mypy clean, `pytest` exit 0, branch cov **97.73%**, **ownership.py 100%** (81/81 stmts, 18/18 branches). EPIC-R: **FEAT-OWNERSHIP-003** + **DEMO-061** (offline departure→orphan over the demo config) → `cdx trace --fail-on-gap` **202/202**, `cdx wiki --check` fresh. | Classifies each resolved doc against the injected roster: `UNOWNED` (no identity named) / `ORPHAN_OWNER_DEPARTED` (accountable gone, no active fallback) / `ORPHAN_DRI_VACANT` (DRI left but the durable team still owns it — soft, reassign a DRI). `OK` omitted by default so the result is exactly what needs a human (K5). An orphan is never healable; reassignment (OWN-05) is the fix. Pure, sorted, clock-free (K1/K10). |
| OWN-03 | `cdx ownership` CLI (read-only) + `load_roster`/`render_ownership_text` | DONE | TDD red→green (+5 unit tests in `test_ownership.py`, +5 system tests in `test_ownership_cli.py`, red via missing-symbol/route first); FULL gate green — ruff format/check + mypy clean, `pytest` exit 0, branch cov **97.71%**, **ownership.py 100%**, cli.py 95%. LIVE: `cdx ownership --config demo/config/cdmon` lists all 8 demo docs (`core-api` accountable=dana, the rest inherit `demo-team` via the unit fallback); `--roster … --fail-on-orphan` exits 1 on a departed-owner orphan. Dogfood: rehealed `docs/api/ops.md` + `README.md` (the CLI command list is monitored). EPIC-R: **FEAT-OWNERSHIP-004** + **DEMO-062** → `cdx trace --fail-on-gap` **203/203**, `cdx wiki --check` fresh. | Read-only + offline (K1/K4), no backend. `--fail-on-orphan` trips on `ORPHAN_*` only — an `UNOWNED` doc is a coverage gap, not a departure. `load_roster` loud on a malformed roster (K8). `_unit_owner_map` builds the doc→unit-owner fallback from the bundle so a doc with no owner inherits its unit frontmatter owner. |
| OWN-04 | Central roster MIRROR + per-repo `GET /ownership` (Alembic 0006, admin token, both-stores parity) | DONE | FULL gate green — ruff format/check + mypy clean, `pytest` exit 0, branch cov **97.73%**; **ownership.py / configsync.py / server/store.py 100%**, server/db.py 99%, server/app.py 96%. **+15 tests**: `tests/integration/test_ownership_server.py` (roster CRUD, routes, admin auth 401/403/201, the cross-repo departure CASCADE, ConfigDocument ownership round-trip — ALL parametrized over InMemoryStore AND SqlStore) + `test_db.py::test_alembic_migration_0006_roster_up_then_down` (upgrade head creates `roster`, downgrade drops it). Central Postgres path: migration 0006 adds the `roster` table; the per-doc owner/team/dri + resolved accountable/durable ride in the existing `config_documents` JSON column (additive K6 — NO column migration). Dogfood rehealed `docs/api/server.md`. EPIC-R: **FEAT-OWNERSHIP-005/006/007** + **DEMO-063/064/065** → `cdx trace --fail-on-gap` **206/206**, `cdx wiki --check` fresh. | DESIGN REFINEMENT vs the OWN-04 pin: NO separate `ownership_mirror` table — folding the resolved owner into `ConfigDocument`'s JSON is simpler and still cascades, because `detect_orphans` runs at READ time against the LIVE roster (so one `POST /admin/roster/{name}/departed` re-flags every repo's orphans with no re-sync). Roster reuses `ownership.Identity`; the precedence is the ONE shared `resolve_accountable_durable` (engine + sync never diverge). Admin auth is a SEPARATE global token (`$CDMON_ADMIN_TOKEN`), never a per-repo token; open when unset (dev). |
| OWN-05 | Reassign-owner edit — `ReassignOwnerEdit` + pure `config.set_document_owner` + generate dispatch | DONE | FULL gate green — ruff format/check + mypy clean, `pytest` exit 0, branch cov **97.71%**; **server/edits.py 100%**, config.py 97%. **+5 tests**: `set_document_owner` unit tests (partial set / new frozen model / round-trip / loud-on-unknown, `test_unit_serializer.py`), a `reassign_owner` through `apply_edits_to_disk` on a demo copy (rewrites `core.yaml`'s dri, keeps owner/team, byte-identical re-apply K7, `test_generate.py`), and the 6th union action wired into `test_config_edits_routes.py::_ALL_ACTIONS` (stage→list→generate over both stores). Dogfood rehealed `docs/api/foundation.md` (config) + `docs/api/server.md` (edits). EPIC-R: **FEAT-OWNERSHIP-008** + **DEMO-066** → `cdx trace --fail-on-gap` **207/207**, `cdx wiki --check` fresh. | The human fix for an orphan, config = truth: a provided owner/team/dri SETS that field, `None` LEAVES it (partial reassignment keeps the rest). `set_document_owner` returns a NEW frozen `UnitFile` (B-02); `apply_edits_to_disk` → `set_document_owner` → byte-stable yaml rewrite → `run_sync` re-mirror, so reassigning a departed DRI to an active person clears the orphan on the next `GET /ownership`. |
| OWN-06a | Live demo roster seed (departure→orphan) + `/ownership` dedup + e2e | DONE | FULL gate green — ruff/mypy clean, `pytest` exit 0, branch cov **97.71%**. `scripts/seed_demo.py` gains `_seed_roster` (cdmon-team + demo-team active, `erin` active, `dana` DEPARTED), so the live :33333 `GET /repos/demo-taskflow/ownership` shows a REAL `core-api` `orphan_dri_vacant` out of the box while the dogfood repo stays clean (verified in-process via TestClient). Fixed a latent OWN-04 bug: the `/ownership` route now DEDUPS by doc_id (a doc mirrored under both git+local config partitions appeared twice). **+2 tests**: `test_demo_e2e.py::test_central_ownership_view_shows_departed_dri_orphan` (e2e over the seeded app) + `test_ownership_server.py::test_ownership_dedups_doc_across_sync_kinds` (both stores). EPIC-R: **FEAT-OWNERSHIP-009** + **DEMO-067** → `cdx trace --fail-on-gap` **208/208**, `cdx wiki --check` fresh. No reheal (route body isn't in the fingerprinted surface). | The "show it working" half: ownership rides through the existing sync (config_documents carry the resolved owner), so seeding only needs the roster + a departure. dana departed + demo-team active → soft `orphan_dri_vacant` (reassign a DRI), the headline the frontend (OWN-06b) will render. |
| OWN-06b | Frontend Ownership page (console per-repo tab) | DONE | Frontend gate green — `astro check` **0 errors / 0 warnings**, `astro build` complete, `npm run test:run` **162 passed / 16 files** (+4 new `Ownership.test.tsx`: list owners, orphan banner + status, clean banner, empty state; +1 RepoNav tab assertion). New `pages/Ownership.tsx` (a sibling per-repo tab after Documents) reads `GET /repos/{id}/ownership` via `api.ownershipFor`, renders an orphan banner + a per-document table (accountable / team / dri / status with signal dots), reusing the Coverage page's table + state pattern; `OwnershipData`/`OwnershipOwner`/`OwnershipFinding` types, `linkToOwnership` route, `RepoRoute` dispatch, `RepoNav` tab, `AppShell` label. NO new engine module/feature (renders FEAT-OWNERSHIP-007), so the catalog/trace are untouched (208/208). NO python change. | The accountability feature is now clickable: open `demo-taskflow` → Ownership shows `core-api` as a departed-DRI orphan (dana), the rest owned by the active demo-team — the live face of the OWN-04 `/ownership` view + the OWN-06a seeded departure. |
| EPIC SVR | ✅ **COMPLETE** — central-server hardening + operator settings: every hardcoded runtime tunable moved into one versioned `config/settings.yaml` (`settings.py` core model + loader + env overlay; secrets stay in env, only PRESENCE surfaced), the missing CORS / TrustedHost / per-process rate-limit / git clone-timeout + SSRF-allowlist hardening wired from it (each added ONLY when configured ⇒ default app byte-identical), `GET /settings` + `cdx settings` (redacted) + a console Settings page, and Docker/compose/DEPLOY packaging | DONE | SVR-01..06. Branch `feat/server-hardening-settings` (commits 83b746c + 765041b). 8 catalogued FEAT-SETTINGS-001..008 + DEMO-068..075. App version single-sourced via importlib.metadata. Gate green: ruff/mypy, pytest --cov-branch **97.79%**, trace 216/216, wikis fresh, astro+vitest 168. | The "central server" main() is settings-driven; `cdx serve` keeps its localhost dev defaults. Defaults reproduce today's behavior so an absent settings.yaml is a no-op (K6). |
| EPIC SLA | ✅ **COMPLETE** — time-based staleness / review SLA (the time-based half of accountability; OWN was departure-based): config-as-truth `DocumentSpec.reviewed` graded against an injected `now` (K10), audience-aware SLA (K3), `cdx staleness` + read-time `GET /repos/{id}/staleness` + a console "Review SLA" section | DONE | SLA-01..05 (commit 64138d6). Pure `staleness.py` (`grade_doc`/`detect_stale`, NEVER_REVIEWED/STALE/FRESH), `MonitorConfig.staleness` (default_days + audience_days), `ConfigDocument.reviewed/sla_days` mirrored by `configsync._build_rows`, demo core-api stamped STALE + getting-started FRESH. 6 catalogued FEAT-STALENESS-001..006 + DEMO-076..081. Gate green: ruff/mypy, pytest --cov-branch **97.82%**, trace 222/222, wikis fresh, astro+vitest 170. | Decided config-as-truth `reviewed` over deriving from resolutions: deterministic + consistent with OWN. Future: auto-bump `reviewed` on a human Resolution to close the loop. |
| EPIC OWN | ✅ **COMPLETE** — ownership & accountability: peg a human/team to every document, a central roster MIRROR that flags departed owners (cross-repo, read-time cascade), and config-as-truth reassignment | DONE | OWN-00..06b all DONE. Tier-1 ownership-of-record in config (`DocumentSpec.owner/team/dri`, own-by-team + DRI), pure `ownership.py` (`resolve_ownership`/`detect_orphans`, clock-free), `cdx ownership` CLI, central roster (Alembic 0006, both-stores parity, admin-token routes), `GET /ownership` cross-repo departure cascade, `ReassignOwnerEdit` orphan fix, live demo seeded departure→orphan + a console Ownership tab. 9 catalogued features (FEAT-OWNERSHIP-001..009) all traced (208/208); central Postgres confirmed + extended (0006). Full gate green at every slice (ruff+mypy+pytest ≥90% branch + dogfood reheal + trace/wiki + astro). | K2 was clarified, not inverted: ownership metadata is config-as-truth per K0, orthogonal to K2 (doc content vs code). The roster is a mirror; orphan detection runs on READ, so one departure re-flags every repo. |
