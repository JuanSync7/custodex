---
cdm:
  audience: eng-guide
  fingerprint: 6248b6fe18587fbd
  region_hashes:
    symbols: 89786fc5065f205b
  schema_version: 1.0.0
---
# io-api

> Engineering guide for the taskflow **io** package: JSON persistence
> (`save_graph`/`load_graph`) and the plain-text run report (`render_report`).
> The managed `symbols` region below is generated by `cdx` from
> `src/taskflow/io` and drifts whenever a public signature there changes.

<!-- CDM:BEGIN symbols -->
| symbol | kind | signature |
|--------|------|-----------|
| _STATUS_ORDER | variable | _STATUS_ORDER: tuple[Status, ...] = ... |
| _tally | function | def _tally(graph: TaskGraph) -> Counter[Status] |
| _task_from_dict | function | def _task_from_dict(data: dict[str, object]) -> Task |
| _task_to_dict | function | def _task_to_dict(task: Task) -> dict[str, object] |
| load_graph | function | def load_graph(path: Path) -> TaskGraph |
| render_report | function | def render_report(graph: TaskGraph) -> str |
| save_graph | function | def save_graph(graph: TaskGraph, path: Path) -> None |
<!-- CDM:END symbols -->
